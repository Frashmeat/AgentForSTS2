# 归档脚本

本目录存放已经不再作为当前推荐入口维护的旧脚本。

当前仍建议直接使用的脚本位于：

- `tools/` 根目录中的安装、启动、开发辅助脚本
- `tools/latest/` 中的打包与部署脚本

已归档内容：

- `tools/archive/sandbox/`
  旧的 Windows Sandbox 安装验证链路。当前仓库不再把这套脚本作为主流程维护，因此移出 `tools/` 顶层，保留为历史参考。
