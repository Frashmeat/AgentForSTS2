# 2026-04-14 服务器模式 CLI 模型选择与凭据池实施计划

## 0. 2026-04-14 当前状态说明

- 本文用于把 [`2026-04-13-服务器模式CLI模型选择与凭据池草案.md`](../2026-04-13-服务器模式CLI模型选择与凭据池草案.md) 收束成正式执行入口。
- 当前状态：进行中
- 截至 `2026-04-29`，本专题已经完成第一批核心实现并从 `未开始/` 移入 `进行中/`；下面保留早期背景描述，后续进度以新增补充说明为准。
- 当前代码事实：
  - `execution_profiles`、`server_credentials`、`credential_health_checks`、`user_platform_preferences` 已有 ORM、migration、repository/service 与接口入口。
  - `/api/admin/platform/server-credentials*` 与 `/api/admin/platform/execution-profiles` 已接入管理员会话强制鉴权。
  - `ExecutionOrchestratorService`、`ExecutionRoutingRepository`、`ai_execution` 快照字段已经接入执行期凭据解析与一次自动切换。
  - 后续剩余重点不再是“是否开始实现”，而是多实例运行时协调、错误分类细化、熔断策略和文档口径收尾。

### 2026-04-14 进度补充

- [x] 已完成接口冻结稿
- [x] 已完成表结构冻结稿
- [x] 已完成管理员权限边界冻结稿
- [x] 已落第一批管理员权限前置代码：
  - `users` 增加 `is_admin`
  - `platform_admin` 路由改为必须管理员会话
  - 相关 auth / admin 定向测试已补齐并通过
- [x] 已落第一批服务器模式数据闭环：
  - `execution_profiles`
  - `server_credentials`
  - `credential_health_checks`
  - `user_platform_preferences`
  - `GET /api/platform/execution-profiles`
  - `GET /api/me/server-preferences`
  - `PUT /api/me/server-preferences`
- [x] 已补运行时服务器模式选择弹窗前端接线
  - 执行方式弹窗已读取 `/api/platform/execution-profiles` 与 `/api/me/server-preferences`
  - 创建平台任务时已显式携带 `selected_execution_profile_id / selected_agent_backend / selected_model`
  - 用户可在运行时把本次选择写回默认服务器配置
- [x] 已补设置页 `服务器模式` Tab
  - 设置页当前已拆分“工作站 / 服务器模式”页签
  - 服务器模式页签已可查看平台执行配置列表并保存 / 清空默认服务器配置
- [x] 已补默认服务器配置失效时的前端基础兜底提示
  - 运行时弹窗在默认值不可用时会显式提示，并自动回退到推荐 / 首个可用配置
  - 设置页在默认值失效时会展示警示文案，提示用户改选或清空
- [x] 已补用户中心执行配置展示
  - 历史列表当前会直接显示任务绑定的 `CLI / 模型`
  - 任务详情页当前会显示所选执行配置与 `selected_execution_profile_id`
- [x] 已补默认 `execution_profiles` 启动自举
  - `Codex CLI / gpt-5.4`
  - `Claude CLI / claude-sonnet-4-6`
- [x] 已把用户选择的服务器配置快照接入 `job`
  - `selected_execution_profile_id`
  - `selected_agent_backend`
  - `selected_model`
- [x] 已把实际命中快照接入 `ai_execution`
  - `credential_ref`
  - `retry_attempt`
  - `switched_credential`
- [x] 已开放管理员只读凭据列表接口
  - `GET /api/admin/platform/server-credentials`
- [x] 已开放管理员只读执行配置列表接口
  - `GET /api/admin/platform/execution-profiles`
- [x] 已落第一批管理员写接口
  - `POST /api/admin/platform/server-credentials`
  - `PUT /api/admin/platform/server-credentials/{id}`
  - `POST /api/admin/platform/server-credentials/{id}/enable`
  - `POST /api/admin/platform/server-credentials/{id}/disable`
  - `POST /api/admin/platform/server-credentials/{id}/health-check`
  - 写入 `credential_ciphertext / secret_ciphertext`
  - 成功响应不回显原始 `credential / secret`
- [x] 已补第一版服务器凭据密文写入能力
  - 优先使用 `SPIREFORGE_SERVER_CREDENTIAL_SECRET`
  - 未设置时回退 `auth.session_secret`
- [x] 已补第一版凭据状态回写能力
  - 手动检测写入 `credential_health_checks`
  - 同步更新 `server_credentials.health_status`
  - 启用 / 禁用会同步调整 `health_status`
- [x] 已补第一版执行期调度真源
  - `ExecutionOrchestratorService` 可基于 `job.selected_execution_profile_id` 自动解析同组合下首个 `enabled + healthy` 凭据
  - 自动写回 `ai_execution.provider / model / credential_ref / retry_attempt / switched_credential`
  - 第一版 `credential_ref` 固定为 `server-credential:{id}`
- [x] 已补同组合下一凭据路由基础能力
  - `ExecutionRoutingRepository` 当前已支持排除指定 `credential_id` 后继续解析下一把 `enabled + healthy` 凭据
  - `ExecutionRoutingService` 当前已新增 `resolve_retry_for_job(...)`
  - 该能力会保持同一 `execution_profile_id / agent_backend / model` 边界，只切到同组合内下一把健康凭据
  - 当前会返回 `retry_attempt=1` 与 `switched_credential=True`
- [x] 已补第一版同组合自动切换一次主链
  - `ExecutionOrchestratorService.run_registered_workflow_to_completion(...)` 当前已在 `failed_system + 可切换错误 + 尚未切换过` 时自动切到同组合下一把健康凭据重跑一次
  - 当前第一版可切换错误按 `error_summary` 关键词收口，先覆盖：
    - `401/403`
    - `429 / rate limit`
    - `quota`
    - `temporarily unavailable / unavailable`
    - `ConnectError / connection / timeout / getaddrinfo`
  - 触发自动切换前，当前还会把首次失败的凭据按错误类型同步回写到 `server_credentials.health_status`，并补一条 `credential_health_checks(trigger_source=runtime_retry)`
  - 当前仍未补更细的错误分类枚举与更完整的熔断策略
- [x] 已补第一版单用户并发上限
  - `JobApplicationService.start_job()` 当前会限制同一用户同时处于 `queued / running / cancelling` 的服务器任务数
  - 当前实现上限固定为 `2`
  - 超限时会直接返回：
    - `400 {"detail":"too many active server jobs for user: limit 2"}`
  - 当前仍未进入排队系统，策略先收口为“拒绝开始”，不继续额外占用执行资源
- [x] 已补第一版创建 / 开始接口限流
  - 当前 `POST /api/me/jobs`、`POST /api/platform/jobs`、`POST /api/me/jobs/{job_id}/start` 与 `POST /api/platform/jobs/{job_id}/start` 已接轻量内存限流器
  - 当前按“每用户、每动作、60 秒窗口内最多 5 次”收口
  - 超限时会直接返回 `429`
  - 当前仍是应用内存级限流，尚未升级到跨进程 / 持久化真源
- [x] 已补第一版真实 caller 前置回填
  - `/api/me/jobs` 与 `/api/platform/jobs` 在请求未显式传执行配置时，会优先继承当前用户“可用的默认服务器配置”
  - 使平台任务草稿可以先带上 `selected_execution_profile_id / selected_agent_backend / selected_model`
- [x] 已补第一版真实 caller 开始桥接
  - `/api/me/jobs/{job_id}/start` 与 `/api/platform/jobs/{job_id}/start` 已可复用 `ExecutionOrchestratorService.start_execution()`
  - 开始任务时会创建 `ai_execution`，并写入实际命中的 `provider / model / credential_ref`
  - 当前桥接边界先到 `ai_execution.status = dispatching`，尚未在 `web` runtime 内直接跑真实 runner
- [x] 已补第一版按次执行参数注入 contract
  - `StepExecutionRequest` 新增内部 `execution_binding`
  - `ExecutionOrchestratorService.run_registered_steps()` 在提供 `user_id` 时，已可基于当前任务自动解析服务器路由并构造执行绑定
  - 执行绑定当前已包含 `agent_backend / provider / model / credential_ref / auth_type / credential / secret / base_url`
  - 内部 runner 现已可收到与 `ai_execution` 对齐的执行上下文，而不必继续假定只能回读 runtime config
- [x] 已补第一版文本类 handler 消费 `execution_binding`
  - `ExecutionAdapter` 已支持 `text.generate`
  - 平台 runner 已新增 `text_generate_handler`
  - 该 handler 已可基于 `execution_binding` 构造文本执行所需 `llm_cfg`，并调用 `text_runner`
- [x] 已补第一版最小文本类真实执行闭环
  - `web` runtime 已开放最小平台 runner 单例：`workflow_registry / step_dispatcher / execution_adapter / workflow_runner`
  - 默认 workflow 注册已补 `("log_analysis", "log_analysis") -> [log.analyze]`
  - `ExecutionAdapter` 已支持 `log.analyze`
  - 平台 runner 已新增 `log_analysis_handler`
  - 该 handler 会读取服务器日志、组装分析 prompt，并委托 `text_generate_handler` 执行
  - `JobApplicationService.start_job()` 对已注册的最小文本类任务，已可在开始阶段同步推进 `ai_execution / job_item / job` 到完成态
- [x] 已补第二条最小文本类真实执行闭环
  - 默认 workflow 当前已继续推进为 `("batch_generate", "custom_code") -> [batch.custom_code.plan, code.generate, build.project]`
  - 平台 runner 已新增 `batch_custom_code_handler` 与 `code.generate` handler
  - `batch_custom_code_handler` 会把批量规划里的 `custom_code` 子项描述转成服务器可执行的文本实现方案 prompt；若任务已携带 `server_project_ref`，当前还会读取服务器工作区里的 `*.csproj / MainFile.cs / 源码样本` 摘要，一并注入 prompt
  - `code.generate` 会基于 `execution_binding` 在 `server_workspace_root` 上运行真实 CLI，把代码写入服务器工作区
  - `build.project` 会继续在同一服务器工作区内完成项目构建
  - 当前创建该任务时要求显式携带 `server_project_ref`
  - 当前仍只收口到服务器构建，不自动进入 deploy
- [x] 已补 `batch_generate/card` 的最小文本闭环
  - 默认 workflow 注册已补 `("batch_generate", "card") -> [single.asset.plan]`
  - `single_asset_plan_handler` 当前已兼容批量规划 payload 的 `name` 字段，并通过 step payload 注入 `asset_type=card`
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的批量卡牌子项；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补 `batch_generate/relic` 的最小文本闭环
  - 默认 workflow 注册已补 `("batch_generate", "relic") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，并通过 step payload 注入 `asset_type=relic`
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的批量遗物子项；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补 `batch_generate/power` 的最小文本闭环
  - 默认 workflow 注册已补 `("batch_generate", "power") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，并通过 step payload 注入 `asset_type=power`
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的批量 Power 子项；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补 `batch_generate/character` 的最小文本闭环
  - 默认 workflow 注册已补 `("batch_generate", "character") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，并通过 step payload 注入 `asset_type=character`
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的批量角色子项；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补 `batch_generate/card_fullscreen` 的最小文本闭环
  - 默认 workflow 注册已补 `("batch_generate", "card_fullscreen") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，并通过 step payload 注入 `asset_type=card_fullscreen`
  - 当前默认文本路径仍限定为“不依赖上传图片、只消费描述文本”的批量全画面卡子项
  - 当前已继续补第一条批量上传图驱动的真实链：若任务同时携带 `uploaded_asset_ref + server_project_ref`，会动态切到 `asset.generate + build.project`
- [x] 已补单资产 `custom_code` 的最小文本闭环
  - 默认 workflow 当前已继续推进为 `("single_generate", "custom_code") -> [batch.custom_code.plan, code.generate, build.project]`
  - `batch_custom_code_handler` 当前已统一收口到 `item_name`，并会复用服务器工作区摘要读取能力来增强文本方案
  - 平台 runner 当前已新增 `code.generate` handler，会基于 `execution_binding` 在 `server_workspace_root` 上运行真实 CLI，把代码写入服务器工作区
  - `build.project` 会继续在同一服务器工作区内完成项目构建
  - 当前创建该任务时要求显式携带 `server_project_ref`
  - 当前仍只收口到服务器构建，不自动进入 deploy
  - 当前已开放给平台 API / `/api/me/jobs` 调用，单资产前端界面也已暴露 `custom_code` 入口
  - 前端 `custom_code` 当前会跳过图片链，直接进入 Code Agent / 服务器任务
- [x] 已补单资产 `relic` 的最小文本闭环
  - 默认 workflow 注册已补 `("single_generate", "relic") -> [single.asset.plan]`
  - 平台 runner 已新增 `single_asset_plan_handler`，会基于单资产描述与 STS2/BaseLib 知识文档生成服务器侧文本实现方案
  - 若任务已携带 `server_project_ref`，该 handler 当前还会读取服务器工作区里的 `*.csproj / MainFile.cs / 源码样本` 摘要，一并注入 prompt
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的遗物任务；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补单资产 `card` 的最小文本闭环
  - 默认 workflow 注册已补 `("single_generate", "card") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，当前会基于卡牌描述与 STS2/BaseLib 文档生成服务器侧文本实现方案
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的卡牌任务；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补单资产 `card_fullscreen` 的最小文本闭环
  - 默认 workflow 注册已补 `("single_generate", "card_fullscreen") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，当前会基于全画面卡描述与 STS2/BaseLib 文档生成服务器侧文本实现方案
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的全画面卡任务；若同时携带上传图片，开始后仍会显式 deferred
  - 当前已继续补第一条上传图驱动的真实链：若任务同时携带 `uploaded_asset_ref + server_project_ref`，会动态切到 `asset.generate + build.project`
- [x] 已补单资产 `power` 的最小文本闭环
  - 默认 workflow 注册已补 `("single_generate", "power") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，当前会基于 Power 描述与 STS2/BaseLib 文档生成服务器侧文本实现方案
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的 Power 任务；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补单资产 `character` 的最小文本闭环
  - 默认 workflow 注册已补 `("single_generate", "character") -> [single.asset.plan]`
  - 继续复用 `single_asset_plan_handler`，当前会基于角色描述与 STS2/BaseLib 文档生成服务器侧文本实现方案
  - 当前边界明确限定为“不依赖上传图片、只消费描述文本”的角色任务；若同时携带上传图片，开始后仍会显式 deferred
- [x] 已补后端第一阶段平台 payload 收口
  - 平台 runner 当前统一只认 `item_name`
  - 平台创建任务当前会直接拒绝 `name / asset_name / project_root / has_uploaded_image / provided_image_*`
- [x] 已补前端第一阶段平台 payload 收口
  - 单资产与批量平台任务当前统一写入 `item_name`
  - 单资产与批量平台任务当前不再写入 `project_root / has_uploaded_image / name / asset_name`
  - 服务器模式下若任务依赖上传图片，前端当前会直接阻止进入服务器模式并提示改用本机执行
- [x] 已补第一版未接通任务的显式 deferred 语义
  - 对尚未注册到最小 web runner 的服务器任务，开始后会追加 `ai_execution.deferred` 事件
  - 当前 deferred payload 会明确区分：
    - `workflow_not_registered`
    - `local_project_root_required`
  - 当前 `job / job_item` 也会显式返回 `deferred`，`ai_execution` 会收口到 `completed_with_refund` 并即时返还额度，不再继续伪装成 `running / dispatching`
- [x] 已补第一版用户中心 deferred 消费链
  - 新增 `GET /api/me/jobs/{job_id}/events`
  - 用户中心任务详情页已开始消费事件流，并优先展示 `ai_execution.deferred` 阻塞原因

## 1. 定位与目标

本计划用于落地“服务器模式下用户只选择 `CLI + 模型`，平台后台维护同组合多凭据池并允许同组合内自动切换一次”的能力。

本专题要解决的核心问题是：

- 普通用户如何在服务器模式下稳定地选择平台提供的执行能力
- 平台如何把“用户可见组合”和“后台真实凭据”分层管理
- 任务如何同时记录“用户选择了什么”和“实际命中了什么”
- 管理员如何安全地维护服务器 key、查看状态并进行检测
- 高并发下如何避免服务器模式把平台主链拖垮

## 2. 当前执行顺序

在 `docs/03-方案/后端专题/` 当前主线中，本专题建议排在第 `3` 顺位：

1. `../进行中/2026-03-27-硬编码Prompt与知识文本模板化改造计划.md`
2. `./2026-04-10-知识库资产化改造计划.md`
3. `./2026-04-14-服务器模式CLI模型选择与凭据池实施计划.md`

排序原因：

- 前两项仍然属于当前后端主线上更靠前的收口任务。
- 服务器模式专题已经形成清晰设计边界，但尚未进入“必须立刻插队实现”的状态。
- 该专题对平台用户链路、管理员链路、数据模型和运行保护都有影响，更适合作为第三顺位专题成体系推进。

## 3. 已冻结的设计结论

- 用户进入服务器模式后，只选择 `CLI + 模型`，不选择 provider、base URL 或具体 key。
- 即使用户本地已有自己的 key，仍可主动使用服务器模式。
- 同一个 `CLI + 模型` 组合下允许挂多组服务器凭据。
- 首次执行失败时，只允许在同组合内自动切换一次。
- 自动切换不得跨 CLI、不得跨模型、不得跨组合降级。
- 每个用户全局只记录一个默认服务器配置。
- 默认服务器配置以服务端字段为真源，设置页与运行时弹窗共用同一份默认值。
- 设置页“服务器模式”与运行时选择流程复用同一套服务器配置选择弹窗。
- 管理员可以配置服务器凭据，并对单个凭据或某个组合进行状态检测。

## 4. 范围与非范围

### 4.1 本计划范围

- 平台用户侧服务器配置列表与默认值接口
- `execution_profiles`、`server_credentials`、`credential_health_checks`、`user_platform_preferences`
- `job / ai_execution` 的服务器模式快照字段
- 管理员凭据管理与手动检测链路
- 设置页 Tab 化与统一服务器配置选择弹窗
- 服务器模式第一版入队、限流、熔断和轻量降级

### 4.2 本计划非范围

- 重新定义 `web` / `workstation` 的后端角色边界
- 让普通用户直接看到 provider、key、base URL 或单把凭据详情
- 第一版就支持跨组合降级、跨模型回退或复杂路由策略
- 第一版就支持定时全自动巡检、成本看板和复杂容量调度

## 5. 代码现状带来的关键约束

基于当前仓库代码，后续实现必须遵守以下约束：

- `platform_admin` 当前是查询型路由，不应被误认为已经具备管理员写操作的鉴权基线。
- 当前未见独立管理员权限模型，因此“管理员凭据写接口”不能直接挂到现有 `/api/admin/*` 上裸跑。
- 当前 `ai_execution` 主体仍与 `job_item` 绑定，若要补服务器模式快照，必须明确“用户选择快照”和“实际命中事实”的字段落点。
- 现有平台主链已具备 `job / job_item / ai_execution / audit` 基础结构，新能力应沿既有平台主链增量扩展，而不是旁路另起一套记录系统。

## 6. 第一批最小闭环

当前建议第一批先做“可上线的最小闭环”，而不是一次性把所有增强项同时塞进来。

### 6.1 第一批必须完成

- 冻结用户侧接口：
  - `GET /api/platform/execution-profiles`
  - `GET /api/me/server-preferences`
  - `PUT /api/me/server-preferences`
- 冻结核心表与字段：
  - `execution_profiles`
  - `server_credentials`
  - `credential_health_checks`
  - `user_platform_preferences`
- 冻结快照字段落点：
  - `job` 记录用户选择快照
  - `ai_execution` 记录实际命中事实
- 补齐管理员权限边界后，再开放：
  - 管理员凭据增删改查
  - 手动健康检测
- 打通前端最小体验：
  - 运行时服务器配置选择弹窗
  - 设置页 `服务器模式` Tab
  - 统一默认值读写
- 打通第一版运行保护：
  - 任务入队
  - 创建/开始接口限流
  - 单用户并发上限
  - 同组合内一次自动切换

### 6.2 可以后置到流量上来再做

- 定时巡检
- 更复杂的健康评分和自动恢复
- provider 维度统计看板
- 成本与用量分析面板
- 更精细的资源池与队列分层

## 7. 分阶段实施顺序

## 7.1 阶段一：冻结接口与数据模型

目标：

- 冻结普通用户侧的服务器配置列表与默认值接口
- 冻结管理员凭据管理与检测接口草案
- 冻结 `execution_profiles`、`server_credentials`、`credential_health_checks`、`user_platform_preferences` 的字段口径
- 冻结 `job` 与 `ai_execution` 的服务器模式快照字段
- 冻结“允许自动切换”的错误分类和审计字段

本阶段产出：

- API 草案
- 表结构草案
- 错误分类与审计字段清单

依赖：

- 无代码前置，但必须以现有平台主链和管理员查询链路为背景约束

## 7.2 阶段二：补齐管理员权限边界

目标：

- 在认证或权限模型中补足管理员能力表达
- 为管理员写接口补齐强制鉴权
- 明确管理员接口和普通用户接口的边界

本阶段产出：

- 管理员权限模型
- 管理员接口鉴权约束
- 管理员接口归属说明

依赖：

- 阶段一已冻结管理员接口边界

说明：

- 在本阶段完成前，不开放服务器凭据写操作接口

## 7.3 阶段三：落地平台侧后端能力

目标：

- 落地 `execution_profiles`
- 落地 `server_credentials`
- 落地 `credential_health_checks`
- 落地 `user_platform_preferences`
- 打通用户默认服务器配置读写
- 打通管理员凭据管理与手动检测
- 在 `job / ai_execution` 中补齐服务器模式快照写入

本阶段产出：

- migration 与 ORM 模型
- 用户接口与管理员接口
- 服务层、仓储层和检测链路
- 执行期 `execution_profile -> credential` 最小解析真源

依赖：

- 阶段一、阶段二完成

## 7.4 阶段四：落地前端选择与设置体验

目标：

- 设置页改为顶部横向 Tabs
- 新增 `服务器模式` Tab
- 统一服务器配置选择弹窗
- 让设置页与运行时共用同一套选择器和同一套接口

本阶段产出：

- 运行时选择体验
- 默认服务器配置管理体验
- 默认值失效时的弹窗兜底

依赖：

- 阶段三的用户侧接口稳定可用

## 7.5 阶段五：落地抗压与运行保护

目标：

- 任务入队
- 执行资源池限流
- 单用户并发上限
- 创建 / 开始任务接口限流
- 凭据健康熔断
- 轻量状态查询与降级反馈

本阶段产出：

- 第一版服务器模式保护机制
- 与自动切换联动的最小排障能力

依赖：

- 阶段三已能提供健康状态、执行结果与审计基础数据

## 7.6 阶段六：文档回写与基线收口

目标：

- 回写实施计划、实施基线和接口文档
- 回写管理员与运维口径
- 视实现结果决定是否把草案归档

本阶段产出：

- 更新后的实施计划
- 更新后的实施基线
- 对应接口文档与运维文档

依赖：

- 前述阶段至少完成第一批闭环

## 8. 推荐执行顺序

若进入正式落地，建议严格按以下顺序推进：

1. 先冻结 API 草案和字段口径
2. 再补齐管理员权限边界
3. 再进入平台后端数据层与服务层实现
4. 再落地前端选择器与设置页 Tab 化
5. 最后补抗压保护、文档回写与基线收口

该顺序当前不建议调整，原因是：

- 如果接口和表结构不先冻结，前后端会反复返工
- 如果管理员权限边界不先补齐，凭据管理接口风险过高
- 如果运行保护太晚才考虑，服务器模式一旦放量会直接放大平台压力

## 9. 风险

### 9.1 管理员权限边界缺失

- 若直接在现有 `/api/admin/*` 上扩写接口，会把“查询入口”误当成“已具备高权限基线”的能力，安全风险最高。

### 9.2 用户选择与实际执行脱节

- 若只记录用户选择、不记录实际命中凭据，后续排障、审计和返还解释都会失真。

### 9.3 数据落点摇摆

- 若 `execution_profile` 快照在 `job`、`job_item`、`ai_execution` 间反复摇摆，前后端语义会难以统一。

### 9.4 设置页与运行时分裂

- 若设置页和运行时各自维护一套选择器与默认值逻辑，后续一定会出现列表、状态和说明文案不一致。

### 9.5 高并发下缺少保护

- 若服务器模式在没有入队、限流、熔断前就放量，高并发下会拖慢 `web` 主链并放大坏凭据重试风暴。

### 9.6 服务器工作区缺少串行保护

- 当前服务器模式虽然已经把工作区落到 `server_project_ref -> user_id/token/project_name` 路径下，但同一个 `server_project_ref` 仍可能被多个任务并发复用。
- 若多个任务同时在同一服务器工作区内执行 `code.generate`、`build.project` 或上传图生成链，可能出现源码互相覆盖、构建中间产物互相污染、结果归属混淆等问题。
- 后续应把“同一 `server_project_ref` 在同一时刻只允许一条写型执行链运行”明确为运行时保护的一部分，至少先补工作区级串行锁或等价语义。

### 9.7 共享部署目录存在同名冲突

- 当前 `build.project` 完成后若进入服务器部署，会把构建产物落到共享 `Mods/{project_root.name}` 目录。
- 该口径在“不同用户创建同名项目”或“同一用户多个服务器工作区使用同名项目”时都可能发生覆盖、误读旧产物或错误复用已有部署结果。
- 当前已冻结第一版策略为“构建隔离，部署独占”：
  - 构建仍在各自 `server_workspace_root` 下完成
  - 最终部署目录暂时继续保持 `Mods/{project_name}`
  - 同名 `project_name` 的部署链必须串行，冲突先复用 `queued + server_deploy_target_busy`

## 10. 验收标准

当以下条件满足时，可认为本专题第一批闭环完成：

- 普通用户在服务器模式下只能看到 `CLI + 模型` 能力列表，不暴露底层凭据细节。
- 每个用户只有一份服务端真源的默认服务器配置，设置页与运行时共用。
- 平台能同时记录：
  - 用户选择的服务器配置组合
  - 单次执行实际命中的凭据与是否发生切换
- 管理员可以安全地维护凭据、启停凭据并进行手动检测。
- 默认配置失效时，运行时会要求重新选择，而不是静默失败。
- 服务器模式具备最小入队、限流和熔断保护。
- 实现完成后，相关实施计划、接口文档和基线文档已同步回写。

## 11. 与草案文档的关系

- 本文是当前正式执行入口。
- [`2026-04-13-服务器模式CLI模型选择与凭据池草案.md`](../2026-04-13-服务器模式CLI模型选择与凭据池草案.md) 继续保留，用于承载更细的设计讨论、扩展想法和历史推导过程。
- 后续若本计划的阶段产出已经被实现吸收，应优先回写本文，再决定是否把草案转入归档。

## 12. 当前立即执行项

> 以下 4 项是立项冻结期的历史记录。截至 `2026-04-15`，它们已经完成；当前主攻方向已转为“真实运行时 caller 接入已落地的调度真源”。

如果现在开始推进本专题，当前建议不要直接写业务代码，而是先完成下面 4 项冻结动作：

1. 冻结普通用户侧 3 个接口 contract
   - `GET /api/platform/execution-profiles`
   - `GET /api/me/server-preferences`
   - `PUT /api/me/server-preferences`

2. 冻结管理员侧第一批接口 contract
   - 服务器凭据列表
   - 新增服务器凭据
   - 手动检测服务器凭据

3. 冻结 4 张核心表的最小字段与关系
   - `execution_profiles`
   - `server_credentials`
   - `credential_health_checks`
   - `user_platform_preferences`

4. 冻结任务快照字段落点
   - `job` 记录用户选择的 `execution_profile`
   - `ai_execution` 记录实际命中的 provider / credential / retry / switched_credential`

### 为什么现在先做这 4 项

- 当前最大的风险不是“不会实现”，而是“接口、表结构和权限边界还没冻结就先写代码”。
- 管理员权限边界仍未补齐，直接上凭据管理实现会把高风险操作暴露到错误的阶段。
- 前端设置页、运行时弹窗、平台任务主链和管理员后台都会依赖同一组字段；这组字段不先冻结，后面一定返工。

### 当前不建议立刻做的事

- 不建议现在直接补 migration
- 不建议现在直接补管理员写接口
- 不建议现在直接改前端设置页或运行时选择弹窗
- 不建议现在直接做自动切换执行逻辑

### 建议的下一个实际产出

当前最合适的下一个文档产出是：

- [`../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池接口冻结稿.md`](../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池接口冻结稿.md)
- [`../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池表结构冻结稿.md`](../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池表结构冻结稿.md)
- [`../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池管理员权限边界冻结稿.md`](../已经完成/2026-04-14-服务器模式CLI模型选择与凭据池管理员权限边界冻结稿.md)

如果只做一件事，优先先做 `接口冻结稿`。

### 2026-04-20 运行时保护补充产出

结合当前已识别出的多人并行风险，后续新增的文档产出建议补为：

- [`../已经完成/2026-04-20-服务器工作区串行保护冻结稿.md`](../已经完成/2026-04-20-服务器工作区串行保护冻结稿.md)
- [`../已经完成/2026-04-20-服务器部署目录隔离与占用策略冻结稿.md`](../已经完成/2026-04-20-服务器部署目录隔离与占用策略冻结稿.md)
- [`../已经完成/2026-04-20-服务器工作区写锁第一版实施计划.md`](../已经完成/2026-04-20-服务器工作区写锁第一版实施计划.md)

这两份文档的定位不是替代接口 / 表结构冻结稿，而是为“排队语义之后的运行时保护”补足工作区与部署目录两类共享资源约束。

### 2026-04-20 工作区写锁第一版进度补充

截至 `2026-04-20`，工作区串行保护已不再停留在冻结稿阶段，当前已开始落第一版实现：

- 已新增 `server_project_ref` 级别文件锁服务
- 已把写型 workflow 的工作区锁接到 `ExecutionOrchestratorService.start_execution()` 开始阶段
- 第一版写型识别当前覆盖：
  - `code.generate`
  - `asset.generate`
  - `build.project`
- 若同一服务器工作区已被占用，当前 `POST /api/platform/jobs/{job_id}/start` 与 `POST /api/me/jobs/{job_id}/start` 会返回：
  - `200 {"status":"queued"}`
  - 并在任务查询结果中补：
    - `queued_reason_code = "server_workspace_busy"`
    - `queued_reason_message = "server workspace is busy"`
- 当前已补同进程内最小续跑：当先占用同一工作区的任务完成后，会继续尝试拉起下一个 `queued` 任务
- 当前仍未接独立后台队列 / 跨进程消费，也未处理共享部署目录占用；这两项继续留在后续步骤

### 2026-04-20 共享部署目录占用保护进度补充

截至 `2026-04-20`，共享部署目录治理也已从冻结稿进入第一版实现：

- 已新增 `project_name` 级别部署目录锁服务，资源键固定为共享 `Mods/{project_name}`
- `build.project` 当前会在“构建完成、准备复制到 Mods”这一刻尝试获取部署锁
- 若同名部署目录正在被另一条部署链占用，当前会把该次执行回收到：
  - `job.status = queued`
  - `job_item.status = ready`
  - `queued_reason_code = "server_deploy_target_busy"`
  - `queued_reason_message = "server deploy target is busy"`
- 当前已补同进程内最小续跑：当先占用同名部署目录的任务完成后，会继续尝试拉起下一个同名 `project_name` 的 `queued` 任务
- `build.project` 与旧 `/api/ws/build-deploy` 当前都不再把目标目录里已有旧 `.dll/.pck` 直接当作本次成功结果
- 旧 `/api/ws/build-deploy` 当前也已接入同一类部署目录锁；若目录正忙，会直接返回 `server_deploy_target_busy`
- 当前已补第一版部署登记元数据：
  - 成功部署后会在 `Mods/{project_name}/.server-deploy.json` 写入最近一次成功部署事实
  - 当前至少记录 `project_name / server_project_ref / user_id / deployed_at / source_workspace_root`
  - 平台 `build.project` 与旧 `/api/ws/build-deploy` 当前都复用同一格式
- 当前仍未接跨进程 / 多实例真源队列；该项继续留在后续步骤

### 2026-04-20 部署登记元数据第一版进度补充

截至 `2026-04-20`，部署登记元数据也已推进到第一版最小实现：

- 已新增独立部署登记服务，统一负责写入 `Mods/{project_name}/.server-deploy.json`
- 当前写入时机固定为：
  - 本次构建产物复制成功之后
  - 部署锁仍在持有期间
- 当前登记字段包括：
  - `schema_version`
  - `project_name`
  - `job_id`
  - `job_item_id`
  - `user_id`
  - `server_project_ref`
  - `source_workspace_root`
  - `deployed_at`
  - `deployed_to`
  - `entrypoint`
  - `file_names`
- 当前明确边界：
  - 只覆盖最近一次成功部署事实
  - 不保留完整历史
  - 不等同于跨进程 / 多实例真源调度

### 2026-04-20 后台排队消费第一版进度补充

截至 `2026-04-20`，`queued` 的恢复机制也已从“只靠顺手续跑”推进到第一版后台消费：

- `web` app 启动后，当前会注册单实例后台 queue worker
- 后台 worker 会周期性扫描可尝试恢复的服务器 `queued` 任务，并调用：
  - `start_job(triggered_by="system_queue_worker")`
- 当前候选任务筛选条件为：
  - `jobs.status = queued`
  - `selected_execution_profile_id is not null`
  - 至少存在一条 `job_item.status = ready`
  - 按 `started_at / id` 先后顺序挑最早候选项
- 当前已补最小退避：
  - 基于最近一次 `job.queued` 事件时间做冷却窗口，避免对持续 busy 的任务空转重试
- 当前后台恢复继续复用既有主链：
  - 工作区忙仍保持 `server_workspace_busy`
  - 部署目录忙仍保持 `server_deploy_target_busy`
- 当前明确边界：
  - 只承诺单 `web` 实例语义
  - 仍未实现跨进程 / 多实例真源排队消费

### 2026-04-15 当前主攻方向补充

截至 `2026-04-15`，更贴近代码事实的下一步应为：

1. 把真实运行时 caller 接到已落地的执行期调度真源
   - 复用 `job.selected_execution_profile_id`
   - 不再由外部手工传入 `provider / model / credential_ref`

2. 在 caller 接通后再补失败重试和同组合自动切换一次
   - 不在当前未查清失败链路前抢跑

3. 后续再推进剩余前端服务器模式体验与抗压保护

### 2026-04-16 当前主攻方向补充

截至 `2026-04-16`，按次执行参数注入的最小 contract 已经落地，下一步应进一步收敛为：

1. 继续扩展更多可服务器执行的任务
   - `log_analysis`、`batch_generate/custom_code`、`batch_generate/card`、`batch_generate/relic`、`batch_generate/power`、`batch_generate/character`、`batch_generate/card_fullscreen`、`single_generate/custom_code`、`single_generate/relic`、`single_generate/card`、`single_generate/card_fullscreen`、`single_generate/power` 与 `single_generate/character` 已成为十三条最小真实闭环
   - 其中 `batch_generate/custom_code` 与 `single_generate/custom_code` 已进一步推进到“文本规划 + 服务器写代码 + 服务器构建”三步流；`batch_generate/card_fullscreen` 与 `single_generate/card_fullscreen` 也已补上“上传图 + 服务器资产代码生成 + 服务器构建”路径
   - 下一步应继续把同样不依赖本地 `project_root`、不依赖上传图片的任务映射到新 runner

2. 收敛更广泛任务的服务器可执行 payload 语义
   - 当前前后端第一阶段已完成平台 payload 主线收口
   - 当前只有 `single_generate/card_fullscreen` 与 `batch_generate/card_fullscreen` 的上传图路径已进入真实服务器执行；其余图片上传与用户本机 `project_root` 等字段仍不适合直接带入服务器执行
   - 下一步应进入工作目录、输入引用和资产持久化边界

3. 后续再补更完整的前端体验与抗压保护

### 2026-04-17 下一步落地顺序

基于当前已经形成的十三条最小服务器文本闭环，后续建议严格按下面顺序推进，避免同时摊大范围：

1. 继续扩展剩余批量文本资产类型
   - 当前批量纯文本资产的最小闭环已进一步收口
   - 下一步应改为评估是否还存在其他“不依赖本地 `project_root`、不依赖上传图片”的文本任务可直接映射到现有 runner

2. 再补仍可走纯文本语义的其他任务类型
   - 优先筛选标准：
     - 不依赖用户本机 `project_root`
     - 不依赖上传图片持久化
     - `input_payload` 已经包含足够的服务器侧描述语义
   - 每接通一个任务类型，都继续维持“小任务、小提交、同步文档”的节奏

3. 再开始收敛批量与单资产 payload 语义
   - 统一 `name / asset_name / asset_type / has_uploaded_image / needs_image` 的服务器执行约束
   - 逐步减少“同一 handler 兼容多种 payload 形态”的临时分支

4. 再推进更完整的阻塞语义治理
   - 对仍未接通的任务类型，继续维护稳定的 `workflow_not_registered / local_project_root_required` 口径
   - 视情况补更细粒度的 deferred subtype，而不是过早扩大主状态枚举

5. 等文本任务覆盖到阶段目标后，再集中做一次后端最小回归验证
   - 继续避免过早跑 build、全量测试和重型验证
   - 先把这一轮服务器文本任务的主链补齐，再做集中校验更划算

### 2026-04-19 当前后续任务选择

基于截至 `2026-04-19` 的代码事实，前后端平台 payload 一期主线已经基本收口：

- 平台创建任务已拒绝旧字段
- 平台 runner 已统一只认 `item_name`
- 已接通资产类 workflow 的 `asset_type` 已下沉到 workflow step 内部注入
- 前端平台 caller 也已停止显式发送 `asset_type`

因此，当前不再建议继续优先补新的文本 workflow 类型；后续任务应改为按下面顺序收口尾部差口：

1. 先完成工作站本地链路的历史输入收口
   - 优先处理 `backend/routers/workflow.py`
   - `2026-04-19` 已完成：不再保留 `provided_image_path` 的本地路径 fallback
   - `2026-04-19` 已完成：删除仅服务于历史兼容分支的注释和返回约定
   - 当前平台链路与本地链路的 payload 边界已进一步稳定

2. 再完成历史命名收口
   - 优先处理 `backend/app/shared/infra/config/settings.py`
   - `2026-04-19` 已完成：把 `legacy_direct` 一类历史命名收口为 `direct_execute`
   - 当前已避免“主链已变、命名仍停留在兼容期”的认知漂移

3. 然后做一次服务器模式专题的最小集中回归与文档回写
   - 以当前已接通的 `log_analysis`、`custom_code`、文本资产类与上传图 `card_fullscreen` 为主
   - 不上 build、不过早跑全量测试，只做能证明主链未回退的局部验证
   - 验证后继续回写实施计划、接口文档和基线口径

4. 完成上述尾部清理后，再回到专题主目标剩余项
   - 同组合失败重试与自动切换一次
   - 入队、限流、单用户并发上限、凭据健康熔断
   - 更完整的前端服务器模式体验与抗压保护

### 2026-04-19 当前完成度判断

如果按“平台 payload / 输入引用 / caller 主线收口”这个子目标看，当前已经进入收尾阶段：

- 主体闭环已形成
- 剩余主要是尾部兼容层、历史命名和集中验证

但如果按本文整个“服务器模式 CLI / 模型选择 / 凭据池第一批闭环”看，仍未到可宣布完成的状态，主要还差：

- 同组合自动切换与失败保护
- 入队、限流、并发和熔断等运行保护
- 更完整的文档回写与基线收口

因此，当前更准确的判断应是：

- `payload 收口子目标`：已接近完成
- `服务器模式专题第一批闭环`：已过主体实现阶段，但还没有完成最终收口

### 2026-04-19 最小集中回归补充

截至 `2026-04-19`，本专题已完成一轮面向当前主链的最小集中回归，验证范围聚焦于：

- 配置默认值与历史命名归一化
- 工作站本地审批 / 上传图链
- 平台任务服务层已接通主链
- 平台路由与 `/api/me/jobs` 已接通主链

本轮已通过的最小验证包括：

- `python -m py_compile`
  - `backend/app/shared/infra/config/settings.py`
  - `backend/routers/workflow.py`
  - `backend/routers/_platform_execution_support.py`
  - `backend/tests/test_approval_config.py`
  - `backend/tests/test_container_registry.py`
  - `backend/tests/test_workflow_approval_mode.py`
  - `backend/tests/platform/services/test_job_application_service.py`
  - `backend/tests/platform/routers/test_platform_jobs_http.py`
  - `backend/tests/auth/test_me_router.py`
- `pytest backend/tests/test_approval_config.py backend/tests/test_container_registry.py backend/tests/test_workflow_approval_mode.py -q`
  - `16 passed`
- `pytest backend/tests/platform/services/test_job_application_service.py -q -k "single_card or batch_card_fullscreen"`
  - `7 passed`
- 在临时 stub 图像运行时依赖后：
  - `backend/tests/platform/routers/test_platform_jobs_http.py -q -k "single_card or batch_card_fullscreen"`
    - `5 passed`
  - `backend/tests/auth/test_me_router.py -q -k "single_card or batch_card_fullscreen"`
    - `5 passed`

补充判断：

- 当前主链的“配置命名收口 + payload / caller 收口 + 工作站本地上传图收口”已经具备稳定验证证据
- 后续剩余重点应从“继续扫尾兼容字段”切换为“专题级运行保护与最终文档/基线收口”

### 2026-04-19 当前直接后续步骤

结合截至当前的代码事实，后续建议不再继续扩散到新任务类型，而是按下面顺序把本专题收口：

1. 先补入队 / 排队语义
   - 当前虽然已补同组合自动切换、单用户并发上限与 create/start 接口限流
   - 但超限时仍统一表现为“直接拒绝”，尚未进入明确的排队状态
   - 下一步应先决定：哪些场景进入 `queued`，哪些场景继续直接拒绝

2. 再补更完整的凭据熔断策略
   - 当前自动切换前已会同步回写坏凭据健康状态
   - 但错误分类仍是第一版关键词收口，且没有更细的熔断恢复策略
   - 下一步应继续明确哪些错误写 `rate_limited`、哪些写 `degraded`、哪些只记录不熔断

3. 再补服务器工作区级运行保护
   - 先把“同一 `server_project_ref` 是否允许并发执行”收成明确结论
   - 第一优先级不是扩更多任务类型，而是补工作区级串行保护，避免多个任务同时写同一服务器工作区
   - 同时要收口共享部署目录冲突：`Mods/{project_root.name}` 不能继续默认承担唯一部署键语义

4. 然后做一次专题级文档 / 基线最终回写
   - 把已经完成的运行保护、自动切换与 payload 收口统一回写到当前接口文档、实施计划和基线口径
   - 视剩余差口决定是否把部分方案文档转为归档或阶段总结
