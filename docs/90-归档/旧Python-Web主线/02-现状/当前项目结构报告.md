# 当前项目结构报告

## 文档定位

本文档描述的是**兼容层收口实施后的目标结构 / 当前实施分支状态**，基线来自：

- `I:\WebCode\AgentTheSpire\.worktrees\compat-layer-cleanup`

这不是对主工作区当前未合入状态的逐文件扫描结果。若主工作区尚未完成合入，应以本报告视为**目标结构说明**，避免与当前主干工作区现状混淆。

## 当前阶段判断

- 事实：项目状态应描述为**主干收口完成，兼容层尾部清理阶段**
- 事实：前后端主路径已经基本收口到统一实现
- 事实：仍保留少量兼容转发层，属于后续尾部清理对象，而非主流程承载层

## 前端目标结构

### 页面层

- 事实：`frontend/src/pages/ModEditor.tsx` 已移除
- 事实：`frontend/src/pages/LogAnalysis.tsx` 已移除
- 事实：`frontend/src/pages/BatchMode.tsx` 现为薄壳入口
- 事实：批量生成功能主体已收口到 `frontend/src/features/batch-generation/view.tsx`

这表示页面层继续向“路由入口薄壳 + feature 主体承载”收拢，兼容层不再分散保留多个旧页面实现。

### 应用装配与配置

- 事实：`frontend/src/App.tsx` 不再使用 `use_unified_ws_contract`
- 事实：`frontend/src/shared/api/config.ts` 中迁移开关已删除
- 事实：`frontend/src/shared/ws/facade.ts` 已收口为统一 WS facade

当前前端目标结构可概括为：

- `App.tsx` 直接依赖收口后的统一应用装配方式
- API 配置层不再保留迁移期切换开关
- WebSocket 能力统一收口到单一 facade，而不是并行维护新旧契约

## 后端目标结构

### 模块 API 收口

- 事实：新增 `backend/app/modules/planning/api.py`
- 事实：新增 `backend/app/modules/codegen/api.py`

这两个模块 API 文件表示后端能力已经从旧兼容入口进一步收口到 `app.modules.*.api` 统一出口。

### Router 依赖方向

- 事实：`backend/routers/workflow.py` 已直接依赖 `app.modules.*.api`
- 事实：`backend/routers/batch_workflow.py` 已直接依赖 `app.modules.*.api`
- 事实：`backend/routers/build_deploy.py` 已直接依赖 `app.modules.*.api`

目标依赖关系应理解为：

- Router 直接依赖模块 API
- 模块 API 承载稳定业务入口
- 兼容代理不再作为主干调用路径

### 兼容层尾部对象

- 事实：`backend/agents/planner.py` 现为兼容转发
- 事实：`backend/agents/code_agent.py` 现为兼容转发
- 事实：以上两个文件属于尾部清理对象

因此，当前后端并非仍处于“新旧并行主路径”状态，而是已经完成主路径收口，仅剩兼容转发残留待删。

### 特性开关清理

- 事实：`backend/app/shared/infra/feature_flags.py` 已删除

这意味着本轮收口后的目标结构不再依赖迁移期 feature flag 来切换主路径。

## 结构结论

基于 `compat-layer-cleanup` worktree 的已实施状态，当前项目的目标结构可以总结为：

1. 前端旧页面入口继续减少，页面层仅保留必要薄壳。
2. 批量生成主实现已集中到 feature 目录，而不是散落在 page 文件中。
3. 前端 API/WS 迁移期开关已撤除，统一契约成为默认且唯一主路径。
4. 后端 Router 已直接面向 `app.modules.*.api`，模块 API 成为稳定入口。
5. 旧 Agent 入口仅剩兼容转发职责，属于可继续删除的尾部兼容层。
6. 前后端迁移期开关均已清理，说明主干收口已经完成。

## 与主工作区的关系说明

- 事实：本报告以 `I:\WebCode\AgentTheSpire\.worktrees\compat-layer-cleanup` 的已实施状态为基线
- 风险：若主工作区尚未合入这些变更，则主工作区实际文件状态可能与本文档不同
- 要求：阅读本文档时，应将其理解为“兼容层收口实施后的目标结构说明 / 当前实施分支状态”，而不是默认等同于主工作区现状

## 后续清理建议

- 建议：在主干合入后，继续删除 `backend/agents/planner.py` 与 `backend/agents/code_agent.py` 等兼容转发残留
- 建议：若后续再更新本报告，应继续以“是否仍存在兼容转发层”作为阶段判断标准
- 建议：当尾部兼容转发全部删除后，再将项目阶段描述从“兼容层尾部清理阶段”更新为“兼容层清理完成”
