# 2026-04-10 知识库主线阶段总结

## 1. 本轮目标

把当前 STS2 / BaseLib 相关知识来源，从“分散在多处、运行时临时判断”的状态，收口到：

- 运行时知识资产目录作为唯一运行时真源
- 仓库内静态资料仅作为打包与初始化 seed source
- 工作流只消费知识状态，不在执行链中构建知识库

## 2. 已完成事项

### 2.1 配置与工具链

- [x] `tools/install/install.ps1` 已纳入 `ilspycmd` 安装
- [x] `tools/dev/verify-install-bat.ps1` 已补 `ilspycmd` 安装校验
- [x] 知识库更新时 `ilspycmd` 查找顺序改为：
  1. 先查项目目录
  2. 再查 `PATH`

### 2.2 Claude API 语义收口

- [x] `llm.mode` 已收口为：
  - `agent_cli`
  - `claude_api`
- [x] `llm.provider` 已退出文本 / 代码主链的用户显式配置
- [x] 设置页不再显示 `provider`
- [x] `Claude API` 模式下，文本和代码统一复用：
  - `model`
  - `api_key`
  - `base_url`

### 2.3 知识来源统一

- [x] 新增统一知识来源 facade：
  - `backend/app/modules/knowledge/application/knowledge_facade.py`
- [x] `codegen.api` 已切到 facade，不再自己拼装多头来源
- [x] 游戏知识来源优先级已明确：
  1. `runtime/knowledge/game/`
  2. `missing`
- [x] Baselib 来源优先级已明确：
  1. `runtime/knowledge/baselib/BaseLib.decompiled.cs`
  2. `missing`

### 2.4 知识库状态与前端融合

- [x] 设置页保留完整知识库状态中心
- [x] 单资产 / Mod 规划 / Mod 修改 / 日志分析 四个工作流页面已统一软提示
- [x] 只有 `missing` 时，执行前弹轻确认
- [x] 工作流页可直接：
  - 打开设置
  - 查看知识库说明
- [x] 前端已能显示当前实际使用来源：
  - 游戏知识来源
  - Baselib 知识来源

### 2.5 静态参考文档基线

- [x] `backend/agents/sts2_api_reference.md` 已补当前游戏版本标注
- [x] 该文档已明确视为仓库静态参考，而非运行时唯一真源

## 3. 当前有效结构

### 3.1 仓库静态 seed source

- `backend/agents/sts2_api_reference.md`
- `backend/agents/baselib_src/BaseLib.decompiled.cs`
- `backend/app/modules/knowledge/resources/sts2/*.md`

### 3.2 运行时知识资产目录

- `runtime/knowledge/knowledge-manifest.json`
- `runtime/knowledge/game/`
- `runtime/knowledge/baselib/`
- `runtime/knowledge/resources/sts2/`
- `runtime/knowledge/cache/`

### 3.3 消费入口

- `backend/app/modules/knowledge/application/knowledge_facade.py`
- `backend/app/modules/codegen/api.py`

## 4. 已验证项

### 4.1 后端

- [x] `backend/tests/test_llm_config_normalization.py`
- [x] `backend/tests/test_text_runner_selection.py`
- [x] `backend/tests/test_agent_runner_selection.py`
- [x] `backend/tests/test_stream_metadata.py`
- [x] `backend/tests/test_local_ai_capability_status.py`
- [x] `backend/tests/test_log_analyzer.py`
- [x] `backend/tests/test_mod_analyzer.py`
- [x] `backend/tests/test_knowledge_runtime.py`
- [x] `backend/tests/test_knowledge_router.py`
- [x] `backend/tests/test_knowledge_facade.py`
- [x] `backend/tests/test_code_agent_lookup.py`
- [x] `backend/tests/test_claude_cli_resolution.py`
- [x] `backend/tests/test_workflow_approval_mode.py`

### 4.2 前端

- [x] `frontend/tests/settingsApiModeField.test.js`
- [x] `frontend/tests/knowledgeSettingsPanel.test.js`
- [x] `frontend/tests/knowledgeSingleAssetNotice.test.js`
- [x] `frontend/tests/knowledgeWorkflowBanner.test.ts`
- [x] `frontend/tests/serverFlowGate.test.ts`
- [x] `frontend/tests/configApi.test.ts`
- [x] `frontend/tests/executionModeDialog.test.ts`
- [x] `frontend/tests/settingsPageRoute.test.ts`
- [x] `frontend/tests/settingsSaveFailure.test.js`
- [x] `frontend/tsconfig.json --noEmit`

## 5. 当前未纳入本线的剩余事项

以下内容仍在工作区，但不属于“知识库主线”本轮范围，应作为独立主题处理：

- STS2 项目路径 / `local.props` 同步修复
- `build_deploy` 构建前兜底同步
- `project_utils.py` 与部署链相关修改
- `tools/latest/deploy-docker.ps1` 与相关文档 / 测试收口

这些事项不应再混入当前知识库主线提交。

## 6. 当前建议的下一步

如果后续继续推进知识库主线，推荐顺序：

1. 清理历史文档与设计稿中残留的旧目录命名
2. 继续把 `knowledge_path` 作为对外主口径，逐步降低 `decompiled_src_path` 的存在感
3. 如后续需要，再独立推进知识库资产化下一阶段

## 7. 阶段结论

当前知识库主线已经完成第一轮结构性收口：

- 语义已统一
- 来源已分层
- 前端已融合
- 工具前提已接入
- 关键测试已覆盖

截至 2026-04-14，这条主线已经完成最终收尾：

- 前端展示与 codegen prompt 已优先使用 `knowledge_path`
- manifest 已优先写入并读取 `knowledge_path`
- 历史文档中的旧目录命名口径已完成清理

可以正式视为“已完成”，不必继续扩大到构建部署、项目路径同步等其他主题。
