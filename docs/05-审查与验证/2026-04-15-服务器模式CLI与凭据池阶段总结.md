# 2026-04-15 服务器模式 CLI 与凭据池阶段总结

## 1. 原定目标

本阶段的目标不是一次性做完服务器模式全链路，而是把该专题从“纯草案讨论”推进到“可安全进入实现，并已有第一批后端落地”的状态。

本阶段重点包括：

- 把服务器模式专题从草案收束成正式实施入口
- 冻结接口、表结构和管理员权限边界
- 补齐管理员最小权限前置能力
- 落第一批服务器模式数据闭环
- 让任务主链开始记录服务器模式选择与执行事实

## 2. 完成情况

### 2.1 已完成的文档冻结

- [x] `未开始-2026-04-14-服务器模式CLI模型选择与凭据池实施计划.md`
- [x] `未开始-2026-04-14-服务器模式CLI模型选择与凭据池接口冻结稿.md`
- [x] `未开始-2026-04-14-服务器模式CLI模型选择与凭据池表结构冻结稿.md`
- [x] `未开始-2026-04-14-服务器模式CLI模型选择与凭据池管理员权限边界冻结稿.md`

### 2.2 已完成的后端代码落地

- [x] auth 用户模型增加 `is_admin`
- [x] `/api/admin/*` 查询链路改为必须管理员会话
- [x] 落地 `execution_profiles`
- [x] 落地 `server_credentials`
- [x] 落地 `credential_health_checks`
- [x] 落地 `user_platform_preferences`
- [x] 落地 `GET /api/platform/execution-profiles`
- [x] 落地 `GET /api/me/server-preferences`
- [x] 落地 `PUT /api/me/server-preferences`
- [x] 默认执行配置在 `web` 启动时自动自举
- [x] `job` 增加用户选择服务器配置快照：
  - `selected_execution_profile_id`
  - `selected_agent_backend`
  - `selected_model`
- [x] `ai_execution` 增加实际命中快照：
  - `credential_ref`
  - `retry_attempt`
  - `switched_credential`
- [x] 落地 `GET /api/admin/platform/server-credentials`
- [x] 落地 `GET /api/admin/platform/execution-profiles`
- [x] 落地 `POST /api/admin/platform/server-credentials`
- [x] 落地 `PUT /api/admin/platform/server-credentials/{id}`
- [x] 落地 `POST /api/admin/platform/server-credentials/{id}/enable`
- [x] 落地 `POST /api/admin/platform/server-credentials/{id}/disable`
- [x] 落地 `POST /api/admin/platform/server-credentials/{id}/health-check`
- [x] 补第一版服务器凭据密文写入能力
  - 优先使用 `SPIREFORGE_SERVER_CREDENTIAL_SECRET`
  - 未设置时回退 `auth.session_secret`
  - 成功响应不回显原始 `credential / secret`
- [x] 检测结果可回写 `credential_health_checks`
- [x] 凭据状态字段可随启停和手动检测同步更新
- [x] 已补第一版执行期调度真源
  - `ExecutionOrchestratorService` 可基于 `job.selected_execution_profile_id` 自动解析同组合下首个健康可调度凭据
  - 自动写回 `ai_execution.provider / model / credential_ref / retry_attempt / switched_credential`
  - 第一版 `credential_ref` 固定为 `server-credential:{id}`

### 2.3 已完成的文档回写

- [x] `docs/02-现状/当前前后端接口文档.md`
- [x] `docs/03-方案/后端专题/未开始-2026-04-14-服务器模式CLI模型选择与凭据池实施计划.md`

## 3. 本阶段形成的实际能力

截至当前，仓库已经不再只是“讨论服务器模式以后怎么做”，而是已经具备以下可运行基础：

1. 平台可以向普通用户返回服务器模式可选 `CLI + 模型` 组合列表。
2. 平台可以读取和更新当前用户的默认服务器配置。
3. 平台任务已经可以记录“用户选了哪一个服务器配置组合”。
4. 单次执行已经可以记录“实际命中了哪一类凭据事实字段”。
5. 管理员查询链路已经具备最小权限边界，不再是完全裸露的只读入口。
6. 管理员已经可以查看服务器执行配置列表和服务器凭据列表。
7. 管理员已经可以新增、编辑、启用、禁用服务器凭据。
8. 管理员已经可以手动检测单个凭据，并把结果落回数据库状态字段与检测历史表。
9. 平台执行编排已经具备“从 `execution_profile` 自动解析真实命中凭据并写回执行快照”的后端真源。

## 4. 当前仍未完成的关键项

本专题还没有进入“可上线完成态”，主要缺口包括：

### 4.1 凭据安全与检测能力仍有边界

- 已补第一版密文写入 / 读取链路，并已打通平台执行编排内的调度读取真源
- 当前真实联网检测只覆盖 `openai + api_key`、`anthropic + api_key`
- `ak_sk` 当前只会返回 `degraded / unsupported_auth_type`，还不是完整的可用性检测

### 4.2 真实运行时 caller 尚未接到新调度真源

- `ExecutionOrchestratorService` 已能根据 `execution_profile + credentials pool` 自动解析并写回命中快照
- 但仓库中的真实运行时 caller 还没有正式接到这条新链路，目前该能力主要由服务层与定向测试覆盖

### 4.3 前端服务器模式体验尚未接入

- 运行时服务器配置选择弹窗尚未正式接后端新接口
- 设置页 `服务器模式` Tab 尚未实现
- 默认值失效后的前端兜底体验尚未形成

### 4.4 抗压与调度保护尚未开始

- 入队
- 单用户并发上限
- 创建 / 开始任务限流
- 凭据健康熔断
- 同组合自动切换一次

以上都还停留在实施计划层，没有进入实现阶段。

## 5. 发现的问题

| 严重程度 | 问题描述 | 根本原因 | 改进建议 |
| --- | --- | --- | --- |
| 必须改正 | 真实运行时 caller 仍未接到新的调度真源，导致后端虽然已经能解析命中凭据，但主运行时还没正式复用这套逻辑 | 现阶段先把管理员维护链路和后端调度真源补齐，caller 集成被继续后置 | 下一阶段优先把 caller 接到已落地的 `execution_profile -> credential` 解析链 |
| 必须改正 | 凭据健康检测目前仍有能力边界，`ak_sk` 与更多 provider 还未形成真实检测 | 第一版先把检测记录、状态回写和主流 `api_key` provider 跑通 | 下一阶段补更完整的 provider / auth_type 检测支持 |
| 应当改正 | 当前接口文档虽然已回写，但前端尚未接新接口 | 前后端推进节奏不同步 | 等后端写接口稳定后，立刻启动前端服务器模式接入 |
| 应当改正 | 测试环境里的平台仓储 / 服务夹具最初没包含 auth 表，新增外键后暴露出问题 | 旧测试基建默认只建 platform 表，未考虑跨模块外键 | 后续新增跨域表时，优先检查测试夹具是否仍只建局部 schema |

## 6. 做得好的地方

- 先冻结接口、表结构和权限边界，再动代码，避免了服务器模式专题一开始就陷入返工。
- 管理员权限前置能力补得及时，使后续 `/api/admin/platform/*` 扩展有了可持续的安全边界。
- 文档回写基本同步，没有让“草案、计划、现状、代码”四套口径严重漂移。
- 任务被拆成了多个小提交，后续定位和回滚成本都更低。

## 7. 下一阶段建议

下一阶段建议严格按以下顺序推进：

1. 先打通执行器与凭据池
   - 让真实运行时 caller 复用已落地的 `execution_profile -> server_credentials` 解析真源
   - 不再由外部手工传 `provider / model / credential_ref`

2. 再补凭据健康检测边界
   - `ak_sk` 检测支持
   - 更多 provider 的真实检测

3. 最后再接前端体验
   - 服务器模式选择弹窗
   - 设置页 `服务器模式` Tab

## 8. 当前阶段结论

当前可以明确认为：

- 本专题已经**进入代码实现阶段**
- 但仍处于**后端第一批基础能力落地阶段**
- 还**不具备直接进入前端全面接入或上线准备**的条件

一句话总结：

**服务器模式专题已经从“管理员凭据管理链路基本成形”继续推进到“后端调度真源已打通”，下一阶段的主攻方向应转向“真实运行时 caller 接入 + 检测能力补全”。**
