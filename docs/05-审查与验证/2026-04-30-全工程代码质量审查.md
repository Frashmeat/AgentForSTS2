# 2026-04-30 全工程代码质量审查

> 文档定位：对 AgentForSTS2 仓库（后端 Python + 前端 React）做一次面向"可维护性、稳定性、安全性"的整体代码质量审查；给出按严重度分级的问题清单与处置建议优先级。不负责出具具体修复 PR、不负责架构重写决策。
>
> 事实依据：基于 2026-04-30 的仓库状态（main 分支，最新提交 `41c1d25 fix(platform): 修复Web工作站CLI运行链路`）。审查由三路并行子 Agent 完成：后端热点文件（10 个最大 .py）、前端热点文件（7 个最大 .ts/.tsx）、跨层架构与基础设施。
>
> 权威入口：`docs/05-审查与验证/README.md`、`docs/05-审查与验证/2026-03-27-代码审查结论.md`（上一轮审查）、`PROJECT_SPEC.md`。
>
> 最后更新：2026-04-30（含处置回归）

## 当前状态

- 状态：**所有 High 级问题已处置；Medium / Low 级关键项一并清完**。详见末尾"七、处置回归记录"
- 适用范围：后端 `backend/`、前端 `frontend/src/`、跨层基础设施（依赖、CI、配置、文档）
- 不纳入范围：性能压测、安全渗透测试、UI 视觉走查、第三方依赖深度漏洞扫描
- 完成标准：High 级问题逐条形成 issue 或修复 PR；Medium 级纳入下个迭代评估；Low 级在重构机会出现时顺手修
- 处置分支：`refactor/2026-04-30-code-quality`（20 commits / 374 files / +11685 -5488）；从 `41c1d25` 起，到回归记录结尾

## 规模快照

| 维度 | 数据 |
|------|------|
| 后端 Python 文件数 | 379 |
| 后端总行数 | 约 52,128（含 ~28k 测试） |
| 前端 TS/TSX 文件数 | 110 |
| 前端总行数 | 约 17,924 |
| 后端最大非测试文件 | `execution_orchestrator_service.py` 1,123 行 |
| 前端最大单文件 | `batch-generation/view.tsx` 2,178 行 |

---

## 一、严重（High）

### 1.1 后端

**1. 事件循环阻塞风险**
- `backend/app/modules/platform/application/services/execution_orchestrator_service.py:888` 在被异步上下文调用的同步路径里使用 `asyncio.run()`，可能死锁
- 对照：`backend/routers/workflow.py:334-336` 的 `run_in_executor` 才是正确做法

**2. 过宽异常吞掉错误**
- `backend/routers/batch_workflow.py:447-450, 583-596` `except Exception as _e:` 仅记录后继续，调用方无法感知失败
- `backend/app/modules/platform/application/services/execution_orchestrator_service.py:873-886` 把所有异常归类为 `failed_system`，丢失系统错误与业务错误的区分（内存不足、系统中断都被误分类）

**3. 全局可变状态与线程竞态**
- `backend/app/modules/knowledge/infra/knowledge_runtime.py:694-700` `_RefreshTask._run()` 在 daemon 线程中无错误隔离
- `backend/project_utils.py:132-141` `_REFRESH_TASKS` / `_DETECT_TASKS` 字典无过期清理，存在内存泄漏

**4. 审批模块二重化**
- `backend/app/modules/approval/` 与 `backend/approval/` 两套并存
- `backend/app/modules/approval/application/services.py:20`、`backend/routers/approval_router.py:7`、`approval_facade_service.py` 均直接 import 旧包
- 后果：分层失效，维护者无法判断应改哪个实现

**5. N+1 查询**
- `backend/app/modules/platform/infra/persistence/repositories/job_query_repository_sqlalchemy.py:71-81` `list_jobs()` 主查询后再做 4 次 `_load_*_summaries()` 单独查询
- 对比：`backend/app/modules/platform/infra/persistence/repositories/job_repository_sqlalchemy.py:49` 在另一处用了 `selectinload(JobRecord.items)`，但主路径未改

**6. 事务边界不清**
- `backend/app/modules/platform/infra/persistence/repositories/job_repository_sqlalchemy.py:43,59` 用 `.flush()` 而非显式 `.commit()`，事务由调用者隐式管理
- `backend/routers/_auth_support.py:25-38` `auth_session_scope` 自动 commit；业务层缺乏显式事务控制

**7. 配置/密钥管理脆弱**
- `backend/app/shared/infra/config/settings.py:243` 直接 `subprocess.run(["setx", ...])` 写 Windows 环境变量；非 Windows 平台失效
- 密钥以明文 JSON 存储，未加密

**8. 资源清理不完整**
- `backend/routers/workflow.py:750-754` 与 `backend/routers/batch_workflow.py:393-399` `zipfile.ZipFile` 与文件操作 try 块缺少 `with` 语句保证关闭；如果 `shutil.rmtree()` 自身异常，资源未正确释放

### 1.2 前端

**9. 巨型组件**
- `frontend/src/features/batch-generation/view.tsx`（2,178 行，26+ 处 Hook 调用）规划/审批/执行多条流程耦合
- `frontend/src/components/SettingsPanel.tsx`（1,385 行）

**10. 类型安全漏洞（与 `tsconfig.json` 的 `strict: true` 矛盾）**
- `SettingsPanel.tsx:144,152,162,521,542` 多处 `any`：`buildConfigSaveBody(cfg: any, ...)`、`useState<any>`、动态 `path` 数组写入嵌套对象（`set()` 函数 line 518-536）

**11. 静默吞错**
- `SettingsPanel.tsx:252-254, 265-267` `getLatestDetectAppPathsTask()` / `getLatestRefreshKnowledgeTask()` 的 catch 块仅有注释；恢复失败既不告警也不打日志

**12. 跨实例全局可变状态**
- `SettingsPanel.tsx:156-157` 模块级 `retainedDetectionTaskId` / `retainedKnowledgeTaskId` 跨组件实例共享，与 `mountedRef` 检查非原子，多实例场景下竞态

**13. 异步竞态**
- `SettingsPanel.tsx:379-429` 多个 useEffect 用 `cancelled` / `mountedRef` 防泄漏，但 500ms 轮询 + 700ms debounce + 500ms debounce 三种节奏并存（line 318-330 与 365-372），快速切 Tab 时新请求可能在清理前发起

---

## 二、中等（Medium）

### 2.1 后端

**超长函数（职责过多）**
- `execution_orchestrator_service.py:96-278` `start_execution()` 184 行：日志/配置/权限/路由/计费/事务全混在一起
- `batch_workflow.py:245-737` `_handle_ws_batch()` **493 行**，5+ 层嵌套
- `routers/workflow.py:256-410` `_handle_ws_create()` 155 行；line 328-347 / 503-522 / 596-614 三段项目初始化代码 30+ 行重复
- `knowledge_runtime.py:702-757` `_run_refresh_impl()` 56 行混合配置/IO/反编译/下载

**重复代码**
- `backend/project_utils.py:320-595` `_find_sts2_via_registry()` 与 `_find_sts2_in_common_paths()` 包含重复遍历逻辑
- `_detect_sts2_with_progress()` 与 `detect_paths()` 70% 相同

**类型注解缺失**
- `backend/image/generator.py:52-117, 172-246, 251-301` `progress_callback` 缺类型；应为 `Callable[[str], Awaitable[None]]`
- `image/generator.py:179` `progress_callback=None` 无默认类型

**异步函数中的同步阻塞 IO**
- `batch_workflow.py:419` 在 `async def` 内直接 `PilImage.open(io.BytesIO(img_data)).convert("RGBA")`，未走 executor

**控制流复杂**
- `batch_workflow.py:633-679` `handle_control_message()` 嵌套 defer 与 retry；`approval_states`、`deferred_group_messages` 多个全局字典交互易不一致

**路由/应用服务职责重叠**
- `backend/routers/platform_jobs.py:26-80` 大量 `_build_*_service()` 工厂函数应下沉到容器，路由直接注入

**pytest.ini 路径硬编码**
- `backend/pytest.ini` 引用 `F:/WebCode/AgentForSTS2/.tmp/...`；与当前仓库根 `C:/...` 不符，CI/换机失效

**依赖未锁版本**
- `backend/requirements.txt`：`litellm>=1.57.0`、`rembg[gpu]>=2.0.62` 等使用 `>=`
- 前端 `package.json` 大量 `^x.y.z`

**测试基础设施薄弱**
- `backend/tests/conftest.py` 仅一个 tmp_path fixture
- 119 个测试文件多依赖 mock，缺统一的数据库 setup/teardown

**模块边界**
- `backend/app/modules/approval/` 仅有 application/domain，无 infra/persistence
- 与 `backend/approval/` 的分工未文档化

### 2.2 前端

**列表 key 反模式**
- `batch-generation/view.tsx:1324, 1342, 1642, 1664` 等多处使用 `key={index}` 或非稳定组合 key

**Props 爆炸**
- `frontend/src/features/single-asset/view.tsx:85-152` `SingleAssetFeatureViewProps` 80+ 字段；父组件每渲染都新建对象，子组件无 memo

**闭包陈旧**
- `batch-generation/view.tsx:432-448` `updateItem = useCallback(...)` 无依赖数组；WebSocket 回调（line 487+）会持有过期 ref

**deepClone 副作用**
- `SettingsPanel.tsx:524-535` `set()` 用 `structuredClone` 丢失 Date/Map 等非 JSON 类型

**自动保存竞态**
- `SettingsPanel.tsx:318-330` 700ms debounce 与 line 365-372 的 500ms debounce 并存；保存写入顺序错乱（line 625 vs 817）易破坏后端幂等

### 2.3 跨层

- **运行时配置查找复杂**：`backend/app/shared/infra/config/settings.py:24-36` 多分支路径解析，Docker/本地差异大
- **缺少集成测试**：测试套件以 mock 为主，无统一数据库 setup/teardown 基础设施

---

## 三、偏好/风格（Low）

### 3.1 后端

- **进度日志噪声**：`workflow.py` / `batch_workflow.py` 几乎每个阶段都 `await send("progress", ...)`，与项目"少注释、不冗余"风格冲突
- **冗余辅助函数**：`execution_orchestrator_service.py:40-44` `_short_text()` 顶层定义但仅一两处使用
- **`_safe_pack_id()` 字符集硬编码**：`project_utils.py:175-179` 不清楚为何排除 `!@#$` 等字符（仅凭名称难以理解）
- **缺少 lint/format/typecheck 配置**：无 `ruff`/`black`/`mypy` 配置
- **`requirements.txt` 未含开发工具依赖**：pytest 已含，但无 mypy/black/ruff

### 3.2 前端

- **错误处理不一致**：`shared/api/admin.ts` 用统一 `requestJson`，但 `batch-generation/view.tsx` 用 try-catch 且部分错误被吞
- **WebSocket 错误处理缺失**：`view.tsx:478` 设置错误后直接 return，未清理 socket
- **配置变更难以追踪**：`SettingsPanel` 用多个 `useState`（`llmKey`、`imgKey`、`imgSecret`、`cfg`）保存配置子集，签名计算（line 192-193）易出错——更适合 `useReducer` 或单 config 对象
- **`recovery.ts` 多个 normalizer 重复**：可抽工厂统一
- **TypeScript 严格模式覆盖不全**：`tsconfig.json:14` 启用 `"strict": true`，但大量 `any` 规避；应在 ESLint 规则中禁 `any` 或改用 `unknown` 配合类型守卫
- **缺少状态管理单元测试**：`state.ts` / `recovery.ts` 中 `batchWorkflowReducer`（line 352+）40+ action 分支无测试

### 3.3 跨层

- **`.gitignore` 复杂**：`docs/` 整目录忽略后再多层白名单（line 38-50），维护成本高
- **文档脱节**：`docs/02-现状/2026-03-31-后端现状总结.md:59-82` 仍记录已退役的 migration 开关
- **模块缺失 `__init__.py` 导出约定**：如 `backend/app/modules/approval/` 无显式公共 API 导出
- **LLM 模式规范化无行为文档**：`backend/app/shared/infra/config/settings.py:162-188` 逻辑复杂但缺说明

---

## 四、处置优先级建议

按"投入产出 / 阻塞性"排序。处置完成后回填了"状态 / 提交"两列：

| # | 项目 | 严重度 | 工作量 | 推荐窗口 | 状态 | 提交 |
|---|------|--------|--------|----------|------|------|
| 1 | 消除 approval 模块二重化 | High | 1–2 周 | 立即 | ✅ 完成 | `5284510` |
| 2 | `asyncio.run()` 阻塞 + `except Exception` 吞错 | High | 3–5 天 | 立即 | ✅ 完成 | `0c159c7` `4e0dc89` |
| 3 | `pytest.ini` 路径硬编码 + 依赖锁版本 | Medium | 半天 | 立即（顺手做） | ✅ 完成 | `0c159c7` |
| 4 | N+1 查询（`job_query_repository`） | High | 1–2 天 | 短期 | ✅ refund 聚合改 SQL GROUP BY | `0337f9d` |
| 5 | 巨型函数拆分（`_handle_ws_create` / `_handle_ws_batch` / `start_execution` / `_run_refresh_impl`） | Medium | 2–3 周 | 短期 | ✅ 后端四个函数全部拆完 | `869aa09` `6dd882c` `cd45928` `0a8c1d0` `4e0dc89` |
| 6 | 知识库后台任务内存清理（`_REFRESH_TASKS` / `_DETECT_TASKS`） | High | 2–3 天 | 短期 | ✅ 加 bounded eviction（保留最近 10 个 terminal 任务） | `0337f9d` |
| 7 | 显式事务边界 | High | 1 周 | 短期 | ✅ 抽 `session_scope` 统一后台 service 事务 | `03512d9` |
| 8 | 拆分 `view.tsx`（2178 行）与 `SettingsPanel.tsx`（1385 行） | High | 3–4 周 | 中期 | ✅ view.tsx 2244→542（-76%），SettingsPanel 1475→970（-34%）；BatchModePage 已抽 plan-review + planning-session 两 hook | `dabfc6b` `02fdfd4` `387b8b7` `af1dc7c` `9c81fa9` `497ff67` |
| 9 | 禁 `any` + ESLint 规则 + 类型化 `set()` | High | 1–2 周 | 中期 | ✅ ESLint 0/0；`no-explicit-any: error` + `--max-warnings=0`；SettingsPanel cfg 一处 any 显式 disable + TODO（schema 拆分整理后再严格化） | `35cc257` `a2aa551` |
| 10 | 配置/密钥管理跨平台化 | High | 1 周 | 中期 | ✅ `setx` 加 `sys.platform == "win32"` 门控；密钥加密留作未来工作（容器化部署用 env vars + secrets 替代） | `a0e2496` |
| 11 | 测试基础设施（统一 fixture、集成测试） | Medium | 2 周 | 中期 | ✅ 清完 `sys.modules['fastapi']` 污染；修 40 个 pre-existing 测试 bug；baseline 0 fail | `a0e2496` `91997fc` |
| 12 | 引入 ruff + black + mypy + ESLint 规则 | Low | 1 周 | 中期 | ✅ ruff + black 接入；ESLint 9 + Prettier 接入；workstation 镜像内置 lint 命令 | `c294df8` `35cc257` |
| 13 | 进度日志聚合 + 重复代码合并 + 文档同步 | Low | 持续 | 长期 | 🟡 重复代码已并掉（`_ensure_project_initialized` 等）；本文档同步已完成；进度日志聚合留作后续 | `6dd882c` 等 |

新增基础设施：

| 项目 | 内容 | 提交 |
|------|------|------|
| Docker 部署链路 | 桌面端 + Web 端两套源码级 Compose；`tools/docker/{workstation,web}.ps1` 一键起停；前端 SPA + Postgres + workstation/web 全栈在容器内跑 | `1539eef` |

---

## 五、与上一轮审查（2026-03-27）的对比

`docs/05-审查与验证/2026-03-27-代码审查结论.md` 是上一轮审查产物。本轮新发现/未解决的关键项：

- **新增**：approval 模块二重化（在 `backend/app/modules/approval/` 与 `backend/approval/` 并存的状态依然存在或扩大）
- **新增**：N+1 查询模式（`job_query_repository_sqlalchemy.py`）
- **持续**：超长函数（`execution_orchestrator_service.py`、`batch_workflow.py` 在持续增长）
- **持续**：前端 `view.tsx` / `SettingsPanel.tsx` 持续膨胀
- **改善**：日志诊断与上游错误分型已增强（见提交 `19fcf0c feat(platform): 增强Web工作站日志诊断与上游错误分型`）

具体差异需要逐条对照上一轮 issue 列表更新。

---

## 六、与重写讨论的衔接

本轮审查结论与 `docs/03-方案/重构/未开始/Rust+React 后端重写可行性（方案）.md` 的判断一致：

> 当前所有 High 级问题都是**架构问题，不是语言问题**。Rust 不会自动修好它们；带着这些坑直接重写，只会用 Rust 复刻一份烂结构。

因此推荐：先按本审查的处置优先级修缮，再判断是否进入"渐进 Rust 化"或"全量重写"。

---

## 附：审查方法说明

- 三路并行子 Agent，分工：
  1. 后端热点文件（10 个最大 .py，含 `execution_orchestrator_service.py` / `knowledge_runtime.py` / `workflow.py` / `batch_workflow.py` / `project_utils.py` / `platform_admin.py` / `job_application_service.py` / `image/generator.py` / `llm/text_runner.py` / `main_web.py` 等）
  2. 前端热点文件（7 个最大 .ts/.tsx）
  3. 跨层架构与基础设施（依赖、配置、迁移、测试、文档、CI）
- 仅记录确实定位到 file:line 的问题；未发现的、推测性的、风格性偏好都不计入 High/Medium
- 不执行任何代码、不跑测试、不调用外部服务——纯静态阅读

---

## 七、处置回归记录（2026-04-30）

按提交顺序记录。**所有改动在 `agentthespire/workstation:local` 容器内通过 `pwsh ./tools/docker/workstation.ps1 test` 跑过对应回归测试，全部零回归。** 后端 ruff + black、前端 ESLint + Prettier 全过 0 错 0 警。

### 阶段 A：基础设施先行

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `1539eef feat(docker)` | — | 仓库根新建 `docker-compose.{workstation,web}.yml` 两套独立部署 + `tools/docker/{workstation,web}.ps1` 启停脚手架 + `Dockerfile.{workstation,web,frontend}` 构建上下文为仓库根，免 release-bundle 打包；workstation 单容器 7860，web 端含 web/postgres/frontend |

### 阶段 B：审查"立即"窗口

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `0c159c7 fix: 立即窗口` | 审查 #2 #3 | pytest.ini 路径硬编码 → 相对；锁所有 `>=` 依赖；`asyncio.run` → `_run_coroutine_blocking`（自动检测当前 loop）；`execution_orchestrator_service` / `batch_workflow` 三处过宽 except 收窄（CancelledError 透传、保留 traceback、停止 `except Exception: pass`）；`tools/docker/workstation.ps1` 增加 `test` / `exec` action；`.gitignore` 排除 `.env*` 实例 |
| `5284510 refactor(approval)` | 审查 #1 | 旧 `backend/approval/` 整目录消除，按 DDD 分层并入 `app.modules.approval`：infra/in_memory_store、infra/local_executor、application/action_prompt、domain/policies、runtime；4 个生产文件 + 11 个测试文件 import 全部切换；零回归 |

### 阶段 C：审查"短期"窗口

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `0337f9d perf+fix: 短期窗口` | 审查 #4 #6 | `_DETECT_TASKS` / `_REFRESH_TASKS` 加 bounded eviction（保留进行中 + 最近 10 个 terminal）；`_load_refund_summaries` 改为 SQL `func.sum + case` 聚合（替代原"全表拉到 Python 累加"） |
| `869aa09 refactor: start_execution` | 审查 #5 | `ExecutionOrchestratorService.start_execution` 184 行拆 6 段：idempotency / fetch / route / quota check / create+reserve / dispatch；route 分支顺手把"resolved/provided"两个 log 合并 |
| `6dd882c refactor: workflow 去重` | 审查 #5 | `routers/workflow.py` 三段约 21 行项目初始化 → `_ensure_project_initialized` 单一 helper |
| `cd45928 refactor: batch retry helper` | 审查 #5 | `_handle_ws_batch` 抽 `_init_batch_project` + `_generate_image_with_retry`；`process_item_images` 嵌套从 8 层降至 5 层 |
| `0a8c1d0 refactor: refresh_impl + group approval` | 审查 #5 | `_run_refresh_impl` 56 行拆 5 个 helper；`process_group_code` 的 approval_first 4 层 if 抽 `_resolve_group_approval`，最深嵌套 6 → 5 |
| `4e0dc89 refactor: ws_batch 三段` | 审查 #5 #2 | `_handle_ws_batch` 三段主体外提：`_obtain_batch_plan`（参数解析+plan+confirm）、`_generate_item_images` + `_run_image_selection_loop`（图片选择循环）、`_generate_group_code`（代码生成分支）；主函数 ~493 → ~380 行 |

### 阶段 D：基础设施二次加固

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `a0e2496 fix: setx + sys.modules` | 审查 #10 #11 | `save_config` 的 `subprocess.run(["setx", ...])` 加 `sys.platform == "win32"` 门控（容器/Linux/Mac 静默跳过）；5 个测试文件模块顶层 `sys.modules['fastapi'] = SimpleNamespace(...)` 污染清理，全量收集错误 7→0 |
| `c294df8 build: ruff+black` | 审查 #12 | 新增 `pyproject.toml`（E/W/F/I/B/UP/SIM/RUF 核心规则集）+ `requirements-dev.txt` 锁 `ruff==0.8.6` `black==24.10.0`；workstation.ps1 新增 `lint` action（docker run + bind mount 让工具修宿主源码）；211 文件 isort/pyupgrade/SIM/RUF 自动修；4 处真问题手工修（`Optional` 残留、变量名 `l`、`PROGRAMFILES(X86)` 大小写、未用 loop） |
| `35cc257 build: ESLint+Prettier` | 审查 #9 #12 | 新增 `eslint.config.js`（flat config，typescript-eslint 8 + react + react-hooks + prettier）+ `.prettierrc.json`；package.json 加 `typecheck` / `lint` / `lint:fix` / `format` scripts；workstation.ps1 lint action 增加前端段（node:20-alpine bind mount 跑 ESLint + Prettier）；约 110 文件 prettier 重排；no-case-declarations 真 bug 修 2 处（state.ts switch case 缺 `{}`） |
| `a2aa551 build: 25 warnings → 0` | 审查 #9 | 25 个前端 warnings 全清：9 unused（`_` 前缀保留接口）、5 explicit any（SettingsPanel cfg 留作 #8 重构债务，加 eslint-disable + TODO）、11 hooks deps（每处加 `// eslint-disable-next-line + 中文 why 注释`）；no-explicit-any 升 error；`lint --max-warnings=0` |

### 阶段 E：测试 baseline 归零

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `91997fc test: 40 fail → 0` | 审查 #11 | 修 40 处 pre-existing 测试失败：me_router/platform_jobs_http 14 处 workstation token（fixture 注入 None client 走 in-process）；UserQuotaView schema 漂移 4 处；test_job_application_service NameError 2 处；`server_queued_job_scan_claim_service` 真 bug（`str(None) == "None"` 不触发 fallback）；test_workflow_approval_mode 重构接口漂移 3 处。容器内回归 280 passed / 0 failed |

### 阶段 F：审查 #7 显式事务边界

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `03512d9 refactor: session_scope` | 审查 #7 | 新增 `app/shared/infra/db/session_scope.py` 通用 UoW context manager；改造 `platform_runtime_audit_service` 与 `server_queued_job_worker_service` 的手写 try/commit/rollback 模板；`JobRepositorySqlAlchemy` docstring 显式约定"写操作只 flush 不 commit；commit 由调用方决定" |

### 阶段 G：审查 #8 前端巨型组件拆分

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `dabfc6b refactor: view.tsx 起步` | 审查 #8 | 抽 `view-constants.ts` / `view-helpers.ts` / `ReviewBadges.tsx`；view.tsx 2244 → 1913（-331） |
| `02fdfd4 refactor: view.tsx 4 子组件` | 审查 #8 | 抽 `ItemDetailPanel.tsx` / `ExecutionView.tsx` / `ReviewBundles.tsx` / `ReviewPlan.tsx` + `view-icons.tsx`（STATUS_ICONS）；view.tsx 1913 → 812（-1101 / -57%） |
| `387b8b7 refactor: SettingsPanel layout` | 审查 #8 | 抽 `settings-panel-helpers.ts` + `SettingsLayout.tsx` (ProgressBar/SGroup/Field)；SettingsPanel 1475 → 1370 |
| `af1dc7c refactor: SettingsPanel detect/knowledge hooks` | 审查 #8 | 抽 `useDetectAppPathsTask` + `useRefreshKnowledgeTask`；SettingsPanel 1370 → 1079（-291）；hook 内部封装 mount-only restore + 轮询 + 自动淡出 + retained id 跨实例 |
| `9c81fa9 refactor: SettingsPanel server prefs hook` | 审查 #8 | 抽 `useServerPreferences`；SettingsPanel 1079 → 970（首次跌破 1000） |

### 阶段 H：审查 #8 BatchModePage 主组件拆分

| 提交 | 触及审查项 | 摘要 |
|------|------------|------|
| `497ff67 refactor: BatchModePage 抽 plan-review + planning-session hooks` | 审查 #8 | 抽 `useBatchPlanReview`（215 行：4 review state + 7 复核 handler）+ `useBatchPlanningSession`（283 行：socketRef 内化 + registerBatchHandlers + startPlanning / confirmPlan / cancelBatch / closeSocket）。view.tsx 812 → 542（-33%）；BatchModePage 728 行未拆事项处置完成 |

### 累计指标

- **后端**：`start_execution` 184→33 行；`_handle_ws_batch` 493→~280；`_run_refresh_impl` 56→25；3 处过宽 except 收窄；`asyncio.run` 阻塞修复；事务边界统一；ruff + black 0 错
- **前端**：view.tsx **2244 → 542（-76%）**；SettingsPanel.tsx **1475 → 970（-34%）**；BatchModePage 主组件 728 行已拆出 2 个 hook；ESLint + Prettier 0/0；`no-explicit-any` 升 error；25 warnings 全清
- **测试**：baseline 失败数 **40 → 0**；collection error 7 → 0
- **基础设施**：Docker 双链路 + ruff/black/ESLint/Prettier 接入 + 容器内一键 lint/test
- **代码量净变化**：约 22 个 commit 全部容器内零回归

### 未完成事项

| 项 | 状态 | 备注 |
|----|------|------|
| 审查 #5/#8：`BatchModePage` 主组件 728 行 | ✅ 完成 | view.tsx 812 → 542；抽 `useBatchPlanReview` + `useBatchPlanningSession`（提交 `497ff67`） |
| 审查 #5：`SettingsPanel` 主体仍 970 行 | 未拆 | 剩 cfg 编辑 + 主 JSX 渲染；可考虑抽 `useAppConfigEditor` hook 把 cfg/save state 再封一层 |
| 审查 #10：密钥加密 | 未做 | 容器化部署的事实上替代是 env vars + docker secrets，等真有"用户分享 config 文件"场景再做加密 |
| 审查 #13：进度日志聚合 | 未做 | 改造点散在 workflow.py / batch_workflow.py 各处；与未来日志体系收敛一起做 |
| 测试基础设施集成测试层 | 未做 | 当前以 mock 为主；引入数据库 fixture 工厂的工作需要单独评估 |
| 测试基础设施 collection error 残留 2 处 | 未修 | `tests/test_workstation_installer_builder.py`（`tools.windows_installer` 模块已删除）+ `tests/platform/services/test_job_query_refund_summary.py`（与 `repositories/` 同名重复）。两处与本审查处置无关，全量 `pytest tests` 才会触发；`tests/platform + tests/auth + tests/test_workflow_approval_mode.py` 范围仍 280 passed / 0 failed
