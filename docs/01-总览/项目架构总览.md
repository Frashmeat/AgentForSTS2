# AgentTheSpire 项目架构分析

## 项目定位

**AgentTheSpire** 是一个 AI 驱动的《杀戮尖塔 2》Mod 生成器，核心链路：**自然语言描述 → AI 生图 → AI 写代码 → 编译部署**，全程自动化。

---

## 整体架构

```mermaid
graph TB
    User["👤 用户"]

    subgraph Frontend["前端 React + TypeScript"]
        UI_Single["单资产模式\nApp.tsx"]
        UI_Batch["批量模式\nBatchMode.tsx"]
        UI_Edit["Mod 编辑\nModEditor.tsx"]
        UI_Log["日志分析\nLogAnalysis.tsx"]
        WS["WebSocket Client\nws.ts / batch_ws.ts"]
    end

    subgraph Backend["后端 FastAPI + Python"]
        subgraph Routers["路由层"]
            R_WF["workflow.py\n单资产工作流"]
            R_BT["batch_workflow.py\n批量工作流"]
            R_AP["approval_router.py\n审批接口"]
            R_CFG["config_router.py\n配置接口"]
        end

        subgraph Agents["代理层"]
            CA["code_agent.py\nC# 代码生成"]
            PL["planner.py\n需求规划拆解"]
            DOCS["sts2_docs.py\nSTS2 API 知识库"]
        end

        subgraph LLM["LLM 调度层"]
            AR["agent_runner.py\n统一入口"]
            RN["_runner.py\n子进程运行器"]
            CC["claude_cli.py"]
            CX["codex_cli.py"]
            TR["text_runner.py\n文本补全"]
        end

        subgraph Image["图像层"]
            GEN["generator.py\n生图调度"]
            POST["postprocess.py\n图像后处理"]
            PA["prompt_adapter.py\nPrompt 优化"]
        end

        subgraph Approval["审批层"]
            SVC["service.py"]
            EXE["executor.py"]
            STR["store.py"]
        end

        CFG["config.py\n配置管理"]
    end

    subgraph External["外部服务"]
        BFL["BFL FLUX.2"]
        JM["火山引擎 即梦"]
        WX["通义万相"]
        CUI["ComfyUI 本地"]
        Claude["Claude CLI"]
        Codex["Codex CLI"]
        LiteLLM["LiteLLM / OpenAI"]
    end

    subgraph Output["本地输出"]
        MOD["Mod 项目\n.cs / .json / 图片"]
        BUILD["编译产物\n.dll / .pck"]
    end

    User --> Frontend
    Frontend -- WebSocket --> Routers
    Frontend -- REST --> R_CFG
    Frontend -- REST --> R_AP

    R_WF --> Image
    R_WF --> Agents
    R_BT --> PL
    R_BT --> Image
    R_BT --> Agents

    CA --> AR
    PL --> TR
    PA --> TR

    AR --> RN
    RN --> CC --> Claude
    RN --> CX --> Codex
    TR --> LiteLLM

    GEN --> BFL
    GEN --> JM
    GEN --> WX
    GEN --> CUI

    R_AP --> Approval
    Agents --> DOCS

    Claude --> MOD
    Codex --> MOD
    MOD --> BUILD
```

---

## 单资产工作流

```mermaid
sequenceDiagram
    actor User as 用户
    participant FE as 前端
    participant WF as workflow.py
    participant PA as prompt_adapter
    participant GEN as generator
    participant POST as postprocess
    participant CA as code_agent
    participant CLI as Claude/Codex CLI

    User->>FE: 填写描述 + 点击生成
    FE->>WF: WebSocket start
    WF->>PA: 优化图像提示词
    PA-->>FE: prompt_preview 事件
    User->>FE: 确认/修改 Prompt
    FE->>WF: confirm

    loop 生图循环
        WF->>GEN: 生成 1 张图
        GEN-->>FE: image_ready 事件
        User->>FE: 选图 or 再来一张
    end

    WF->>POST: 图像后处理（缩放/抠图/轮廓）
    WF->>CA: 生成 C# 代码
    CA->>CLI: 执行 Agent 任务
    CLI-->>FE: agent_stream 流式输出
    CLI->>WF: 完成
    WF-->>FE: done 事件
```

---

## 批量工作流

```mermaid
flowchart TD
    A["用户提交批量需求"] --> B["planner.py\n拆解为 PlanItem 列表"]
    B --> C["依赖拓扑排序"]
    C --> D{"有无依赖关系"}

    D -- 独立资产 --> E["并发生图\n最多 2 路并行"]
    D -- 有依赖 --> F["串行处理"]

    E --> G["图像后处理"]
    F --> G

    G --> H["按组串行调用\ncode_agent"]
    H --> I["create_asset_group\n一次 Prompt 生成多个相关资产"]
    I --> J["统一 dotnet publish\n编译一次"]
    J --> K["完成"]
```

---

## LLM 调度层

```mermaid
graph LR
    subgraph 入口
        AR["agent_runner.py"]
        TR["text_runner.py"]
    end

    subgraph 子进程运行器
        RN["_runner.py\n共享基础设施\n• subprocess pump\n• stdout/stderr 双线程\n• asyncio queue\n• 实时流式输出"]
    end

    subgraph Backend选择
        CC["claude_cli.py\nJSON stream 解析\nANTHROPIC_API_KEY"]
        CX["codex_cli.py\nWindows 禁沙箱\nOPENAI_API_KEY\n输出文件读取"]
    end

    AR -->|agent_backend=claude| CC
    AR -->|agent_backend=codex| CX
    CC --> RN
    CX --> RN
    TR -->|litellm| LiteLLM["LiteLLM\n15+ providers"]
```

---

## 审批模块状态机

```mermaid
stateDiagram-v2
    [*] --> pending : AI 规划动作
    pending --> approved : 用户审批通过
    pending --> rejected : 用户拒绝
    approved --> executed : 执行命令/操作
    executed --> [*]
    rejected --> [*]

    note right of pending
        风险分级
        low / medium / high
    end note

    note right of executed
        executor.py 执行
        命令白名单校验
    end note
```

---

## 模块全景

```mermaid
mindmap
  root((AgentTheSpire))
    前端
      单资产 App.tsx
        Prompt 确认
        图片选择
        Agent 日志
      批量 BatchMode.tsx
        规划展示
        进度追踪
      ModEditor.tsx
        Mod 文件编辑
      LogAnalysis.tsx
        崩溃日志分析
    后端
      路由层
        workflow 单资产流
        batch_workflow 批量流
        approval_router 审批
        config_router 配置
      代理层
        code_agent C#代码生成
        planner 需求拆解
        sts2_docs 知识库注入
      LLM调度
        agent_runner 统一入口
        _runner 子进程基础设施
        claude_cli JSON流解析
        codex_cli 沙箱管理
        text_runner 文本补全
      图像层
        generator 多云厂商调度
        postprocess 抠图/缩放/轮廓
        prompt_adapter Prompt优化
      审批层
        service 请求管理
        executor 命令执行
        store 状态存储
    外部依赖
      生图 BFL/即梦/万相/ComfyUI
      代码 Claude CLI/Codex CLI
      LLM LiteLLM 15+providers
```

---

## 模块职责一览

| 模块 | 文件 | 职责 |
|------|------|------|
| **工作流路由** | `routers/workflow.py` | WebSocket 驱动单资产完整链路，管理 13 个流式事件阶段 |
| **批量工作流** | `routers/batch_workflow.py` | 依赖拓扑排序 + 2路并发生图 + 串行代码生成 |
| **代码代理** | `agents/code_agent.py` | 将 STS2 游戏知识注入 Prompt，驱动 CLI 写 C#、本地化文件并编译 |
| **规划器** | `agents/planner.py` | 将自然语言需求拆解为 PlanItem，处理资产间依赖关系 |
| **图像生成** | `image/generator.py` | 统一调度 BFL/即梦/万相/ComfyUI，处理轮询、超时、下载 |
| **图像后处理** | `image/postprocess.py` | 自动抠图、生成轮廓、按资产类型输出多尺寸版本 |
| **LLM 运行器** | `llm/_runner.py` | 共享子进程基础设施，双线程 pump stdout/stderr，实时推流 |
| **审批模块** | `approval/` | 风险分级（low/medium/high）+ 人工审批 + 安全执行隔离 |
| **配置管理** | `config.py` | 单例配置，支持环境变量覆盖，Windows 持久化写入 |
| **WebSocket 客户端** | `frontend/lib/ws.ts` | 持久断线检测，连接异常实时反馈到 UI |
