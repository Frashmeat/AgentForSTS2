# 项目架构总览

> 文档定位：本文描述 AgentTheSpire 当前 Rust/Tauri 实现的稳定结构、模块职责和关键数据流。
>
> 事实依据：`Cargo.toml`、`crates/`、`src-tauri/`、`src/` 和当前配置/工程实现。
>
> 权威入口：阶段方向见 `docs/03-当前方案/当前方案.md`。
>
> 最后更新：2026-08-02

## 项目定位

AgentTheSpire 是 AI 驱动的 Mod 生产工具。当前首要目标游戏是《杀戮尖塔 2》，核心链路为：

```text
自然语言需求
  → 知识检索与规划
  → LLM/图像生成
  → 结构与编译验证
  → 构建和打包
  → 真实游戏人工验收
```

## 总体结构

```text
                         React + TypeScript
                                  |
                         src/services/api.ts
                          /               \
                 Tauri IPC                 HTTP
                    |                       |
          +---------v---------+   +---------v---------+
          | src-tauri Desktop |   |     ats-web       |
          | ProjectSession    |   | lightweight shell |
          +---------+---------+   +---------+---------+
                    |                       |
                    +-----------+-----------+
                                |
                      +---------v---------+
                      |     ats-core      |
                      | domain/application|
                      +----+----+----+----+
                           |    |    |
             +-------------+    |    +----------------+
             |                  |                     |
      Project Folder     Game Pack + Truth      External adapters
      Run / Artifact      Snapshot context       LLM / Image API
                                                ilspycmd / dotnet
                                                Godot / filesystem

ats-cli ------------------------------------------------> ats-core
```

## Stage 2 迁移状态

Work Order 1 已建立目标模块化单体骨架，Work Order 2-3 已在该边界内建立 Run/Artifact、Game Context 和 Resource Workspace 合同：

```text
ats-features ------> ats-runtime ------> ats-kernel
       |                   ^
       +--> ats-game-context ----------+
       +--> ats-workspace -------------+

ats-adapters ------> runtime / game-context / workspace / kernel
```

`scripts/check-stage2-dependency-dag.mjs` 通过 `cargo metadata` 校验上述精确依赖集合，并禁止 Runtime、Game Context、Workspace 或 Adapters 反向依赖 Features，也禁止六个目标 crate 依赖旧 `ats-core`。

`ats-runtime` 当前拥有无产品枚举的 `RunRecord` v3、typed payload envelope、基础 `ArtifactManifest` v3 和 publisher port；`ats-features` 用 typed registry 校验请求/结果；`ats-adapters` 负责同目录 staging、hash、Windows 瞬态 rename 重试、失败清理和 immutable final。它们不依赖 Feature 实现、Game Pack、codegen、image processing 或旧 Core。

`ats-game-context` 当前拥有 pinned Game Pack v2、Contribution Resolver、Truth Snapshot v2、bounded Evidence Query 和 verified context；`ats-workspace` 拥有三类 Resource origin、immutable content version、selection 和 provenance；对应文件验证与持久化位于 `ats-adapters`。STS2 与 synthetic Pack 已使用同一新合同，但现有 Shell 尚未读取这些 v2 文件。

这仍不是生产链切换标志。当前 Tauri/Web 和所有生产 handler 继续调用 `ats-core` 的 RunRecord/ArtifactManifest v2；Game Pack、Truth、Workspace、Prompt 和 Feature 将在后续 Work Orders 中逐步迁移，Work Order 7 才切换 Shell 并删除旧 Core。迁移完成前，下文继续描述当前实际生产链。

## Workspace

```text
.
├─ Cargo.toml
├─ crates/
│  ├─ ats-core/       迁移期当前领域逻辑、Prompt、Run/Artifact 与通用执行器
│  ├─ ats-kernel/     Stage 2 稳定 ID、hash 和 schema envelope
│  ├─ ats-runtime/    Stage 2 Run/Artifact v3 合同与通用 ports
│  ├─ ats-game-context/ Stage 2 Pack v2、Contribution、Truth 与 Evidence
│  ├─ ats-workspace/  Stage 2 versioned Resource Workspace 合同
│  ├─ ats-features/   Stage 2 typed Feature registry 与后续纵切面
│  ├─ ats-adapters/   Stage 2 Artifact、Truth 与 Resource 文件适配器
│  ├─ ats-web/        axum HTTP 与静态资源壳
│  └─ ats-cli/        构建、部署、日志和停止命令
├─ src-tauri/         Tauri v2 桌面壳与 IPC commands
├─ src/               React 19 + TypeScript 共享前端
├─ game_packs/        游戏声明、guidance、模板、资源规格和交付布局
├─ runtime/           可发布的配置示例；可变 app-data 位于仓库外
├─ artifacts/         本机构建、候选、模型与 E2E 产物（Git 忽略）
└─ scripts/           CI、E2E 和辅助脚本
```

Cargo workspace 使用 Rust edition 2024，并禁止 `unsafe_code`。`ats-core` 不依赖具体 UI 或传输框架，桌面/Web/CLI 只负责装配和 IO 边界。

## 核心模块

`ats-core` 当前导出以下主要能力：

| 范围 | 主要模块 | 职责 |
| --- | --- | --- |
| 工程 | `project`、`project_utils`、`fs_atomic` | 工程目录、锁、路径和原子文件更新 |
| 执行与产物 | `platform`、`plan_artifact` | Run 生命周期、handler、history、ArtifactManifest 和计划产物 |
| AI | `llm`、`image_gen`、`prompting`、`codegen`、`planning` | 模型调用、Prompt 组装、需求规划和代码/图片生成 |
| 游戏上下文 | `game_pack`、`knowledge` | Pack 加载、Truth Snapshot 获取/校验、当前源码事实选择和 Evidence |
| 图片 | `image_proc` | 背景去除、资源派生、可选 ML 处理和真实 processor provenance |
| 工具链 | `toolchain`、`mod_analyzer`、`capabilities` | Godot/.NET/游戏环境、项目分析和能力探测 |
| 基础设施 | `build_info`、`cancellation`、`controlled_process`、`config`、`health`、`failure`、`errors` | 编译期构建身份、协作式取消、受控外部进程、配置、健康状态和共享结构化失败契约 |

Platform DDD 内部结构：

```text
crates/ats-core/src/platform/
  domain/
    RunRecord / RunStatus / RunResult / timeline invariants
          |
          v
  repository/
    transition CAS / FileRunRepository / schema-v2 history
          |
          v
  application/
    RunApplicationService / submit / handlers / cancellation
          |
          v
  artifact/
    immutable ArtifactManifest / file hashes / Evidence / provenance
```

壳层可以选择项目、提供适配器和提交请求，但不能重新实现 Run 状态机、Artifact schema、错误字段或候选构建身份。

## 桌面壳

`src-tauri` 以 Tauri IPC 暴露 core 能力，不在桌面进程内启动 HTTP 服务。主要托管：

- 当前配置快照及保存后的热替换。
- app data 路径和 recent projects。
- `ActiveProject -> Arc<ProjectSession>`，统一持有当前工程、操作系统文件锁、共享 Run repository 和活动 task scope。
- image processing prewarm 状态。
- project、run、knowledge、planning、codegen、LLM、settings 和 plan artifact commands。

启动期非致命预热通过后台任务执行，窗口不等待 ML 模型或缓存准备完成。

工程 Run 的生命周期由桌面 composition 层统一管理：

```text
ActiveProject
  lifecycle mutex
  current: Arc<ProjectSession>
             |
             +-- ProjectFolder / OS exclusive lock
             +-- Arc<FileRunRepository>
             +-- RunTaskScope
             |     run-id -> CancellationToken + JoinHandle
             +-- state: open | closing

submit
  -> 在同一 task-scope 临界区检查 accepting 并注册 SpawnedRun

close / switch / app ExitRequested
  -> enter closing
  -> reject new submissions
  -> cancel every project Run
  -> wait for stream / file rollback / image worker / process tree cleanup
  -> drained: release OS lock
  -> 30s timeout: keep closing + task handles + OS lock
```

Tauri 应用退出通过 `RunEvent::ExitRequested.prevent_exit()` 先排空活动工程；排空成功后内部放行一次 `app.exit(code)`，避免退出事件递归。异常进程终止留下的 Pending/Running Run 在下次 `ProjectSession::open` 返回前统一 reconciliation 为 `failed + run.interrupted`。全局 ML prewarm 不读写工程，因此不注册到 ProjectSession。

Core 根级 `failure` 模块定义唯一的 `ActionableFailure` schema 和 typed normalizer。`RunRecord.failure` 直接保存该类型；Tauri 只用透明 `CommandFailure` newtype 将相同 JSON object 作为 command reject value 返回，不复制领域字段，也不把未知错误的 `Display` 文本暴露给前端。

## Web 与 CLI 壳

`ats-web` 当前是轻量 Web 入口，装配 health、knowledge、planning、codegen、LLM 和静态前端。它不是完整 SaaS 平台，也没有开放端口所需的完整应用鉴权，因此默认只应绑定本机或放在外部鉴权代理之后。

`ats-cli` 提供自动化和运维入口。壳层不得复制 core 中的任务、构建或错误分类逻辑。

## 共享前端

前端使用 React 19、TypeScript 5.6、Vite 6、Zustand 5 和 HashRouter。主要页面包括 Dashboard、Mod Editor、Batch Generation、Log Analysis、System、Settings 和 DevTools。

`src/services/api.ts` 根据构建期 `__IS_TAURI__` 选择：

- `tauriApi.ts`：通过 Tauri `invoke` 调用桌面 commands，并作为 API 类型权威。
- `webApi.ts`：通过 HTTP 调用 `ats-web`，必须满足与 `tauriApi.ts` 相同的导出签名。

页面组件只消费统一 API，不自行维护桌面/Web 两套业务流程。

所有 Tauri `invoke` 经过 `invokeCommand` 和严格 runtime guard。React 错误 state 使用 `ActionableFailure | null`，统一由 `ActionableErrorNotice` 展示安全 message、恢复动作提示和可选 diagnostic ID；字符串或畸形 reject 固定降级为本地 `core.unclassified`，原值不进入 UI。

```text
typed Core error
  -> FailureNormalizer
  -> ActionableFailure
       +-> RunRecord.failure
       +-> CommandFailure (transparent Tauri reject)
              -> invokeCommand runtime guard
              -> ActionableErrorNotice
```

## 工程数据

桌面端不依赖数据库，每个 Mod 工程是可复制、可归档的自包含目录。核心内容包括：

```text
<project>/
├─ project.json
├─ plan.json
├─ items/
├─ artifacts/<artifact-id>/runs/<run-id>/
│  ├─ artifact-manifest.json
│  └─ files/
├─ history/                 # schema v2 RunRecord
├─ Generated/
└─ .ats/
   ├─ lock
   └─ diagnostics/<run-id>/
```

`RunRecord v2` 是执行状态、timeline、failure 和 result 的唯一事实；独立 Run lifecycle audit 已删除。成功产物先以不可变 `ArtifactManifest` 和文件哈希发布，再通过 repository CAS 完成 Run；失败诊断进入 `.ats/diagnostics/<run-id>/`，不能污染正式工程文件或成功 Artifact Store。图片 ArtifactManifest schema v2 还保存实际 processor、model SHA、ONNX Runtime、fallback 原因和编译期 BuildInfo；图片质量报告与 processor provenance 是两个独立事实。

候选构建身份由 `ats-core/build.rs` 从受控环境和实际 Cargo feature 嵌入，health、capabilities 与图片 provenance 读取同一个 `BuildInfo`。`build.ps1 -Variant Baseline|Ml` 使用 `artifacts/build/<variant>/<build-id>/target` 隔离 Cargo/Tauri 输出，再把本次 bundle 复制到 `artifacts/release/<build-id>/<variant>` 并生成 SHA-256 manifest；脏工作区、variant/feature 不一致和复用 build ID 都在构建前拒绝。

Work Order 5 在它们之上增加唯一候选编排，不复制身份规则：

```text
scripts/release-candidate.ps1 -Variant All
  |
  +-- frontend tests + production build
  +-- workspace check + test + clippy
  +-- build.ps1 -Variant Baseline -BuildId <same-id>
  |      +-- final EXE BuildInfo handshake
  |      +-- baseline/release-manifest.json
  +-- build.ps1 -Variant Ml -BuildId <same-id>
  |      +-- final EXE BuildInfo handshake
  |      +-- ml/release-manifest.json
  +-- recompute manifest identity / sizes / SHA-256
         |
         +-- release-verification.json
             running | failed | succeeded
```

普通 push/PR 的 Windows CI 只编译 desktop baseline 与 `ml-rembg` 两种 feature 组合，不构建 installer。

## Mod 生产边界

长期目标是“一条通用 Mod 生产流水线 + 多个 Game Pack”：

- 通用引擎实现获取、索引、生成、验证、构建、打包和追踪机制。
- Game Pack 声明游戏真相源、专属资源、工程骨架、资源规格、验证规则和交付布局。
- STS2 的 C#、Godot、BaseLib、DLL/PCK 与具体 hook 属于 STS2 Game Pack 内容，不进入通用领域接口。

Game Pack Stage 1 已完成：工程通过 `game_id` 绑定受控 Pack，生成任务固定经过验证的 Truth Snapshot；STS2 的 guidance、模板、manifest、资源规格、验证规则、build recipe 和 package layout 已迁移到 `game_packs/sts2/`，通用 Core 只保留可复用执行器。当前仍只有 STS2 一个完整样本，workstation 发现 UI 和有限 `Sts2CodeFactsProvider` 是否继续声明化，等待第二个真实游戏需求后再决定。完整边界和实施依据见[通用 Mod 流水线与 Game Pack 边界](../03-当前方案/通用Mod流水线与Game-Pack边界.md)与[Stage 1 职责清单](../03-当前方案/2026-07-30-Game-Pack-Stage-1职责清单与迁移设计.md)。

```text
Project.game_id
      |
      v
GamePackRegistry ---> validated game-pack.json
      |                    |
      |                    +-- guidance / project template
      |                    +-- resource + validation specs
      |                    +-- build recipe + package layout
      v
TruthSnapshotStore
  .staging -> hash/index/verify -> snapshots/<snapshot-id>
                                      |
                                      v
                            VerifiedGameContext
                         Pack SHA + Snapshot ID
                         sources/indexes/tools
                                      |
                   +------------------+------------------+
                   v                                     v
            Prompt + Evidence                    Run / ArtifactManifest
```
