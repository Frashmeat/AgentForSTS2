# 当前进度说明

> 文档定位：`rust` 分支当前代码能力、有效机器证据和剩余门禁的事实入口。
>
> 不包含：历史 candidate 的完整流水；任务证据保留在对应 Trellis 和 artifacts 目录。
>
> 当前任务：`.trellis/tasks/08-22-human-semantic-feedback-closure/`。
>
> 最后更新：2026-09-06

## 1. 当前结论

Typed Behavior IR、确定性 Game Adapter、人工单 Item 语义反馈闭环和 production
`--headless-jsonl` Shell 已在提交 `2d192b22fd3892dba2c2a71fdde0c56bbc4d1179` 完成并通过完整机器门禁。
replacement candidate `rc-20260824-headless-2d192b22` 已通过 release verification 10/10、
独立 hash/残留审计、ML 安装、installed BuildInfo 和 headless smoke。

安装态 fresh root `20260825-164001` 已创建全新 Truth 和 Project。最初五个 Plan Runs 因人工验收
环境错误地把图片专用 `OPENAI_API_KEY` 绑定为 LLM key 而失败；这不是 candidate 代码缺陷或已证实的
上游故障。2026-09-06 使用产品 `llm.api_key` 重新分层探测，`qwen3.7-max` 已列出，四种请求形状
全部 HTTP 200，项目形状 SSE 收到 `[DONE]`。当前可 Resume 同一 candidate-specific Graph。

```text
branch: rust
implementationCommit: 2d192b22fd3892dba2c2a71fdde0c56bbc4d1179
task: 08-22-human-semantic-feedback-closure
working state: installed fresh closure ready to resume with corrected LLM credential
current candidate: rc-20260824-headless-2d192b22 (installed ML)
fresh Truth: e45138d30a9bbb3f6b94bef5b8572327034e17e1f617d6b1ee0ac875c8b651eb
fresh Project: ATSHeadless2D19
paused Graph: graph-000000000000000018cf0003dcb047e0-00000000 revision 20
validated model: qwen3.7-max via product llm.api_key
```

## 2. 当前生产链路

```text
confirmed ItemDefinition
        |
        v
Plan -> Typed Behavior IR -> local IR validation
        |
        v
registered deterministic Game Adapter
        |
        v
RenderedItemBundle -> game-specific Pipeline Validate/Build/Package
        |
        v
one atomic project publication + one immutable Artifact
```

机器 `succeeded` 只证明技术合同通过。真实玩法由用户判断；单 Item 反馈从 succeeded source 派生
新 Graph，只替换目标 Behavior/Render 并复验完整 closure。反馈原文不持久化。

## 3. Shell 与后台验收能力

```text
Tauri UI IPC ---------+
                      +--> shared Shell service functions
JSONL stdio headless -+          |
                                 v
                     ProjectSession + Stage2Composition
```

当前 `--headless-jsonl` 已覆盖 health/settings、Project、Truth、generic Feature、Run/Graph、
Item/Draft/Resource 和 human feedback。它不包含 `e2e` WebDriver，不监听网络端口，每个响应绑定
当前 BuildInfo；EOF/Shutdown 通过 ProjectSession drain 后释放工程锁。未知进程参数直接退出 2，
不会再误启动桌面 UI。

详细设计见
[`安装态后台验收执行入口方案`](../03-当前方案/安装态后台验收执行入口方案.md)。

## 4. 当前有效验证

production implementation commit `2d192b22` 已完成完整机器门禁：

```text
cargo check --workspace --all-targets
passed

cargo test --workspace --all-targets --no-fail-fast
280 passed; 1 real STS2/BaseLib environment-bound test ignored

cargo clippy --workspace --all-targets -- -D warnings
passed

cargo test -p agentthespire-desktop --lib --no-fail-fast
25 passed

cargo test -p agentthespire-desktop --test headless_transport --no-fail-fast
3 passed

cargo test -p agentthespire-desktop --test composition_generation --test stage2_character_composition --no-fail-fast
11 passed

cargo check -p agentthespire-desktop --features ml-rembg
cargo check -p agentthespire-desktop --features e2e
passed

npm run test:frontend
41 passed

npx tsc -b --pretty false
npm run build
passed; production bundle built

node scripts/check-stage2-dependency-dag.mjs --self-test
node scripts/check-stage2-dependency-dag.mjs
passed

cargo run -q -p ats-cli -- features
12 exact, unique Features

scripts/test-release-candidate.ps1
scripts/test-build-plan.ps1
passed

legacy Core/Prompt/handler/feedback/old-Graph guards
passed

node --check scripts/e2e/headless-client.mjs
node --check scripts/e2e/run-headless-smoke.mjs
passed

debug binary JSONL health/settings/current-project/shutdown smoke
passed; 12 Features; no UI process retained

debug binary Project create/list Runs/close/shutdown smoke
passed; one succeeded project.create Run; project lock reacquired

cargo fmt --all --check
git diff --check
passed
```

candidate 额外完成 10/10 release verification、四安装器独立 hash、零 candidate/build 残留、
baseline/ML release-binary headless smoke、ML 安装和 installed BuildInfo。fresh closure 已完成 Truth/
Project 阶段；此前五个 Plan failure 保留为验收凭据绑定错误的诊断证据。

修正后的模型探测矩阵：

```text
credential: product config llm.api_key (not the image-only OPENAI_API_KEY)
GET /models: 200; qwen3.7-max listed
plain non-stream: 200
plain SSE + usage: 200 + DONE
json_object non-stream: 200
project-shape SSE + usage + json_object: 200; 88 events + DONE
```

## 5. 剩余顺序

1. 使用正确的产品 LLM key 和 `qwen3.7-max` Resume 当前 Graph revision 20。
2. 完成 Draft、Item、复用历史图片字节的新 Resource、Run、Graph 和 Artifact closure。
3. 审计 terminal Run、ArtifactManifest/hash、Package、零 staging/transaction 残留和工程锁。
4. 用户在真实 STS2 中完成 Character、战斗、奖励、Continue/save 和单 Item feedback 修正验收。

## 6. 证据与授权边界

- 不删除或改名任何旧 candidate、verification、Truth、Project、Run、Graph、Artifact、Package、
  `.tmp` 或失败证据。
- source/debug smoke 不能冒充 installed candidate closure。
- candidate 已构建并安装；后续 push、rebase 和发布仍需独立授权。
- Agent 不操作真实 STS2 UI；真实玩法结论只由用户确认。
- 只有新 candidate 的 installed fresh closure 和用户真实游戏确认都完成后，才能勾选最后 PRD 项、
  完成并归档任务。
