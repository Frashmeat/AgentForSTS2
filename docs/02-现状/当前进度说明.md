# 当前进度说明

> 文档定位：`rust` 分支当前能力、验证结论和剩余门禁的事实入口。
>
> 不包含：逐次 candidate 诊断、详细实现设计或可执行合同。
>
> 本地任务证据账本：`.trellis/tasks/08-05-sts2-character-cross-item-composition/acceptance.md`（由 Trellis 管理，不属于 `docs/`）。
>
> 最后更新：2026-08-12

## 1. 当前结论

Game Pack Stage 2 基础架构和 STS2 六种 Mod 类型已落地。当前活动任务是 STS2 Character 与跨 Item 组合：O1-O7 已完成，O8 仍为 `in_progress`。

当前已完成：

- Pack v4、ItemDefinition v2、ResourceAsset v2 和 CompositionDraft v2。
- Character Studio、typed reference graph 和 whole-closure atomic publication。
- Placeholder 与 Branded Placeholder Character，以及 Prototype/Standard/Custom profiles。
- 可恢复的 `composition.plan` 和 `composition.generate` durable execution graph。
- `51e0ae6b61dc` 的完整机器门禁、隔离 GUI E2E 8/8 和 release 10/10。
- 旧 candidate 的安装、全新 Truth/Draft/11 Item confirmation 和三次 Character Single 失败诊断。
- definition-bound Plan 权威上下文与 Single 脱敏 failure-details 的本地修复及针对性回归。
- 修复提交 `235696d6` 的完整机器门禁与隔离 GUI E2E 8/8。
- `bb96a80f9de6` candidate 的构建、release 10/10、安装，以及全新 Truth/Draft/Resource/11 Item confirmation。
- Character Single 两次 `merge_content` 失败的本地根因诊断：merge role 被错误声明为 JSON string，形成双重编码合同。
- merge role 直接 typed flat object、本地确定性规范化和 checkpoint 兼容修复的完整机器门禁与隔离 GUI E2E 8/8。
- `ExecutionGraphRecord v2`、registered validation typed diagnostics、受策略约束的完整文件 repair loop 和 `commit_prepared` 后移已提交为 `b68c33df`。
- Desktop 对新项目严格使用 Settings 初始化 `local.props`；已有项目优先采用有效 Settings 覆盖，否则结构化验证并沿用已有 project-local STS2/Godot 路径。
- project-local fallback 修复已提交为 `659a04c3`，并通过完整机器门禁和保留证据的隔离 GUI E2E 8/8。
- 绑定最终 HEAD `660d513a` 的 candidate `rc-20260812T104652Z-660d513ac582` 已通过 release 10/10、安装和运行时身份核对。
- 安装态已创建全新 Truth、Project、11-Item Draft、Resources、confirmed definitions、Plan Graph 和 Generate Graph；成功 Plan checkpoint 在多次 Resume 中始终复用，未重跑。
- Generate Graph 在 Character Single 的 Provider/typed-output failure 后安全 paused，未发布 Artifact、ZIP 或工程文件，且无 staging/transaction 残留。
- 统一 Generation Feedback Controller、ExecutionGraphRecord v3、Composition Generate request v4/Blueprint v2 和 Shell `feedbackPhase` 已在当前实现中落地；旧非持久化 Composition 执行入口已删除。
- 当前统一反馈实现已通过完整机器门禁和全新隔离 GUI E2E 8/8；GUI 根保留在 `C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-JEXEjm`。绑定该实现的新 candidate、安装态 Provider closure 和真实游戏验收尚未执行。

当前未完成：

- 构建并安装绑定新代码 HEAD 的 replacement ML candidate；当前 `660d513a` candidate 已因代码变化失效。
- 使用全新 Truth、Project、Draft、Graph、Run、Item、Artifact 和 Package 完成新的安装态 Provider closure，不复用当前暂停 Graph。
- 用户在真实 STS2 中完成角色、战斗、奖励、选卡、退出重启和存档恢复验收。

## 2. 当前代码事实

| 层 | 当前责任 |
| --- | --- |
| Kernel | 稳定 ID、schema/hash、typed failure、BuildInfo 和工程模板合同；不认识游戏类型 |
| Runtime | RunRecord v3、ArtifactManifest v3、取消、provider-neutral ports、typed validation issues 和 ExecutionGraphRecord v3/DAG/CAS/feedback/validation/repair/commit 状态机 |
| Game Context | pinned Pack v4、Truth Snapshot v2、item capability readiness、typed references、resource/composition profiles |
| Workspace | ProjectFolder/OS lock、受控 `local.props`、Run/Truth/Resource/Item/Draft/ExecutionGraph stores、pointer/publication journals 和原子 batch save |
| Feature | 12 个通用 Feature；Plan、Single、Batch、Complex、Composition、Build、Package、Resource 和 Log Analyze |
| Adapter | 共享 FIFO model queue、typed response format、HTTP model/media、filesystem repositories、PNG transforms、dotnet build 和 ZIP |
| Desktop/React | typed commands、Item/Resource/Composition workbenches、ExecutionGraph progress 与 Pause/Resume/Cancel；不解码 checkpoint |
| Web/CLI | Web 提供 health/catalog/SPA；CLI catalog 来自共享 Feature registry |

稳定架构说明见 [`项目架构总览`](../01-总览/项目架构总览.md)；当前阶段方案见 [`当前方案`](../03-当前方案/当前方案.md)。

## 3. Pack、Item 与 Resource 事实

STS2 Pack 正式注册六种 Item type：

```text
custom_code
relic
card
potion
power
character
```

Pack v4 顶层 `itemTypes` 是类型、canonical/localization fields、required locales、Evidence Queries、typed reference slots 和 conditional Resource profiles 的唯一真源。`compositionProfiles` 声明 preset/custom 参数、交叉约束和 128-node 上限。

```text
Pack SHA-256:
90b72f51911317e166802517329be5a5848b17b79961498953c5dfa72ee85df4

Character identity master SHA-256:
ec1b4f15aeb864166e0ec60936acfa390de9a0e9f2e06e41c5dc5a729a34c393
```

Character 合同包含 7 个 canonical fields、14 个 localization fields、6 个 typed reference slots 和 8 组 Truth Queries。

- `placeholder` 不要求 Character Resource binding。
- `branded_placeholder` 要求五个静态身份 roles。
- 512x512 `character.identity_master` 确定性派生顶栏 icon/outline、select/locked icon 和 map marker。
- Pack default 只按精确 Pack ID/SHA、asset ID 和 asset SHA 解析，不接受调用方路径。
- Upload、AI 和 Pack default 都先产生不可变 candidate；只有显式 select 才更新 selected pointer。

`character_suite` 默认 Standard；Prototype 为 11 nodes，Standard 为 35 nodes，当 Custom 在 Standard 上增加 8 个 Power 时为 43 nodes。数量、类型、依赖和 reference quantity 均来自 Pack，Feature 不写死 STS2 数量。

## 4. Composition 与恢复事实

CompositionDraft v2 保存 Pack/profile/root/nodes/revision、`sourceExecutionGraphId` 和 `validatedContentDigest`。审阅修订使用 revision CAS；内部 pinned DAG 在每次修订时自底向上重算 definition hashes；最终提交使用 `createOrMatch`实现精确创建或幂等匹配。

ExecutionGraphRecord v3 保存 owner/request/Blueprint identity、有序 nodes、attempt count/active attempt、当前已验证 checkpoint、hashed safe feedback state、active/previous Run、graph failure、commit intent 和 final result reference。feedback state 包含 round/phase、typed diagnostic fingerprint、candidate SHA 和/或 checkpoint hash，不保存 raw completion。状态修改使用 revision CAS，Run 创建前必须先获得 graph claim。v3 只读取 `.ats/execution-graphs-v3`，不读取或迁移 v1/v2，也不保留历代生成文件。

```text
composition.plan
  Pack-driven Blueprint
  -> Suite Brief
  -> serial bounded nodes
  -> local reference binding
  -> whole-graph validation
  -> atomic Draft createOrMatch

composition.generate
  item.000.plan
  -> item.000.single
  -> ...
  -> item.NNN.single
  -> composition.finalize
  -> registered validation
     -> pass: commit_prepared
     -> repairable: complete role-file replacement + full revalidation
     -> local/unsafe/limit: paused
  -> one build/package/publication transaction
  -> one composition Artifact
```

模型 transport/Provider failure 耗尽后暂停，不进入 semantic feedback。Direct Single 的 decode/typed output validation 仍严格单次失败；Composition Generate request v4 则把可修订 output-contract diagnostic 和可唯一归因的 registered-validation generated-content issue 纳入同一 controller。每次模型反馈调用使用独立 terminal child Run，并在调用前 CAS 持久化 typed/redacted envelope。错误候选仍被严格拒绝，重新生成后必须重走同一 output contract 和完整 validator suite。Feature 不自动换 model、endpoint 或 response format。

`repairPolicy` 是整个 Graph 的总 semantic-round 预算，不按节点放大；`until_passed` 也有 20 轮绝对安全上限。本地环境、不可修订错误、共享 merge 文件无法唯一归因、轮次耗尽、模型未改变任何 role 内容、同一 diagnostic fingerprint + candidate SHA，或同一 diagnostic fingerprint + checkpoint 都稳定暂停。`commit_prepared` 只在 validation 成功后创建。

definition-bound Plan 接收完整已验证 `StoredItemDefinition`，而不是仅依赖 behavior prose。Recipe 必须把 canonical fields、localizations、Resource/reference bindings 视为权威；typed decode 后 Feature 本地回填 `itemId`、`itemType` 和 `behaviorIntent`。Single 遇到 Plan prose 冲突时采用 definition，不通过文本启发式重建事实。

## 5. Prompt 与 Provider 事实

```text
Feature Recipe
+ Pack Contribution / selected Item descriptor
+ bounded Truth Evidence
+ selected Resource versions
+ Project Context
+ llm.custom_prompt
+ Typed Output Contract
= ModelRequestSnapshot v1
```

跨游戏任务语言属于 Feature Recipe；游戏类型知识属于 Pack；当前 API 事实属于 Truth；资源版本属于 Workspace；用户补充指令属于 `llm.custom_prompt`。

所有桌面 HTTP model 任务共享一个 FIFO 单飞队列。一个逻辑请求在全部 HTTP attempts 期间始终占用一个席位；排队取消不发请求。OpenAI-compatible output format 由 typed `json_schema`/`json_object` 配置在 Run 前固定，失败后不降级重发。

Single multi-file Recipe 输出上限为 16,384 tokens。普通 file role 是 bounded string；`compositionMerge=json_object` role 在模型合同中直接是 flat string-valued object，由 Feature 确定性排序并序列化进既有 checkpoint/project string 合同。invalid/truncated output 只持久化 `feature.mod-generate-single-failure-details` v1 的闭集 `reasonCode`，不保存 raw completion、parser text、Provider body 或未知 role 文本。

## 6. 当前验证证据

### 6.0 2026-08-12 统一反馈工作区增量证据

以下证据绑定当前统一反馈实现，不是 release candidate、安装态 Provider 或真实游戏验收：

| 门禁 | 结果 |
| --- | --- |
| Runtime ExecutionGraph v3 | 9/9 passed；含 checkpoint 前反馈、repair 输出反馈、双 hash、schema 拒绝、恢复和 validating/repairing 暂停 |
| Feature 单元测试 | 59/59 passed；含 closed diagnostic、redaction、Graph-total policy、ownership 和 User Cancel/Pause 分流 |
| Desktop staged composition | 7/7 passed；含 output failure -> feedback -> success、repair output failure -> feedback -> success、no-progress、repair crash recovery 和回滚 |
| Cargo workspace | all-target tests 232/232；check 与 `clippy -D warnings` passed |
| Desktop / Character | staged 7/7、Character 2/2；default、`ml-rembg`、`e2e` checks passed |
| Frontend / TypeScript / build | 36/36 passed；`tsc -b --noEmit` 与 production build passed |
| DAG / catalog / scripts | DAG self-test/real passed；CLI exactly 12 Features；release/build-plan script tests passed |
| GUI E2E | 全新隔离根 `C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-JEXEjm` 8/8 passed |
| GUI 持久化证据 | Graph v3 为 1 cancelled + 2 succeeded；Generate 23/23，同父 Run 内 1 次 output feedback；43 个 terminal Runs |
| Artifact / residue / lock | 4/4 manifest-to-Run、44/44 snapshot/published file length/hash；零 `.tmp`/`.bak`/staging/transaction；工程锁可独占重获 |
| rustfmt / diff / forbidden scan | passed；通用 feedback/Runtime/controller 无模型、Provider、STS2、Character 或 `merge_content` retry 分支 |

GUI 收口时保留了三个诊断根：`R1tI1I` 暴露 E2E 仍读 `execution-graphs-v2`，`OPEXZw` 在业务开始前发生 embedded WebDriver 初始化超时，`S1Wtao` 证明旧测试错误等待 Paused，而产品 Graph/Run/Build/Package 已在同一父 Run 内成功。三者均未删除，也不冒充通过证据。当前尚未执行：真实 candidate 构建/release verification/安装、安装态 fresh Provider closure 和用户真实 STS2 单人验收。

### 6.1 `660d513a` 提交基线证据

以下证据绑定 `660d513a` project-local fallback 最终树，不代表当前统一反馈工作树：

| 门禁 | 结果 |
| --- | --- |
| Runtime execution graph 定点测试 | 5/5 passed |
| Adapter validation runner 定点测试 | 5/5 passed |
| Workspace local.props / configured Create rollback 定点测试 | 20/20 workspace tests passed |
| Desktop composition generation 回归 | 6/6 passed；含 validation reject -> 单 owner repair -> 全量复验 -> succeeded，以及 repair Single checkpoint 落盘后、finalize checkpoint 替换前的 crash/recover/resume |
| Feature repair policy / ownership / stop-condition 单元测试 | 57/57 passed |
| Cargo workspace all-targets check | passed |
| Cargo workspace all-targets tests | 225/225 passed；含环境绑定 STS2 Character 2/2 |
| Cargo workspace clippy | passed with `-D warnings` |
| Desktop features | `ml-rembg` / `e2e` checks passed |
| Frontend | 35/35 passed |
| TypeScript / production build | passed |
| Stage 2 DAG | self-test / real check passed |
| CLI catalog | exactly 12 unique Features |
| Release / build-plan scripts | passed |
| Legacy guards / rustfmt / diff | passed；production forbidden-pattern search zero matches |
| v2 隔离 GUI E2E | 8/8 passed；保留根 `C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-7o278C` |
| GUI Artifact / residue / lock 独立复核 | 44/44 file length/hash、4/4 manifest-to-Run matched；Truth/project/package staging/transaction 0 children；lock reacquired |

`b68c33df` candidate 已构建并安装，但后续生产 fallback 修复使其失效。`660d513a` 的完整机器门禁已经通过；当前统一反馈工作树不能复用该结论。

### 6.2 更早已提交基线

| 门禁 | 结果 |
| --- | --- |
| Cargo workspace | 206 tests passed |
| Frontend | 35/35 passed |
| GUI E2E | 8/8 passed |
| Workspace check/clippy | passed |
| Desktop baseline/`ml-rembg`/`e2e` checks | passed |
| TypeScript / production build | passed |
| DAG self-test / real check | passed |
| CLI catalog | exactly 12 Features |
| Release/build-plan scripts | passed |
| Legacy guards / rustfmt / diff check | passed |
| Environment-bound STS2 Character tests | 2/2 passed |

上一轮 definition-bound 修复的隔离 GUI 证据根 `C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-juNdlk` 通过 8/8。Generate graph 23/23 succeeded，24 logical attempts 证明只重试失败的 Character Single；composition manifest SHA `fa523c76d0275a657571d05e1946b1f82c0dcb1c7742a1fcb4f8073e8492fc7f` 与成功 Run 匹配，41/41 Artifact 文件长度/hash 重算一致，两个 ZIP 存在，Truth staging、project transaction 和 package transaction 子项为零，工程锁可重新独占获取。首次 120 秒外层超时但子进程继续完成的根 `ats-gui-e2e-O3OWY3` 也保留为非正式诊断证据，不冒充正式通过根。本轮 typed merge-object 修复另行通过隔离 GUI E2E 8/8；runner 按成功策略清理了临时根，因此只记录命令终态与 reporter 结果，不虚构保留路径。

### 6.3 已失效 Candidate 与失败构建证据

```text
ID: rc-20260811T093826Z-bb96a80f9de6
commit: bb96a80f9de6c35ccaba4c009e3336ef0f3b671e
verification: artifacts/release/rc-20260811T093826Z-bb96a80f9de6/release-verification.json
status: succeeded
steps: 10/10
variants: baseline [] + ml [ml-rembg]
runtime BuildInfo: 2/2 verified
installer size/SHA-256: 4/4 independently matched
candidate files: 7
candidate/build residue: 0
signature: 4/4 NotSigned
installed: yes; invalidated by the typed merge-object repair
```

Verification SHA-256：

```text
90af5367a6ab12a20846eb6b776c0bd5c58e3cb46c73cedb4c5c23722b78476d
```

两个 variant 的 runtime BuildInfo 均已验证；baseline Features 为 `[]`，ML Features 为 `["ml-rembg"]`。独立复算匹配 verification、两个 manifest 和四个安装器的长度/SHA-256，candidate 精确包含 7 个文件，candidate 与两个隔离 build 根均无 `.tmp-*`、`.staging` 或 transaction 残留。该 candidate 后续已安装并产生全新物理验收证据，但 Character Single 两次稳定失败为 `model.output_invalid / merge_content`；当前代码修复使它失效，不能继续代表成功验收。四个安装器均为 `NotSigned`。完整账本、Run ID 和 hashes 见本地 Trellis 任务的 `acceptance.md`。

绑定 `dcf711218e828fb730c9251de71637c7f55d441f` 的 replacement candidate 已发起四次，均保留原始目录和 `release-verification.json`，但没有一次形成可安装的完整 candidate：

| Build ID | 构建位置 | 终止步骤 | 原因 |
| --- | --- | --- | --- |
| `rc-20260811T151018Z-dcf711218e82` | 主仓库 | `build-ml` | `I:` 磁盘空间耗尽；baseline 已验证 |
| `rc-20260811T152046Z-dcf711218e82` | `D:` detached worktree | `workspace-check` | Git-ignored STS2 SDK package cache 不存在 |
| `rc-20260811T152344Z-dcf711218e82` | 同一 worktree，补齐精确 cache 后 | `build-ml` | baseline 后 `Cargo.toml` 内容相同但 Git stat 状态为 `M` |
| `rc-20260811T153735Z-dcf711218e82` | `D:` 独立 clone，补齐精确 cache 后 | `build-ml` | 独立复现同一 stat-dirty 误判 |

后两次中 `src-tauri/Cargo.toml` 的工作区 blob 和 `HEAD` blob 均为 `e254682a3e86cfd7c3789a131ee07c97103d8831`，`git diff` 为空。根因是 `build.ps1` 把 `git status --porcelain` 的 stat-dirty 结果直接当成内容变化；`release-candidate.ps1 -Variant All` 完成 baseline 后，ML 的独立 clean guard 因此立即失败。当前发布脚本修复改为分别检查 unstaged diff、staged diff 和非 ignored untracked files；真实变更继续失败，同内容重写不会阻断后续 variant。发布脚本定点测试和失败 clone 对照已通过，但该代码变化使 `dcf71121` 及其失败构建均不能代表后续候选。

### 6.4 已失效的 Replacement Candidate

```text
ID: rc-20260812T084008Z-b68c33df4e3b
commit: b68c33df4e3b87112b6b92d499f4123a0b5ef70b
root: I:\WebCode\AgentTheSpire\artifacts\release\rc-20260812T084008Z-b68c33df4e3b
status: succeeded
steps: 10/10
variants: baseline [] + ml [ml-rembg]
runtime BuildInfo: 2/2 verified
candidate files: 7
candidate/build residue: 0
installed: yes; runtime ml / exact Build ID / 12 Features verified
```

该 candidate 的 release verification 为 10/10 succeeded，baseline/ML 两个 runtime BuildInfo 均验证，四个 installer 长度/hash 独立匹配。安装态 Dashboard 报告 exact Build ID、`variant=ml`、12 Features 和 Truth ready；SYSTEM 已核对 `openai/deepseek-v4-flash`，配置显式为 `json_object` 与 120/300 秒退避。

候选根精确包含 verification、两个 manifest 和四个安装器，零临时残留；四个安装器均为 `NotSigned`。本轮 GUI E2E 随后暴露 `execution-graphs-v1` 旧测试路径和全局 Godot 清空错误阻断有效 project-local Build 两个缺陷。当前代码修复使该 candidate 失效，不能用于后续成功验收。

## 7. 当前任务与下一步

```text
task: .trellis/tasks/08-05-sts2-character-cross-item-composition
status: in_progress
currentOrder: 8
completedOrders: [1,2,3,4,5,6,7]
```

下一步是提交已通过机器门禁和最终审查的统一反馈闭环，并重新构建、安装绑定新最终 HEAD 的 ML candidate；随后使用全新的 Truth、Project、Draft、Graph、Run、Item 和 Artifact 根完成安装态 closure。push、rebase、签名、发布和真实游戏 UI 仍是独立授权门禁。

真实 STS2 UI 由用户操作。只有用户明确确认真实游戏结果后，才能勾选 PRD 剩余验收项、完成 Trellis 任务并归档。

## 8. 边界与非结论

- 已安装的 `51e0ae6b61dc` 与 `bb96a80f9de6` candidates 都只形成失败诊断，均已被后续代码修复失效。
- `b68c33df` replacement candidate 已构建、安装并验证身份，但已被当前生产 fallback 修复失效；历史失败构建和本轮 GUI 失败根仍只作为诊断证据保留。
- `660d513a` replacement candidate 已构建、安装并推进到 fresh Character Generate；其暂停 Graph 证明 Provider output contract 尚未收敛，未形成成功 closure。
- 统一生成反馈闭环已通过 workspace 232/232、frontend 36/36、完整机器门禁和隔离 GUI E2E 8/8；尚未完成绑定该实现的新 candidate 和安装态 Provider closure，必须使用新 candidate 和新物理根重新验收。
- 未验收真实 STS2 角色行为、奖励池和存档恢复。
- 未验收 AI 图片美术质量、安装器签名或 SmartScreen。
- 历史 candidate、Truth、Run、Artifact 和诊断根只能证明其绑定代码，不能代表当前 candidate。
