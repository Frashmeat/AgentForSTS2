# 当前进度说明

> 文档定位：`rust` 分支当前能力、最新 candidate 证据、已确认缺口和剩余门禁的事实入口。
>
> 不包含：新架构的完整实施合同或逐次 candidate 流水。
>
> 当前任务：`.trellis/tasks/08-05-sts2-character-cross-item-composition/`。
>
> 最后更新：2026-08-18

## 1. 当前结论

O1-O8、Game Pipeline Provider foundation 和显式模型输出预算合同已完成。输出预算实现已以 `b7d8e698 feat(model): add explicit output budget contract` 提交，并构建、安装了绑定该提交的 replacement candidate。

该 candidate 的 fresh installed Character Composition 已完成 35/35 生成节点，随后编译验证将 17/17 Items 纳入 generated-content Repair Campaign。系统完成 11/17 个修复目标后，Graph 因累计 semantic request 达到既有绝对上限 20 而安全暂停。

```text
branch: rust
HEAD: b7d8e698 feat(model): add explicit output budget contract
current candidate: rc-20260817T093831Z-b7d8e698ace8
installed variant: ml / ml-rembg
acceptance root:
D:\AgentTheSpire-real-game-acceptance\rc-20260817T093831Z-b7d8e698ace8\20260817-183704
project: ATSCharacterO9-B7D8-20260817-1837
generate graph: graph-000000000000000018cc9e786cb0b874-00000003
graph state: paused
graph nodes: 35/35 succeeded
repair targets: 11/17 completed, 6 pending
semanticRequestCount: 20
failure: validation.repair_limit / composition.generate.validate
```

这套 candidate、Graph、Run 和未发布的闭包仅保留为架构失败证据。直接 Resume 不会清零 Graph 总计数，只会再次命中 `validation.repair_limit`。

## 2. 已实现能力

| 层 | 当前事实 |
| --- | --- |
| Runtime | RunRecord v3、ArtifactManifest v3、ExecutionGraphRecord v4、DAG/CAS、checkpoint、repair campaign、commit 状态机 |
| Game Context | Pack v4、Truth Snapshot v2、typed references、Pipeline Provider contract/registry |
| Game Provider | 独立 `ats-game-sts2` 拥有 STS2 validation/build/package/Prepare；synthetic data-only Provider 已证明可变阶段图 |
| Feature | 精确 12 Features、Composition Plan/Generate、whole-closure publication、多 Item repair、单 Item adjustment |
| Model transport | 共享 FIFO、OpenAI-compatible SSE、分层 transport retry、显式 `max_output_tokens`、Graph-pinned limits |
| Resource | Upload/AI/Pack default 不可变 candidate、确定性 transform、显式 select、role/version binding |
| Shell | typed Tauri commands、Item/Resource/Composition workbench、简化运行状态和单 Item 调整 |

STS2 Pack 当前精确注册 `custom_code`、`relic`、`card`、`potion`、`power`、`character` 六种 Item type。Character 支持 Placeholder 和 Branded Placeholder；身份 master 为 512x512 并确定性派生五个静态资源。

## 3. 本次 E2E 证据

### 3.1 输入与资源

```text
model: deepseek-v4-pro
configured max output tokens: 16384
draft: o9-b7d8-custom-20260817-195031 revision 39
items: 17
confirmed root hash: b18bda5c3df32ffa0b4e5d7d52e61163f60e4fd0c07883445a152ff09335f6d8
validated content digest: a3caa7aed546ec24622fc79c914c6c2c0867b88221ace97c5268ccea875adeef
required resource bindings: complete
```

图像只执行了一次 `gpt-image-2` 健康生成；其他 Card/Relic/Potion/Power 资源复用已有图片或本地确定性缩放，没有进行大规模批量生图。

### 3.2 Generate Graph

- Character 和 17 个 Item 的 Plan/Single 节点全部成功。
- output feedback 实际处理了 `output_truncated`、`json_decode` 和 `merge_shape`。
- 节点暂停后 Resume 只继续当前 checkpoint，未重跑早期成功节点。
- `composition.finalize` 进入真实 dotnet whole-closure validation。
- 首轮生成的每个 Item 都包含不存在或错误的 STS2/BaseLib 类型、命名空间或生命周期用法。
- Repair Campaign 能通过 typed compiler diagnostics 修复 11 个 Item，但固定 Graph 总预算使剩余 6 个目标失去首次执行机会。

### 3.3 尚未产生的证据

由于 registered validation 未通过，当前 Graph 没有可验收的 final result、composition Artifact、最终 package ZIP 或已发布工程目录。不得将 staging/checkpoint 中间产物人工命名为成功 Artifact。

## 4. 已确认的架构缺口

当前 Pack-driven 边界仍然允许 AI 直接生成 source 和 localization bundle。因此：

1. 模型必须同时正确理解业务意图、游戏 API、跨 Item 引用、文件角色和合并格式。
2. 一个 source 编译问题会迫使模型重新输出无关 localization，制造新的格式错误。
3. Adapter/Truth 缺陷和语义缺陷无法从产物作者权上清晰区分。
4. 固定 20 次 Graph 总语义预算同时承担首次修复和额外反馈，对大闭包会出现合法节点饥饿。

已确认采用 [`Typed Behavior IR 与 Game Pack 确定性生成架构`](../03-当前方案/Typed-Behavior-IR与Game-Pack确定性生成架构方案.md)，不单独提高 20 次常量。

## 5. 当前任务与下一步

```text
parent: 08-05-sts2-character-cross-item-composition
completed subtask: 08-17-model-output-budget-contract
current subtask: 08-18-typed-behavior-ir-game-pack-adapter
parent currentOrder: 10
```

下一步是先实现 Behavior IR 与 STS2 确定性 Adapter，再构建新 candidate。当前失败 Graph 不继续 Resume，也不用于新实现验收。

## 6. 授权与证据边界

- 当前文档/Trellis 方案更新已获授权；跨 crate 实现尚未开始。
- 实现完成后的全量测试/构建必须根据影响范围单独执行。
- candidate 构建、安装、发布、Git 提交/push/rebase 需要明确授权。
- 真实 STS2 UI 只由用户操作。
- 不删除任何 release、verification、Truth、Project、Draft、Item、Resource、Run、Graph、Artifact、Package 或失败证据。
