# 当前进度说明

> 文档定位：`rust` 分支当前能力、最新验证结论和剩余门禁的事实入口。
>
> 不包含：逐次 candidate 时间线、详细实现方案或可执行合同。
>
> 当前任务证据：`.trellis/tasks/08-05-sts2-character-cross-item-composition/acceptance.md`。
>
> 最后更新：2026-08-15

## 1. 当前结论

Game Pack Stage 2 基础架构和 STS2 六种 Mod 类型已经落地。活动任务仍是 `08-05-sts2-character-cross-item-composition`，状态为 `in_progress`。

2026-08-15 子任务 `08-15-game-pipeline-provider-foundation` 已完成。当前未提交代码已实现
Provider v1/registry、独立 `ats-game-sts2`、Pack `pack.composition-generate` v2、Blueprint v6
完整 graph 持久化、Provider-driven validation/build/package，以及受信任 Prepare executor 编译
边界。synthetic data-only Provider 已通过真实 Composition service 路径，在零模型、零 validator、
零 build/package 调用下产生 succeeded Run/Graph、Artifact 和最终 JSON。O4 合同审计、完整
机器门禁和隔离 GUI E2E 8/8 已通过；父任务只因 O9 candidate 和真实游戏验收保持 `in_progress`。

```text
branch: rust
committed baseline: ccb4be11 docs(stage2): bind mid-repair resume evidence
working tree: uncommitted Game Pipeline Provider foundation Orders 1-4
last installed candidate: rc-20260814T175633Z-ccb4be116617 (invalidated by current code changes)
completedOrders: [1,2,3,4,5,6,7,8]
currentOrder: 9
O8: completed; multi-item automatic repair and single-item adjustment
O9: in progress; new candidate and installed real-game acceptance
```

当前 STS2 Pack SHA-256 已因 Provider selection 变为
`9dfb4d80a8fb5abae41abcb57869b13443038d6ba60ff1129060137bc3d55254`。所有已构建 candidate
（包括 `rc-20260814T175633Z-ccb4be116617`）均只保留诊断价值；本轮未构建或安装新 candidate。

当前代码已经具备 Pack-driven Character Studio、typed cross-item composition、ExecutionGraph v4、可恢复多 Item repair campaign、版本绑定的单 Item 用户调整、whole-closure validation、原子发布和 Provider-owned 游戏阶段图。O8 与 Provider foundation 已通过全部机器门禁。上一 candidate 已被当前跨 crate 代码变化失效，必须在明确授权后绑定新 candidate 并重做 fresh closure。

所有既有 O9 candidate 均只保留诊断价值，不能代表当前修复后的实现。最新完整路径和失败证据见 Trellis `acceptance.md`，不得删除、改名或 Resume 旧 Graph 冒充成功结果。

## 2. 当前实现事实

| 层 | 当前责任 |
| --- | --- |
| Kernel | 稳定 ID、schema/hash、typed failure、BuildInfo 与工程模板合同；不认识具体游戏类型 |
| Runtime | RunRecord v3、ArtifactManifest v3、ExecutionGraphRecord v4、DAG/CAS、feedback、repair campaign、adjustment 和 commit 状态机 |
| Game Context | pinned Pack v4、Truth Snapshot v2、typed references、Resource/composition profiles、Pipeline Provider contract 与 registry |
| Workspace | Project lock、受控 `local.props`、Run/Truth/Resource/Item/Draft/Graph stores 和原子 publication journals |
| Feature | 精确 12 个通用 Features；Plan、Single、Batch、Complex、Composition、Build、Package、Resource 和 Log Analyze |
| Adapter | 共享 FIFO model queue、typed response format、HTTP model/media、Provider 图片规范化、PNG transforms、dotnet build 和 ZIP |
| Desktop/React | typed commands、Item/Resource/Composition workbenches、简化生成状态、Pause/Resume/Cancel 与逐 Item 调整 |

当前 Composition Generate 的关键事实：

- 模型请求使用共享 FIFO 单飞队列；请求间隔、transport retry 与语义 feedback 分层处理。
- Direct Single 保持严格单次输出合同；Composition 可把多个 generated-content issues 精确归属到 Item，并按 DAG 顺序串行修复。
- Graph-total repair policy 和 20 次绝对语义请求上限覆盖整个 campaign，不按 Item 数量放大。
- 本地环境、共享文件、归因不明确、重复诊断、未改变 candidate、取消和存储错误都会安全暂停。
- `commit_prepared` 只在 registered validation 成功后创建；发布仍是 whole-closure 原子事务。
- succeeded 结果的逐 Item 调整派生新 Graph/Run/Artifact version，源证据不变；paused pre-commit Graph 才允许原地 CAS 调整。
- shared、ambiguous、local 或 non-repairable issue 继续安全暂停，不发送给模型。

## 3. Pack、Item 与 Resource

STS2 Pack 精确注册六种 Item type：

```text
custom_code
relic
card
potion
power
character
```

```text
Pack SHA-256:
9dfb4d80a8fb5abae41abcb57869b13443038d6ba60ff1129060137bc3d55254

Character identity master SHA-256:
ec1b4f15aeb864166e0ec60936acfa390de9a0e9f2e06e41c5dc5a729a34c393
```

- Pack v4 是字段、localization、Evidence Query、typed reference slot、Resource profile 和 composition profile 的真源。
- Character 支持 Placeholder 和 Branded Placeholder。
- Branded Placeholder 由 512x512 master 确定性派生五个静态身份资源。
- Pack default 只按精确 Pack ID/SHA、asset ID 和 asset SHA 解析，不接受调用方路径。
- Upload、AI 和 Pack default 均先形成不可变 Resource candidate，只有显式 select 才改变 selected pointer。
- `character_suite` 支持 Prototype、默认 Standard 和有界 Custom；数量、依赖和引用均由 Pack 驱动。

## 4. 当前机器证据

当前 Provider foundation 最终工作树的机器证据：

| 门禁 | 结果 |
| --- | --- |
| Cargo workspace | all-target tests 252/252；check 与 `clippy -D warnings` passed |
| Game Context / Provider / Desktop focused | Game Context 18/18、STS2 Provider 2/2、Desktop composition 10/10、Character 2/2 |
| Frontend | 38/38；TypeScript、production/e2e build passed |
| Desktop features / DAG / catalog | default workspace、`ml-rembg`、`e2e` checks；DAG self-test/real；exactly 12 Features |
| Scripts / guards | build-plan、release-candidate self-tests、legacy/Prompt guards、rustfmt 与 diff check passed |
| Isolated GUI E2E | 8/8；2026-08-15 成功根按 runner 成功清理合同自动清理，reporter 终态保留 |
| GUI / integration evidence | Run/Graph/manifest hash、单一 Artifact、零 staging/transaction/atomic-write residue 和 lock reacquisition passed |

本轮修复将通用 composition contributor ownership 与 STS2/BaseLib runtime localization key
分离：`unique_keys` 在当前 Single checkpoint 前检测跨 Item 重复键并进入 typed feedback，
所有 restore 与 repair replacement 路径重验 claims，`exclusive_path` 和 finalize 保留硬校验。
Feature 不改写或强制 Item ID key；Pack、Truth 与 validator 继续拥有游戏运行时身份合同。

本轮 GUI E2E 首次失败根 `C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-HQJGur` 和
`C:\Users\25012\AppData\Local\Temp\ats-gui-e2e-iaMkRa` 已保留。诊断证明 Pack、Truth 与 Draft
身份一致，实际根因是前端仍提交 Composition Generate request v5，而后端已破坏性升级到 v6；
Run/Graph 因此尚未创建。前端 envelope 与 canary 同步到 v6 后，全套 GUI E2E 通过 8/8，
没有放宽 Pack/Draft identity 校验。

这些事实证明 O8 当前代码和机器合同，不证明新 release candidate、安装态 Provider closure 或真实游戏验收完成。

## 5. 最新安装态 Fresh Closure

```text
candidate:
rc-20260814T175633Z-ccb4be116617

acceptance root:
D:\AgentTheSpire-real-game-acceptance\rc-20260814T175633Z-ccb4be116617\20260815-021518

Composition Generate Graph:
graph-000000000000000018cbc3042055f228-00000001
```

实际结果：

- fresh Truth、Project、Draft、11 个 immutable Items、26/26 required Resource bindings、37/37 manifests 和 11/11 `resource.prepare` Runs 完整。
- 唯一 Generate Graph 在第一个 Character Single node 暂停；一次模型结果因 localization `merge_shape` 进入 bounded feedback，后续请求耗尽为 `model.transport_failed`。
- 没有 build output、ZIP、Artifact 或最终发布，也没有 `.staging`/transaction 残留。
- 当前 Provider foundation 代码变化已使该 candidate 失效；目录与 Run/Graph 继续原样保留为诊断证据。

## 6. 当前缺口

当前只剩 O9：

1. 取得明确授权后提交代码，并构建、安装绑定最终 HEAD 的新 candidate；此前 candidate 不得复用。
2. 使用全新 Truth、Project、Draft、Items、Run、Graph、Artifact 和 Package 完成成功 closure。
3. 证明真实 multi-Item generated-content failure 可自动串行修复并通过 whole closure。
4. 核对 succeeded Run/Graph、ArtifactManifest/hash、ZIP、最终目录、零残留和工程锁重获。
5. 由用户完成真实 STS2 Character、战斗、奖励、选卡、重启、存档恢复和逐 Item 调整验收。

详细设计见 [`批次生成自动修复与单项调整架构方案`](../03-当前方案/2026-08-13-批次生成自动修复与单项调整架构方案.md)。

## 7. 授权与证据边界

- candidate 构建、安装和应用启动需要再次明确授权；提交、发布、push、rebase 和真实游戏 UI 操作均不在当前修改授权内。
- 代码变化后不得复用旧 candidate、旧 Truth、旧 Run 或旧 Artifact 代表新实现。
- 不删除 release、verification、`.tmp`、Truth、Project、Draft、Run、Graph、Resource、Item、Artifact、Package 或失败证据。
- 只有用户明确确认真实游戏结果后，才能完成并归档当前 Trellis 任务。

被替代的 2026-08-12 长篇进度快照已移至 [`历史归档`](../90-归档/历史审查与验证/Rust重写/2026-08-12-Stage2-O8当前进度快照.md)，仅供追溯，不代表当前状态。
