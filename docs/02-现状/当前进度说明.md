# 当前进度说明

> 文档定位：本文是 `rust` 分支当前阶段、能力状态、验证证据和执行队列的事实入口。
>
> 事实依据：当前 `crates/`、`src-tauri/`、`src/`、`scripts/` 代码，Git 工作区状态，以及截至 2026-08-01 的自动与人工验证证据。
>
> 权威入口：协作规则见 `PROJECT_SPEC.md`；阶段计划见 [`docs/03-当前方案/当前方案.md`](../03-当前方案/当前方案.md)。
>
> 最后更新：2026-08-02

## 1. 当前结论

项目当前处于 **Rust/Tauri 桌面端 MVP 收口阶段**，不是 Web/SaaS 平台建设阶段。

桌面端主要功能、真实游戏主链、运行时加固和 baseline/ML 安装版 provenance 已完成验收。Work Order 5 的机器发布范围也已完成：唯一 Windows release candidate 入口、完整门禁、双 variant 候选、机器 verification 和文档/code-spec 均已收口。当前只等待用户对新候选执行最终安装/启动冒烟。

本阶段不包含：Web auth/admin/users/tokens、SQLx/PostgreSQL/migrations、平台级 jobs、new-api 管理集成、自动更新、签名公证和跨平台打包。

## 2. 已落地能力

| 范围 | 当前代码事实 | 状态说明 |
| --- | --- | --- |
| 核心领域 | `ats-core` 覆盖配置、工程、LLM、图像、知识库、规划、代码生成、Run 与 Artifact Store 等能力 | 代码已落地 |
| 桌面工程 | Tauri 支持创建、打开、关闭和 recent projects；`ProjectSession` 统一持有 OS 锁、共享 repository 和活动 Run task | close/switch/app exit 已接入 30 秒 cancel-and-drain |
| Run 链路 | 已注册 `text_generate`、`single_asset_plan`、`code_generate`、`asset_generate`、`batch_custom_code`、`build_project`、`package_project`、`log_analysis`、`truth_snapshot_refresh` 9 类 handler | RunRecord v2、timeline、typed result、V1 history 备份和前端 Run API 已落地 |
| 模型接入 | 支持 Anthropic 与 OpenAI 兼容 LLM、OpenAI Images 兼容图像生成、流式输出和重试 | 代码已落地，真实代理错误路径待验收 |
| 图像处理 | 支持可选 `ml-rembg`、Tauri prewarm/singleflight retry、稳定 fallback 和 processor/model/runtime provenance | ML 安装版已从空目录完成首次 prewarm 与图片 ArtifactManifest 验收 |
| 游戏真相源 | Game Pack 声明 source/indexer/provider；Truth Snapshot 对 staged source/index/tool 进行内容寻址、完整性验证和原子激活 | STS2 游戏与 BaseLib `v3.3.8` 的 3,425 文件游戏索引、Pack/Snapshot identity 和生成 Evidence 已通过真实链验收 |
| 桌面前端 | 已有 Dashboard、Mod Editor、Batch Generation、Log Analysis、System/Settings 与 DevTools 页面 | Godot 配置、新建工程和 single asset/build/package GUI 主流程已验收 |
| 配置与运行记录 | Settings 热替换、Health/Capabilities 和 Run history 已接入；独立 lifecycle Audit 已删除 | Run 与 Tauri command 共用结构化失败契约，前端统一展示可行动错误 |
| Web 壳 | `ats-web` 提供 health、knowledge、planning、codegen、llm 和静态资源托管 | 轻量可用，不等同完整 SaaS |
| CLI | `ats-cli` 提供 build、deploy、logs、stop 和 compose template 等命令 | 代码已落地，不是本阶段主交付面 |

## 3. 2026-05-29 后的重要进展

在原桌面端 MVP 收口计划之后，仓库继续完成了以下工作：

- 前端 Run ID 进入跨路由 store，任务执行期间切换页面不再丢失进度。
- Knowledge 配置、状态和操作被整理到统一卡片，System 页面职责进一步收敛。
- LLM 与 image generation provider/size 改为稳定的选项控件，同时兼容当前自定义值。
- 设置表单的敏感字段语义得到统一：后端脱敏值不装入 GitHub token/API key 编辑框，未编辑时不产生 patch，明确输入空值时仍可清空。
- 工程锁从“锁文件是否存在”改为 `std::fs::File::try_lock()` 的操作系统独占锁；进程退出后锁自动释放，常驻诊断文件不再造成假死锁。
- 新增前端纯逻辑测试命令，并接入 GitHub Actions 与本地 CI 脚本。
- README、前后端质量规范和 cross-layer skill 中的失效或过时说明已在工作区修正。

上述收口改动已经在当前 `rust` 分支形成提交；其中工程锁与敏感设置修复见 `224e1d69`，前端开发依赖安装修复见 `4cf58195`。本次 E2E 开始前工作区为干净状态。

## 4. 当前验证证据

2026-07-27 已执行并通过：

| 验证项 | 结果 |
| --- | --- |
| 工程文件夹定向测试 | 6/6 通过，包括持锁拒绝二次打开、释放后复用常驻锁文件 |
| 前端设置表单测试 | 3/3 通过，包括脱敏 token 不回写、修改和清空语义 |
| `cargo clippy -p ats-core --all-targets -- -D warnings` | 通过 |
| `npx tsc --noEmit` | 通过 |
| `git diff --check` | 通过 |

这些证据只覆盖本轮修复及相关静态检查，不能替代真实桌面端和完整工作区验收。

2026-07-28 又执行了一次真实 single asset 核心服务链 E2E：

- 真实 LLM 规划与真实图片/C# 生成完成；最终 7 条 history 和 21 条 audit 覆盖 completed、build failed 与 LLM 空输出 failed，状态可追溯。
- `dotnet publish` 首次因默认 `GodotPath` 不存在停止；补充 `I:\Godot` 后，人工修正 C# 和本地化的夹具通过正式 build/package job，生成 DLL、约 3.01 MB PCK 和约 3.43 MB zip。
- 自动 codegen 仍不可交付：大写类型不命中 guidance；小写 GUI 等价请求仍生成错误 hook/using；prompt 与 handler 无法正确落双语本地化；一次相同请求还出现瞬时空输出。
- 图片包含像素化棋盘格，simple rembg 未形成真实透明背景；PCK 还误打包 history、items、artifacts、packages 和 project metadata。
- 详细证据与后续顺序见 [`2026-07-28 single asset E2E 验证记录`](../90-归档/历史审查与验证/Rust重写/已经完成/2026-07-28-single-asset-E2E验证记录.md)。

同日按主链优先完成修复并在全新隔离工程复验：

- `Relic` 等类型统一归一化；prompt 内联真实 ModId 和 `MainFile.cs`，模型返回严格 C# + `eng/zhs` localization bundle。
- 空输出/非法 bundle 最多尝试 2 次；正式 C#、本地化和运行时图片使用文件事务，compile gate 失败统一回滚，artifact 保留诊断。
- compile gate 使用隔离 `ModsPath`，并修复 Windows `\\?\` MSBuild 属性路径；`E2EEnergySeedRelic` acronym 分词与 STS2 analyzer 对齐。
- Rust 自动落普通/outline/big 遗物图片；PCK 真实目录含本地化和导入纹理，不含 `.ats/`、`artifacts/`、`history/`、`items/`、`packages/`、`Generated/` 和 `project.json`。
- `.tmp/e2e-runs/1785222129-38860` 中 4 个 job 全部 completed、12 条 audit 一致；compile gate、正式 publish、package 和 PCK 目录复验通过，全程未人工修改生成内容。

随后完成 `GodotPath` 产品配置链和真实 Tauri GUI E2E：

- Settings 新增 `toolchain.godot_exe_path`，非空值保存前运行有超时的真实 `--version` 校验，只接受 4.5.1；空字符串可明确清空。
- `local.props` 改为结构化更新 `SteamLibraryPath` / `GodotPath`，创建、打开、设置保存、asset/code 和 build 入口复用同一同步实现，并保留未知 XML、`ModsPath` 和自定义字段。
- E2E 专用 config、app-data、工程和 Mods 全部位于临时根；本地确定性 LLM/image stub 不调用外部 API，.NET、STS2 DLL 和 Godot 保持真实。
- 最终 WebdriverIO embedded Tauri suite 为 3/3 通过：无效路径拒绝、4.5.1 持久化、新建工程、delayed job 跨页恢复、asset compile gate、GUI build/package，以及清空后 build 前置拒绝。
- 详细证据见 [`2026-07-28 GUI E2E 与 GodotPath 验证记录`](../90-归档/历史审查与验证/Rust重写/已经完成/2026-07-28-GUI-E2E与GodotPath验证记录.md)。

同日使用 Steam build `23811903`、游戏版本 `v0.107.1` 执行知识库隔离验证：

- 最新 `sts2.dll` 真实反编译成功，生成 3,425 个 C# 文件、18,378,292 字节；manifest 与源 DLL 匹配，二次运行正确缓存命中。
- 使用现有 BaseLib `v3.2.1` 完成隔离反编译，知识状态为 `fresh`；card/relic/power/character 查询和 5 个关键游戏 symbol 均返回事实。
- `Sts2CodeFactsProvider` 固定只扫描 2,000 个文件，已复现 `NSettingsScreen` 游戏类被遗漏；未认证 GitHub 请求被限流，当前配置 token 返回 401。
- 详细证据见 [`2026-07-28 最新游戏版本知识库验证记录`](../90-归档/历史审查与验证/Rust重写/已经完成/2026-07-28-最新游戏版本知识库验证记录.md)。

随后完成知识库全量事实索引与 GUI E2E 修复：

- `.cs` 路径先确定性排序再扫描，安全上限提高到 10,000；2,002 文件回归、稳定顺序、小上限明确截断和空目录场景均通过。
- 对正式知识目录做只读查询，`NSettingsScreen`、`DamageCmd`、`PowerCmd`、`CardModel`、`RelicModel`、`CharacterModel` 均返回游戏事实且无截断 warning。
- GitHub 401、rate-limited 403/429 和普通 403 统一映射为可行动且不回显原始 body 的错误。
- 当时曾让 `list_jobs/get_job/cancel_job` 同时读取工程任务和全局 `knowledge/jobs`；该兼容路径随后已被 RunRecord v2 与 `ProjectSession` 取代。当前产品只使用活动工程的 `list_runs/get_run/cancel_run`，legacy `knowledge/jobs` 不再进入查询或取消路径。
- 真实 Tauri GUI suite 为 4/4 通过：最新 DLL 产生 3,425 文件，manifest/实际计数一致，BaseLib 401/403 warning 在任务详情中可见且不泄露 stub 敏感内容。
- 更新本地忽略配置中的 GitHub token 后，项目客户端真实下载 BaseLib `v3.3.8`（1,014,272 字节），`ilspycmd` exit 0，生成 1,607,318 字节 C# 输出并构造匹配的 release record。

2026-07-31 完成桌面运行时 Work Order 2 的结构化失败契约：

- Core 根级 `ActionableFailure` 是 Run、Tauri 和 React 的唯一字段模型，首批覆盖 LLM、Image、Package、Project、Toolchain、ImageProc 和 Run。
- 所有生产 fallible Tauri commands 使用透明 `CommandFailure`；未知错误固定映射 `core.unclassified`，provider body、token、URL query 和绝对用户路径不再通过字符串边界对外。
- 前端所有 Tauri invoke 经过严格 schema guard，页面错误 state 统一为 `ActionableFailure`，字符串或畸形 reject 不直接显示。
- Core handler 69 项、ML 7 项、Tauri failure 2 项和 frontend 7 项定向测试通过；Core/Desktop check、TypeScript check 与 `git diff --check` 通过。

2026-08-01 完成桌面运行时 Work Order 3 的 ProjectSession 与 Run 并发正确性：

- `ActiveProject` 只暴露一个长期 `Arc<ProjectSession>`；工程 OS 锁、共享 `FileRunRepository`、Run cancellation token 和 `JoinHandle` 具有同一生命周期。
- close、工程切换和 Tauri `ExitRequested` 均先拒绝新提交、取消并等待 Run 清理；30 秒超时保留 closing、task handles 和 OS 锁，内部退出放行标记避免 `app.exit()` 重入。
- LLM stream、ZIP、Truth Snapshot、图片处理和 `dotnet/ilspycmd` 接入统一 token。Windows 受控进程使用 Job Object，测试确认取消父进程后延迟子进程不会存活写入。
- Truth Snapshot 下载 request/body/retry、source/index hash 和反编译可取消；prepare 与 current pointer 激活分离，取消后保持旧 current。
- submit/closing barrier、成功排空后第二 opener、超时锁保持、打开前 reconciliation、cancel/success CAS、stalled HTTP、图片 blocking cleanup 和 handler 终态均有定向测试。

2026-08-01 开始并完成桌面运行时 Work Order 4 的自动实现与定向门禁：

- `BuildInfo { commit, variant, features, buildId }` 由 Core build script 按实际 Cargo feature 嵌入，health、capabilities 和图片处理 provenance 共用同一身份；baseline/ML 不一致和候选 `e2e` feature 会被拒绝。
- `ImageProcClient` 返回 PNG 与结构化 provenance；Simple、ML u2netp、ML→Simple fallback 均记录真实 processor，图片 ArtifactManifest schema v2 保存 model SHA、ONNX Runtime、fallback 原因和 BuildInfo，同时继续独立保存图片质量报告。
- ML prewarm 状态增加 attempt、Ready metadata 和结构化 failure；startup/retry 复用 singleflight，并发调用不会重复下载或初始化。
- `build.ps1 -Variant Baseline|Ml` 使用独立 target/bundle/release 目录，拒绝脏工作区和额外 feature 参数，并生成带 commit、variant、features、buildId、文件大小与 SHA-256 的 release manifest。
- Core no-feature/ML 图片测试、Artifact/asset manifest、Desktop prewarm singleflight、两种 feature 编译、TypeScript 和 PowerShell 隔离/哈希 fixture 已通过。完整 baseline/ML Tauri release bundle 已在提交 `a2570e26` 上构建，最终 GUI EXE 通过文件握手回报的 BuildInfo 与请求身份逐字段一致，四个 installer 的 manifest/release/bundle SHA-256 均可复算一致。

随后完成 baseline/ML NSIS 候选的安装版人工 E2E：

- baseline 与 ML 均完成安装、启动、隔离配置和运行时 BuildInfo 对照；baseline 可通过 UI 创建工程并正常退出。
- ML 从空 app-data 下载并校验 u2netp 与 ONNX Runtime 1.22.0，Health 显示就绪；UI 完成 Plan 和代码/图片生成。
- ML ArtifactManifest schema v2 记录 `processor=ml_u2netp`、正确 model SHA/runtime/BuildInfo 且无 fallback；图片质量 `accepted=true`。
- ArtifactManifest、RunRecord、Game Pack、Truth Snapshot、release manifest 和已发布文件 SHA-256 已逐项复算一致。
- 应用通过窗口正常退出，stub 停止，候选卸载器退出码 0，安装目录和卸载注册表项均已清理。

## 5. 当前缺口与未验证项

Game Pack Stage 1 已完成自动迁移和真实游戏复验。2026-08-02 首次观察确认 `ATSReleaseSmoke` 的图片与透明背景、Mod 本体、中文内容和游戏内功能正常；日志确认 BaseLib 266 patches、0 failed，以及 Mod DLL、PCK 和 initializer 加载成功。当时唯一可见问题是描述将模型生成的 `[yellow]1[/yellow]` 原样显示。

该问题已定位为资源约束缺口：STS2 当前富文本注册了 `blue` 等颜色标签，但没有 `yellow`；此前结构化 bundle 只校验 locale、键和空值。当前代码已由 Game Pack `allowed_rich_text_tags` 声明白名单，Prompt 使用同一声明，并在写文件和 compile gate 前确定性拒绝未知、属性式、未闭合或错配标签。`ATSReleaseSmoke` 已在隔离目录改为 `[blue]1[/blue]` 后重新 publish、打包并安装；安装 DLL/JSON/PCK 与 staging SHA-256 一致，PCK 静态扫描为 `[blue]` 2 处、`[yellow]` 0 处。用户通过 `relic add ATSRELEASESMOKE-RELEASE_SPARK` 获取遗物后确认富文本、中文内容和首回合能量行为全部正常，本次 Mod 候选的真实游戏复验通过。

Work Order 1-4 已完成，Work Order 5 的机器候选门禁已通过。当前剩余缺口是最终人工冒烟和少量不阻断发布正确性的桌面体验问题：

- 提交 `6e8d2919` 的最终候选 `rc-20260802T064241Z-6e8d291971e8` 已通过总入口 10/10 steps 和 2/2 variants；frontend、workspace check/test/clippy、baseline/ML Tauri build、BuildInfo、manifest 与四个 installer SHA-256 均通过机器复验，临时文件残留为 0。
- 当前新候选尚未执行人工安装/启动冒烟；旧候选的完整 baseline/ML 安装、prewarm、图片任务、退出和卸载证据不能冒充本次新候选的人工结果。
- 当前 installer 均为 `NotSigned`。本阶段只形成可复验的未签名候选，不把它描述为面向陌生用户的正式签名发行版；证书、SmartScreen 信誉和自动发布不纳入当前任务。
- 真实链曾两次在 Artifact staging 目录最终原子重命名处失败，并被丢失为 `asset_generate.publish / core.unclassified`；完整 staging 稍后可手动重命名，证明是 Windows 短暂 access/sharing conflict，而非产物或 manifest 错误。当前 Core 已保留原子目录发布，只对可识别的 Windows 瞬态重命名错误执行有界退避，失败后清理 staging 并使用结构化 `artifact.*` 错误。Artifact、failure、asset、codegen、batch、package 和 workspace 全量回归已随最终候选通过；安装版再次触发真实 Artifact 发布链仍属于人工未验证项，不复用人工恢复结论。
- 缺少 Truth Snapshot 时的图片任务错误仍偏通用；失败终态读取可能短暂显示无 payload；切换工程后 Single Asset 卡片可能残留旧 plan/error。它们不影响已落盘 Run/Artifact 事实，但需要单独的 UI 任务处理。
- 安装版 E2E 使用确定性本地 LLM/image stub 验证真实桌面和 ML 处理链，不等同外部模型提示词语义质量评测。

自动门禁结果只证明候选构建、测试和机器身份一致性；最终桌面安装/启动观察仍以用户人工结果为准。

## 6. 当前任务文档与执行队列

当前直接服务于 `rust` 分支收口的文档如下：

1. 本文：当前事实、可靠证据和剩余缺口入口。
2. [`当前方案`](../03-当前方案/当前方案.md)：当前阶段目标、稳定约束和专题方案总入口。
3. [`桌面后端运行时加固与发布收口方案`](../03-当前方案/2026-07-31-桌面后端运行时加固与发布收口方案.md)：Work Order 1-5 的完整设计与门禁。
4. [`通用 Mod 流水线与 Game Pack 边界`](../03-当前方案/通用Mod流水线与Game-Pack边界.md)：长期 Game Pack 架构边界。
5. [`Game Pack Stage 1 职责清单与迁移设计`](../03-当前方案/2026-07-30-Game-Pack-Stage-1职责清单与迁移设计.md)：已完成 Stage 1 的实现依据与验收结论。
6. [真实游戏加载与资产质量问题审查](../90-归档/历史审查与验证/Rust重写/已经完成/2026-07-28-真实游戏加载与资产质量问题审查.md)：最终真实游戏人工验收证据。
7. `.trellis/tasks/archive/2026-07/07-29-game-pack-stage1/`：Stage 1 PRD、状态与执行记录。
8. `.trellis/tasks/archive/2026-07/07-28-real-game-load-asset-quality/`：被 Stage 1 接管的原始问题任务记录。

2026-07-28 已逐项核对并归档全部 14 个 4 月旧任务：12 个按历史交付事实归档，规范初始化合并到统一清单 P1，未实现的旧统一弹窗方案合并到 P2 并标记为被 Rust 路线替代。整理任务归档后，Trellis `current task` 为 `none`，活跃目录不再保留这些旧任务。

## 7. 建议的后续顺序

Game Pack Stage 1 与桌面运行时 Work Order 1-4 已完成，Work Order 5 的机器范围也已通过。当前顺序为：

1. 用户安装并启动 `rc-20260802T064241Z-6e8d291971e8`，完成最终桌面冒烟；如继续生成 Mod，同时记录 Artifact 发布是否正常。
2. 人工结果通过后收口并归档当前 Trellis 任务。
3. 签名、SmartScreen、自动发布和单独记录的 UI 易用性问题进入后续任务，不混入本次候选结论。

历史队列保存在 [`2026-07-28-统一后续任务清单`](../90-归档/Rust重写过程材料/03-方案/2026-07-28-统一后续任务清单.md)，不再作为当前入口。
