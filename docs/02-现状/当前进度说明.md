# 当前进度说明

> 本文是 `rust` 分支当前能力、验证证据和执行队列的事实入口。
>
> 最后更新：2026-08-04

## 1. 当前结论

Game Pack Stage 2 基础架构与 STS2 Pack Mod 类型/资源 Orders 1-8 已全部完成。绑定当前代码的 Windows 候选 `rc-20260804T132520Z-e00f0fa8545e` 已通过完整候选门禁和安装态 E2E，用户于 2026-08-04 明确确认验收。当前开发队列已清空，可进入下一阶段范围讨论；`character`、动画、音频、3D 和跨 Item 引用仍未实现。

Order 1-8 和绑定提交的安装态验收均已收口：通用 Item/Resource 地基、Relic/Card/Potion/Power、Item Library 驱动的 Batch/Complex、真实 Build/Package、Run/Artifact 与工程锁恢复均有新证据。本轮未验收真实外部 Provider、AI 图片质量、真实 STS2 游戏行为、签名或 SmartScreen；push、rebase 和发布仍需单独授权。

## 2. 当前代码事实

| 范围 | 当前事实 |
| --- | --- |
| Kernel | 稳定 ID/schema/hash、Item/Item Type/field/locale ID、typed failure、BuildInfo 和工程模板合同；不认识 STS2 类型 |
| Runtime | RunRecord v3、ArtifactManifest v3、取消和 provider-neutral ports；无产品中心枚举、无游戏类型分支 |
| Game Context | pinned Pack v3、顶层 `itemTypes` 目录、Contribution Resolver、Truth Snapshot v2、Evidence Query 和 typed capability readiness |
| Workspace | ProjectFolder/OS lock/recents、`.ats/runs-v3`、ResourceAsset v2 candidate/selected、ItemDefinition v1，以及 `.ats/items-v1` 不可变版本/current pointer |
| Feature | 9 个通用 Feature；Plan/Single 统一消费 Pack 顶层类型目录，不再分别复制 Truth query 或 Resource role |
| Adapter | Settings、HTTP model/media、真实 PNG probe/resize/outline、Run/Truth/Resource/Artifact filesystem store、dotnet validation/build 和 ZIP package |
| Desktop/React | typed capability/Item/Resource commands、Run 前 readiness/resource gate、Item Library、descriptor-driven 通用字段编辑器、locale confirmed/outdated、Pack-driven Resource Workbench，以及可视化 Batch/Complex、只读 request preview、persisted child Runs 和精确失败重试；ProjectSession 持有唯一 Resource repository |
| Web/CLI | Web 只提供 health/catalog/SPA；CLI catalog 仍来自共享 9-Feature registry |

Pack v3 顶层 `itemTypes` 是 Mod 类型身份、受限通用字段、required locales、Evidence Queries 和 Resource roles 的唯一真源。`pack.mod-plan-guidance` v3 只提供规划指导；`pack.mod-generate-single` v4 分离通用生成 guidance 与每类型 guidance，并声明验证 Primitive 和输出文件角色。新增类型不应修改 Runtime 枚举，也不应在 React、Plan 和 Single 中再维护平行列表。

STS2 Pack 当前正式注册 `custom_code`、`relic`、`card`、`potion` 和 `power`。Power 的 canonical fields 为 `power_type/stack_type/instance_type/allow_negative`，必需英中 locale，readiness 查询 `CustomPowerModel/PowerModel/PowerType/PowerStackType/PowerInstanceType/PowerCmd`；192x192 RGBA `power.master` 确定性派生 48x48 icon 与 192x192 big，生成文件为 C# source 和英中 `powers.json`。当前 pinned Pack SHA-256 为 `5bb8158fdfd1f05c92af5cf5110e4a2ceed8ab73a086dfa876429b97756ecc03`。

ItemDefinition v1 保存稳定 `itemId/itemType`、canonical structured fields、behavior intent、本地化确认/过期状态和精确 Resource version binding。定义使用有序序列化计算 SHA-256；后续 Run/Artifact 必须绑定该 hash，不能从自然语言反推锁定字段。

Capability readiness 由 pinned Pack 与当前 verified Truth 确定性计算：未声明类型不出现；无当前 Truth 时所有已声明类型 blocked；部分 Evidence Query 不命中时只禁用受影响类型并返回 typed query-index blocker；ready 之前不得进入模型调用。

Item repository 使用 `.ats/items-v1/<itemId>/definitions/<definitionHash>.json` 保存不可变历史，并只原子更新 `current.json`。同一 `ProjectSession` 持有唯一 repository 同步门；即使指针更新前崩溃留下孤立 snapshot，stable itemId 也不能切换 itemType。Tauri capability wire 固定使用 camelCase，包括 `truth.evidence_missing.queryIndex`。

ResourceAsset 已破坏性升级为 schema v2。`pack.resource-specs` v2 声明 exact media shape 与 direct master/derived graph；STS2 的 512x512 `relic.master` 可确定性派生 128x128 normal、128x128 radius-4 outline 和 256x256 big。Upload、AI 与 Pack default 都先产生 `selectedVersion=null` 的 immutable candidates；显式 select 后 Single 才接受精确版本。PNG media type、解码、尺寸和 alpha channel 在落盘前由 Adapter 探测，批失败回滚 staging/final 并保留既有 selected。派生 provenance 绑定 source role/version、transform Primitive/version/parameters hash 与 pinned Pack identity。Resource Workbench 现在按 Pack role 显示 master/override、candidate preview、selected/bound 状态，并将显式选择写入 ItemDefinition。

Single Generate request 为 v3。Batch request 已破坏性升级为 v4、result 为 v2：输入只包含 `modId`、固定 `StoredItemDefinition`/`definitionHash`、确定性 `artifactId` 和 `failFast`，Feature 为每项持久化 Plan child Run 后复用 Single v3；父 Run `succeeded` 表示编排完成，Item 失败保留在 typed result 中。Complex request 为 v3、result 为 v2，直接复用同一 Batch，只在全部 Item 成功后继续 Build/Package。公开请求不再接受 caller-authored Plan、`selectedResources` 或可编辑 JSON。Single 在创建 Run 和调用模型前验证 definition hash、Ready mode、Plan identity、required roles、selected pointer 与 Pack media shape；Prompt Recipe v3、ModelRequestSnapshot、Artifact extension v2 和 provenance 均绑定精确 `definitionHash`。

## 3. Prompt 与所有权

```text
Feature Recipe
+ Pack Contribution / selected Item descriptor
+ bounded Truth Evidence
+ Selected Resources
+ Project Context
+ llm.custom_prompt
+ Typed Output Contract
= ModelRequestSnapshot v1
```

跨游戏任务语言属于 Feature Recipe；游戏类型/字段/资源/查询和指导属于 Pack；当前 API 事实属于 Truth；资源版本属于 Workspace；用户补充指令属于运行时设置。代码只保留 role、schema、slot、JSON、安全、截断、脱敏和 typed validation 等协议合同。

Plan result v2 的 `evidenceRequirements` 仍只描述业务证据意图。可执行 `symbols/terms` 查询来自 Pack v3 item descriptor；readiness 与 Single Generate 复用同一查询。Single 将所选类型的 `generatedFiles[].role` 编译为 run-scoped bundle v2 strict JSON Schema。

## 4. Order 2-8 机器证据

2026-08-04 当前 Order 2 工作树已通过：

```text
cargo test --workspace --all-targets
  117 passed
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo check -p agentthespire-desktop --features ml-rembg
cargo check -p agentthespire-desktop --features e2e
npm run test:frontend
  15 passed
npx tsc -b --pretty false
npm run build
node scripts/check-stage2-dependency-dag.mjs --self-test
node scripts/check-stage2-dependency-dag.mjs
cargo run -p ats-cli -- features
cargo fmt --all -- --check
git diff --check
```

这些证据覆盖不可变 Item 版本/current pointer、tamper/type/path 和 crash-orphan recovery、Pack Draft/Ready 校验、四种 Item-bearing Run request readiness、capability camelCase wire、Item Library 编辑模型、本地化过期/确认、真实 dotnet 组合测试和既有 Run/Artifact/lock 回归。Order 2 阶段当时未执行新 release candidate、安装器、真实 Provider 或真实游戏验收。

2026-08-04 Order 3 最新工作树重新通过同一完整门禁，其中 workspace 为 126 tests、frontend 为 15 tests。新增证据覆盖真实 PNG/RGB alpha 探测、malformed/media mismatch、master 四候选、重复 hash、精确 provenance、Pack Primitive/version drift、显式选择、Single 未选/错误尺寸拒绝、批失败保留既有 selected、无 staging、ProjectSession 单 Resource repository 和稳定 `resource.*` failure。Order 3 阶段当时未执行 Resource Workbench 人工体验、真实图片 Provider、完整 release candidate、安装器或真实游戏验收；这些不属于 Order 3 机器门禁。

2026-08-04 Order 4 工作树通过同一完整门禁，其中 workspace 为 126 tests、frontend 为 19 tests。新增证据覆盖 typed Resource catalog/list/preview/select、预览字节上限与 hash 复算、Workbench role completeness/binding/stale/shape 检测、definition-bound Single/Batch/Complex 输入、Relic definition -> selected normal/outline/big -> 真实 dotnet compile -> Artifact v3/hash/provenance -> succeeded Run v3，以及无 staging 残留。Prompt Recipe v3 SHA-256 为 `a387246f80806441de58a852f3cff3516103e1da2b9b43f8244657854f5a6210`。Order 4 阶段当时未执行真实图片 Provider、完整 release candidate、安装器、人工 Workbench 美术审查或真实游戏验收。

2026-08-04 Order 5 工作树通过完整门禁，其中 workspace 为 127 tests、frontend 为 19 tests。Card 针对性证据证明缺 `TargetType` Truth 或缺 `card.big` 绑定时模型调用为 0；完整 definition 通过真实 dotnet compile，发布 3 个文本文件和 2 个图片文件，Artifact/definition hash 可复算，Prompt 含 Card item guidance 且不含 Relic guidance，无 staging/transaction 残留。Order 5 阶段当时未执行完整 release candidate、安装器、真实 Provider 和真实游戏验收。

2026-08-04 Order 6 工作树通过完整门禁，其中 workspace 为 128 tests、frontend 为 19 tests。Potion 针对性证据证明缺 `PotionUsage` Truth 或缺 `potion.icon` 绑定时模型调用为 0；完整 definition 通过真实 dotnet compile，发布 C#、英中本地化和 icon 共 4 个文件，Artifact/definition hash 可复算，Prompt 只含 Potion item guidance，无 staging/transaction 残留。Pack 扩展同时暴露并修复了 `mod-plan` pretty catalog 超过旧 16k slot 的容量回归；槽位现有界提升为 32k，Recipe SHA-256 为 `efd695f87099d9c1bebc670e61885210641dbd3abd89d11099568fd8e8b5954c`，facade Plan 成功/typed failure 均已回归。Order 6 阶段当时未执行完整 release candidate、安装器、真实 Provider 和真实游戏验收。

2026-08-04 Order 7 工作树通过完整门禁，其中 workspace 为 129 tests、frontend 为 19 tests。Power 针对性证据证明缺 `PowerStackType` Truth 或缺 `power.big` 绑定时模型调用为 0；完整 definition 通过真实 dotnet compile，发布 C#、英中本地化、icon 和 big 共 5 个文件，Artifact/definition hash/provenance 可复算，Prompt 只含 Power item guidance，无 staging/transaction 残留。完整 built-in item catalog pretty JSON 为 20,931 字符，facade Plan 成功/typed failure 均通过，未突破 32k 槽位。Order 7 阶段当时未执行完整 release candidate、安装器、真实 Provider 和真实游戏验收。

2026-08-04 Order 8 工作树通过最终机器门禁：workspace 132 tests、frontend 23 tests、GUI E2E 5/5，以及 workspace check/clippy、desktop `ml-rembg`/`e2e` feature check、TypeScript/build、DAG self-test/real、CLI catalog、fmt/diff。四类型 Batch 组合测试生成 Relic/Card/Potion/Power，持久化 8 个 Plan/Single child Runs，执行 4 次真实 dotnet compile，复算 4 个 Artifact manifest/hash/definition provenance，且无 staging/transaction 残留。桌面隔离 E2E 另证明 Stage 1→2 Truth 导入、`custom_code` definition、Batch v4、Complex v3、真实 Build/Package、child Run 身份和工程本地工具链优先级。Order 8 阶段当时未执行新 release candidate、安装器、真实 Provider 或真实游戏行为验收。

## 5. 已验收候选

STS2 Pack Orders 1-8 当前最终候选：

```text
ID: rc-20260804T132520Z-e00f0fa8545e
commit: e00f0fa8545ef4113e6b14d932b980c03f548b33
verification: artifacts/release/rc-20260804T132520Z-e00f0fa8545e/release-verification.json
steps: 10/10 succeeded
variants: baseline + ml-rembg
installer size/SHA-256: 4/4 matched
candidate residue: 0
signature: 4/4 NotSigned
```

安装态 ML E2E 使用全新 Pack v3 Truth snapshot `f379cdf78b586f3eb032f01f6d506da1c5f8bcee71ba261db350a001a19db818` 和 `custom_code` ItemDefinition `installed-rc-e00f0fa8-20260804-2253`，definition hash 为 `6164b28f12292d99a6d2bfad2ee8ea13c6a60c075cc331a182472eec2aa00baa`。Batch v4 的 parent/Plan/Single 和 Complex v3 的 parent/Batch/Plan/Single/Build/Package 共 9 个 RunRecord v3 全部 `succeeded`。Batch manifest SHA-256 为 `05e50988ec2db788fb13dc1d9290ef752d634f5e122f3afb056165bc45f2a363`；Package manifest SHA-256 为 `49ffa93ecae8f58f8566859d7c127167edac4911f2bec622ab9a23ce90c74036`，894,049 字节 ZIP SHA-256 为 `868a539bbe5146396c870b8dd4fb53708326ffcbbe035afa45a52c8fdc599133`，精确包含 6 个 BaseLib/ATSReleaseSmoke DLL/PCK/JSON。本轮 Artifact staging 和 transaction children 为 0，应用正常退出后同一工程锁可重新获取并释放。

用户已明确确认上述安装态验收。证据不包含真实外部 Provider、AI 图片质量或真实 STS2 游戏行为；四个安装器仍未签名。

上一个 Stage 2 架构候选完整保留：

```text
ID: rc-20260803T125802Z-bde2d64969d9
commit: bde2d64969d9b8d0b41357dfc3bb36932e9b1056
verification: artifacts/release/rc-20260803T125802Z-bde2d64969d9/release-verification.json
steps: 10/10 succeeded
variants: baseline + ml-rembg
installer hash: 4/4 matched
signature: 4/4 NotSigned
```

安装态自然语言 `custom_code` E2E 已证明真实 Plan/Single、dotnet validation、succeeded Run v3、Artifact v3/hash、无本轮 staging、正常退出和工程锁恢复。该候选只证明绑定提交，不能代表当前 Pack v3 未提交代码，也不能被重命名或复用为本轮验收。

所有新旧 Truth、Run、Artifact、release、`.tmp` 和真实验证证据必须保留；任何后续 Pack identity 变更仍要导入匹配的新 Truth，并创建全新 Item/Run/Artifact，不得复用本轮结果代表新代码。

## 6. 当前 Trellis 队列

父任务、8 个顺序 Orders 和当前最终候选/安装态验收任务均已完成并归档到 `.trellis/tasks/archive/2026-08/`。

顺序 Orders：

1. Pack/Item contracts、ItemDefinition/hash、Truth readiness（已完成）
2. Item repository、Item Library、通用编辑器、本地化（已完成）
3. Resource Prepare v2、真实媒体探测、master/derived、候选版本（已完成）
4. Resource Workbench + Relic Gate（已完成）
5. Card（已完成）
6. Potion（已完成）
7. Power（已完成）
8. 可视化 Batch/Complex、Build/Package 与全量机器门禁（已完成）

本轮开发和安装态验收队列已清空。下一步是讨论后续 Pack 类型/资源能力，或在单独授权下验收真实 Provider、图片质量和真实游戏行为；push、rebase、签名和发布仍是独立授权门禁。
