# 当前方案

> 文档定位：本文是项目当前方案总入口，负责说明阶段目标、稳定约束、总体路线和完成标准，并索引仍指导当前实现的专题方案；不承载具体任务状态。
>
> 事实依据：当前 Rust/Tauri 代码、最新桌面端验证事实，以及历史方案和 ADR 中仍然有效的结论。
>
> 权威入口：当前能力见 `docs/02-现状/当前进度说明.md`；具体任务见 `.trellis/tasks/`。
>
> 最后更新：2026-08-01

## 1. 当前目标

当前阶段目标是完成 **Rust/Tauri 桌面端 MVP 的真实可交付收口**：用户能够从自然语言需求出发，生成 STS2 Mod 的代码、图片和本地化资源，并完成验证、构建、打包与真实游戏验收。

当前优先级是提高已有主链的正确性、可解释性和交付质量，不继续扩展 Web/SaaS 功能面。

### 当前专题方案

- [`2026-07-31-桌面后端运行时加固与发布收口方案.md`](./2026-07-31-桌面后端运行时加固与发布收口方案.md)：当前待执行任务，收口错误契约、工程会话、Run、Artifact Store、安装版和发布门禁。
- [`通用Mod流水线与Game-Pack边界.md`](./通用Mod流水线与Game-Pack边界.md)：长期稳定架构边界，一条通用流水线由多个 Game Pack 提供游戏声明和资源。
- [`2026-07-30-Game-Pack-Stage-1职责清单与迁移设计.md`](./2026-07-30-Game-Pack-Stage-1职责清单与迁移设计.md)：已完成的 Stage 1 破坏性迁移设计与验收结论，继续作为当前 Game Pack 实现依据。

## 2. 产品范围

### 纳入当前阶段

- 桌面端工程创建、打开、锁定和历史记录。
- 单资产、批量、自定义代码、构建、打包、日志分析和知识刷新任务。
- Anthropic 与 OpenAI-compatible LLM、OpenAI Images-compatible 图像生成。
- STS2/BaseLib 知识刷新、事实检索和生成上下文组装。
- 图片处理、compile gate、文件事务、Run history、ArtifactManifest、diagnostics 和可行动错误提示。
- baseline 与可选 `ml-rembg` 桌面构建及真实安装运行验证。
- 真实游戏中的行为、视觉、文本、manifest 和稳定性验收。

### 当前不纳入

- Web auth、admin、users、tokens 和平台额度体系。
- SQLx/PostgreSQL、migrations 和平台级任务队列。
- new-api 管理集成、自动更新、签名公证。
- 未有明确用户和测试环境的 Mac/Linux 发布验证。
- 仅为兼容旧 Python/Web 实现而保留的双口径。

这些方向只有在用户重新启动相应阶段后，才通过新的 Trellis 任务研究和确认。

## 3. 稳定架构约束

### 3.1 Core 与双壳

- `ats-core` 承载领域逻辑，不依赖 `axum` 或 `tauri`。
- `src-tauri` 是桌面壳，通过 Tauri IPC 调用 core，不启动本地 HTTP 服务。
- `ats-web` 是轻量 HTTP 壳，当前只装配 health、knowledge、planning、codegen、LLM 和静态资源。
- `ats-cli` 提供构建、部署、日志、停止和模板命令，不复制 core 业务逻辑。

### 3.2 共享前端

- React/TypeScript 前端由桌面和 Web 共用。
- `src/services/tauriApi.ts` 是 API 签名权威，`webApi.ts` 必须在编译期满足同一导出形状。
- `src/services/api.ts` 只负责根据 `__IS_TAURI__` 选择实现，页面不直接分叉业务逻辑。
- 路由使用 `HashRouter`，同时适配 Tauri webview 和静态 Web 部署。

### 3.3 工程与任务数据

- 桌面端项目采用自包含工程文件夹，不依赖数据库。
- `.ats/`、`items/`、`artifacts/`、`history/`、`Generated/` 和项目元数据共同形成可追溯记录。
- 文件更新优先使用临时文件加原子替换；生成或 compile gate 失败时不得留下半成品正式资源。
- 同一工程使用操作系统文件锁防止多进程并发写入，常驻诊断文件不等同于持锁状态。

### 3.4 启动与失败边界

- Tauri 启动期的 ML、缓存或索引预热必须后台执行，并通过状态接口暴露进度；不得阻塞窗口显示。
- Web 端先完成配置和路由装配，再绑定端口，并通过 Ctrl+C/SIGTERM 优雅停机。
- 非致命外部能力失败应降级并给出可行动提示；配置、鉴权、网络、工具链和写盘错误必须可区分。

### 3.5 通用流水线与 Game Pack

长期边界采用“一条通用 Mod 生产流水线 + 多个 Game Pack”：

- 引擎负责资源获取、索引、生成、验证、构建、打包、事务和任务追踪的通用机制。
- Game Pack 声明游戏真相源、工程骨架、资源规格、稳定验证规则、构建步骤和交付布局。
- 不为每个游戏复制一套 handler，也不把 STS2 的 C#、Godot、BaseLib 或 hook 写入通用领域接口。
- 行为代码优先检索当前版本真相源；历史 Markdown 示例不是跨版本权威答案。
- 当前只有 STS2 一个完整样本，不提前设计过度通用的 Game Pack schema。

## 4. 当前推进路线

具体工作包由 Trellis 串行管理，项目层只固定以下顺序：

1. Game Pack Stage 1 与真实游戏行为、图片、本地化、manifest 和稳定性验收已经完成。
2. RunRecord v2、ArtifactManifest、结构化失败契约和 ProjectSession cancel-and-drain 已完成。
3. baseline/ML 隔离构建、BuildInfo、图片处理 provenance 和 prewarm singleflight 已实现；下一门禁是经授权构建 installer 并人工验证启动和首次网络 prewarm。
4. 在阻断项清零后形成发布候选，执行经用户确认的全量测试和构建。
5. 发布收口后补全与当前 Rust/Tauri/React 实现一致的开发规范。

任务名称、负责人、状态、证据和逐项验收只记录在 `.trellis/tasks/<task>/prd.md`，不再复制到本文。

## 5. 完成标准

桌面 MVP 收口至少满足：

- 真实 single asset 主链能生成可编译、可打包、可在游戏中加载的产物。
- 行为、图片、本地化和 manifest 在真实游戏验收中达到可接受质量。
- 配置、鉴权、网络、知识、工具链和文件错误能指导用户修复或重试。
- baseline 安装版可完成基本工程和任务流程；ML 版本的准备、成功与降级行为明确。
- 双进程不能同时写入同一工程，退出后锁能正常恢复。
- 发布候选通过约定的 Rust、前端、Tauri 和关键 E2E 验证。
- 总览、现状、当前方案和实际代码保持一致。

## 6. 方案维护规则

- 任务执行不直接向本文追加流水账。
- 阶段范围、稳定架构边界或完成标准变化时才更新本文。
- 已失效的方案原文进入 `90-归档/`；仍指导当前实现的专题方案可并列保留，但必须由本文索引且不得形成互相冲突的口径。
- 如果方案变化需要大范围重构、公开接口变化或重要依赖调整，先通过 Trellis PRD 取得用户确认。
