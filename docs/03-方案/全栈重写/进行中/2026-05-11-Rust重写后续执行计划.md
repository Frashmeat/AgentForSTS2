# 2026-05-11 — Rust 重写后续执行计划

| 项 | 值 |
| --- | --- |
| 分支 | `rust` |
| 起始计划 | [2026-05-11-Rust+Tauri全栈重写计划.md](./2026-05-11-Rust+Tauri全栈重写计划.md)（战略 + 架构 ADR） |
| 本文档作用 | 战术执行计划 —— 当前进度快照 + 剩余工作的可执行拆解 |
| 维护节奏 | 每完成一个阶段更新进度表；新决策 / 未决问题修订对应 § |

---

## 1. 当前进度快照

### 1.1 已完成（截至 stage 3.5 第二轮完成）

| 阶段 | 状态 | commit | 单测 |
| --- | --- | --- | --- |
| 0 — 骨架 + 设计文档 | ✅ | `8c8bf3c` | — |
| 1 — config 加载 + walk-up 路径 | ✅ | `6628f26` + `7ee0b57` | — |
| 2.1 — knowledge 静态部分 | ✅ | `0600b80` | — |
| 2.3 — planning domain + validation | ✅ | `334f070` | 10 |
| 2.3.2 — planning execution_bundles | ✅ | `204ca72` | +6 |
| 2.3.5 — prompting + knowledge contracts | ✅ | `1160eaf` | +11 |
| 2.3.6 — sts2 providers + resolver | ✅ | `750bea7` | +12 |
| 2.4 — codegen prompt_assembler | ✅ | `7aaeb3a` | +7 |
| 2.4.1 — codegen 两壳接口 + 前端 Card | ✅ | `a120309` | — |
| 4 — LLM 客户端 + 流式前端 | ✅ | `d626f89` + `c983d98` | +9 |
| 3.1b — 桌面端工程文件夹存储层 | ✅ | `e8aee5c` | +12 |
| 3.2-3.4 — 桌面端 Platform Job 全链路 | ✅ | `adf33cf` | +8 |
| 3.5 第一轮 — code_generate + build_project | ✅ | `1b8e2bf` | +6 |
| 3.5 重构 — handlers/ 子目录拆分（零行为变更） | ✅ | 本轮 | +2 |
| 3.5 第二轮 — log_analysis / package_project / batch_custom_code / single_asset_plan | ✅ | `519aa4f` | +23 |
| 2.2.1 — ilspycmd 子进程 + manifest + KnowledgeRefreshHandler | ✅ | 本轮 | +17 |

合计：123 个 cargo unit test 全过；Web 路由 + Tauri command 双壳贯穿
（platform: 8 job submit kind / 2 query / 1 cancel；codegen 6 prompt 端点；
knowledge/planning/llm/project 各模块齐全）。

### 1.2 已落地的代码资产

```
ats-core/
  config        figment 三层加载 + role 校验
  errors        thiserror CoreError
  health        Role / HealthReport / ConfigStatus 注入
  knowledge/
    contracts                 KnowledgeQuery/Fact/Guidance/Lookup/Packet
    models                    KnowledgeStatus/Game/Baselib/SourceMode/OverallState
    paths                     KnowledgePaths::from_runtime_dir
    runtime                   ensure_dirs / get_status / has_cs_files
    templates                 8 个 sts2 模板 include_str! 内嵌
    sts2_guidance             guidance_for_asset_type / planner_guidance
    sts2_guidance_provider    KnowledgeGuidanceItem 装配
    sts2_lookup_provider      KnowledgeLookupItem 装配
    sts2_code_facts_provider  stub（stage 2.2 待补）
    sts2_knowledge_resolver   3 provider 协调
  planning/
    models                    PlanItem / ModPlan / AssetItemType
    dependency_graph          topological_sort / find_groups
    validation                4 级 strictness + 5 类 issue
    parse                     parse_plan (JSON → ModPlan)
    execution_bundles         find_groups → bundle 推断 + risk 评估
  prompting/
    assembler                 PromptContextAssembler (5 段渲染)
    loader                    PromptLoader 含 bundle 解析 + 变量替换
    （6 个 built-in prompts 内嵌）
  codegen/
    models                    AssetCodegenRequest / CustomCodegenRequest /
                              AssetGroupRequest+Item / ModProjectRequest
    prompt_assembler          6 个 assemble_*_prompt 方法
  platform/
    contracts                 7 个 Submit*Request DTO + SubmitJobAck
    domain                    Job / JobKind(8) / JobStatus / JobRepository trait / errors
    infra/file_job_repository file 存储（原子 rename）+ list/get/update
    application/
      job_application_service submit_text/code/build/log/package/batch/single + cancel
      handlers/
        common                ProgressEvent / ProgressSink / 状态迁移 helper
        text_generate         LLM 流式 → result.content
        code_generate         PromptAssembler + LLM → 解 fence → 写 .cs
                              + generate_and_write_code_artifact (pub(crate) 复用)
        build_project         dotnet publish 子进程 + 启发 success 判定
        log_analysis          build log → LLM 出诊断 markdown
        package_project       walkdir + zip crate → release tarball
        batch_custom_code     多 item 调度（fail_fast / 部分成功）
        single_asset_plan     LLM JSON → PlanItem（fence / prose 容错）

ats-web/
  main                        axum 路由 + config 装配
  routes/
    knowledge                 GET status / POST check
    planning                  POST validate / execution-plan
    codegen                   6 个 prompt 预览端点
  static_files                rust-embed dist/ 内嵌

src-tauri/
  lib                         tauri::Builder + AppConfig.manage
  commands/
    health / knowledge / planning / codegen   平行实现

src/
  App.tsx                     主页（4 card）
  components/
    HealthCard / KnowledgeCard / PlanningCard / CodegenCard
  services/
    api / tauriApi / webApi   双适配链路
```

### 1.3 已落地的脚本与配置

- `rust/dev.ps1` / `dev-web.ps1` / `build.ps1` / `build-web.ps1` —— Tauri 桌面 / Web 服务器 / 桌面打包 / Web 二进制（含 `-Docker`）
- `rust/Dockerfile` —— 三阶段（node → rust:alpine → alpine 运行时）
- `rust/runtime/agentthespire.config.example.json` —— 配置模板
- `rust/src-tauri/icons/` —— 全平台图标（含 Win .ico / macOS .icns / iOS / Android）
- `rust/.gitignore` / `.dockerignore` —— 排除规则

---

## 2. 剩余工作拆解

### 2.1 概览（按依赖序）

```
┌─────────────────────────────────────────────────────────────┐
│                     Q1 / Q3 决策门                            │
│       桌面端 DB？ + 远端 workstation 是否保留？                  │
└─────────────────────────────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 4: LLM 客户端          │  ← 并行可启动
        │  （Anthropic + OpenAI）       │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 3: Platform DDD       │  ← 依赖 Q1/Q3 + DB 决策
        │  （DB / Repo / Service /     │
        │   Runner / Routes）           │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 2.2: Knowledge 动态    │  ← 依赖 job 状态机（platform 内）
        │  + Image-proc / 其它小模块     │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 5: 装配收口            │
        │  Stage 6: 前端业务页（并行）   │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 7: Deploy CLI         │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  Stage 8: 切换 main          │
        └──────────────────────────────┘
```

---

### 2.2 Stage 4 — LLM 客户端（推荐立刻启动）

**目标**：让 codegen / planner 真正能调 LLM 出代码，不再只能预览 prompt。

**估时**：1–2 周单人全职。

#### 2.2.1 子任务

| 子任务 | 文件 | 估计 LOC | 备注 |
| --- | --- | --- | --- |
| Anthropic Messages API 客户端 | `ats-core/src/llm/anthropic.rs` | ~200 | reqwest + serde；非流式先 |
| Anthropic 流式 SSE | 同上 | +150 | `text-delta` 事件解析 + tokio mpsc |
| OpenAI Chat Completions | `ats-core/src/llm/openai.rs` | ~150 | 非流式 |
| OpenAI 流式 | 同上 | +100 | |
| LLM trait 抽象 | `ats-core/src/llm/client.rs` | ~80 | `async trait LlmClient { complete / stream }` |
| 重试 / 速率限制 | `ats-core/src/llm/retry.rs` | ~80 | 指数退避，429/5xx 重试 |
| Token 计数（可选） | `ats-core/src/llm/usage.rs` | ~40 | tiktoken-rs 或自己估算 |
| 配置接入 | 扩展 `config::LlmConfig` | +30 | endpoint / model / api_key 取自 Settings |
| 集成测试（mock 服务器） | `ats-core/tests/llm_test.rs` | ~150 | wiremock 模拟 Anthropic 响应 |
| 真实 LLM 冒烟（手动） | — | — | 用本地 API key 跑一次确认 |
| ats-web 路由 `/api/llm/complete` | `ats-web/src/routes/llm.rs` | ~80 | 调试用，前端 LlmCard 展示 |
| src-tauri command | `src-tauri/src/commands/llm.rs` | ~40 | 同上 |
| 前端 LlmCard 演示 | `src/components/LlmCard.tsx` | ~100 | 简单输入框 + 流式渲染 |

#### 2.2.2 关键设计点

- **Trait 形状**：
  ```rust
  #[async_trait]
  pub trait LlmClient: Send + Sync {
      async fn complete(&self, req: CompletionRequest) -> Result<CompletionResponse, LlmError>;
      fn stream(&self, req: CompletionRequest)
          -> Pin<Box<dyn Stream<Item = Result<StreamEvent, LlmError>> + Send>>;
  }
  ```
- **流式选型**：reqwest 0.12 的 `bytes_stream()` → 自己解 SSE。考虑直接用 `eventsource-stream` crate 省事。
- **超时**：硬上限 5 分钟；流式期间无新 chunk 60s 视为超时。
- **错误分型**：thiserror `LlmError { Http, Auth, RateLimit { retry_after_secs }, Stream, Parse, Cancelled }`，传到前端转 string。

#### 2.2.3 验收

- [ ] 单测：mock 服务器响应 200 / 401 / 429 / 5xx 全路径
- [ ] 单测：流式分包重组（含 UTF-8 字节边界、ping、error 事件）
- [ ] 集成测试：完整跑通 Anthropic + OpenAI 至少各一次（手动，用 .env 注入 key）
- [ ] 端到端：前端 LlmCard 输入 prompt → 看到流式 tokens 累积
- [ ] cargo test 全过、cargo clippy 无 warning

---

### 2.3 Stage 3 — Platform 模块（最大）

**估时**：4–8 周（单人全职），按子阶段拆。

**前置决策**：
- **Q1**：桌面端是否需要数据库？**已决：B — 工程文件夹模式**（无 DB，详见 §2.3.0）
- **Q3**：远端 workstation HTTP 模式是否保留？倾向 **B**：保留，`ats-web --mode workstation` 启动只跑 worker。

桌面端走文件夹后，Stage 3 实际上拆成**两条并行轨道**：
- **Web 轨**：sqlx + Postgres，所有 repo 走 SQL（Stage 3.1a / 3.3a / 3.4a-Web 部分）
- **Desktop 轨**：filesystem repo，按工程文件夹组织（Stage 3.1b / 3.3b / 3.4a-Desktop 部分）
- 两轨共享 contracts / domain / repository trait / services 上层（Stage 3.2 / 3.4 / 3.5）

#### 2.3.0 工程文件夹布局规范

**单个项目目录**（用户在桌面 GUI 中"打开"或"新建"的单位）：
```
<user-chosen-path>/<project_name>/
├── project.json                    # 项目元数据：name / created_at / sts2_path / template_version
├── plan.json                       # 当前 ModPlan（domain::ModPlan 序列化）
├── plan_validation.json            # 最近一次 validate_plan 结果（可选）
├── execution_plan.json             # 最近一次 build_execution_plan 结果（可选）
├── items/                          # 每个 PlanItem 的详细状态
│   ├── <item_id>.json              #   item 配置 + 用户标注 + clarification 状态
│   └── <item_id>.image_paths.json  #   关联图片路径列表
├── artifacts/                      # 生成产物（C# / 图片 / 配置）
│   ├── <item_id>/
│   │   ├── <ClassName>.cs
│   │   ├── <ClassName>.png
│   │   └── localization/...
│   └── _shared/                    # MainFile.cs / .csproj / nuget.config 等
├── history/                        # 任务历史（按时间倒序）
│   ├── 2026-05-11_15-23-text-generate-<item_id>.json
│   ├── 2026-05-11_15-23-text-generate-<item_id>.log
│   └── ...
└── .ats/                           # 内部状态（用户不应直接编辑）
    ├── lock                        # 排他锁，防止多实例同时打开
    └── version                     # 工程文件夹 schema 版本
```

**App 级别全局状态**（跨项目共享，落在 OS app data dir）：
```
%APPDATA%/AgentTheSpire/   (Windows)
~/Library/Application Support/AgentTheSpire/   (macOS)
~/.local/share/AgentTheSpire/   (Linux)
├── config.json                     # 活动配置（runtime / llm / image_gen / paths）
├── credentials.json                # LLM API key 等敏感凭据（OS keyring 优先）
├── recent_projects.json            # 最近打开的 N 个项目路径
├── knowledge/                      # 反编译产物（跨项目共享，避免每项目重复占盘）
│   ├── game/...
│   ├── baselib/BaseLib.decompiled.cs
│   └── knowledge-manifest.json
└── logs/                           # 全局应用日志
```

**设计取向**：
- 项目目录**自包含可移植**：整体 zip / git / 复制粘贴都能跑
- App 数据**仅缓存与凭据**，丢失不影响项目本身
- 没有 schema 迁移压力——`.ats/version` 用纯增量演化（旧字段保留，新代码读时按字段缺失处理）
- 不存放二进制/大文件以外的内容到 `~/.ats`，避免被备份工具误拖

#### 2.3.1a Stage 3.1a — Web DB 层（1 周，Web 轨）

| 子任务 | 文件 | 备注 |
| --- | --- | --- |
| sqlx 连接池 | `ats-core/src/db/pool.rs` | PgPoolOptions，配置 max_connections / acquire_timeout |
| migrations 重写 | `ats-core/migrations/2026XXXX_*.sql` | 从现 Python alembic 全量 schema dump 出 .sql，**不**还原历史 |
| `cargo sqlx prepare` 离线缓存 | `.sqlx/` | 提交到仓库，CI 校验 |
| 配置接入 | 扩展 `config::Settings` 加 `database.url` etc. | |
| 启动检测 | `ats-web::main` 启动期 PingDB | 失败转 `degraded` |
| 单测基础设施 | `ats-core/tests/db_helpers.rs` | sqlx-test 或 testcontainers |

**验收**：`cargo run -p ats-web` 启动连上本地 PG，迁移自动执行，`/api/health` 报告 db_ready=true。

#### 2.3.1b Stage 3.1b — Desktop 工程文件夹存储层（4–5 天，Desktop 轨）

| 子任务 | 文件 | 备注 |
| --- | --- | --- |
| ProjectFolder 抽象 | `ats-core/src/project/folder.rs` | open / create / lock 管理 |
| 工程目录 schema 读写 | `ats-core/src/project/schema.rs` | project.json / plan.json / items/*.json |
| `.ats/lock` 排他锁 | 同上 | 防多实例；进程退出释放（fs2 crate 或自己写） |
| `.ats/version` 演化策略 | `ats-core/src/project/version.rs` | 缺失字段视为默认值；不做破坏性迁移 |
| AppDataPaths 解析 | `ats-core/src/app_data.rs` | 用 `dirs` crate 定位 %APPDATA% / Library / .local |
| recent_projects.json 读写 | `ats-core/src/app_data/recent.rs` | LRU 含 5–10 条 |
| credentials.json | `ats-core/src/app_data/credentials.rs` | OS keyring 优先（keyring crate），fallback 加密 JSON |
| 单测 | 用 tempfile 起隔离工程目录 | |

**验收**：Tauri 端启动 → 用户"新建项目"或"打开已有项目" → ProjectFolder 加载/创建成功 → `.ats/lock` 写入 → health 报告 active_project=<path>。

#### 2.3.1 Stage 3.1 — 共享 db/project 装配（1 天）

把上面两个轨道挂到双壳：
- `ats-web` 启动期 ping DB（必需）
- `src-tauri` 启动期不要求项目目录（用户进 GUI 后再选），但 AppData 必须就绪
- 配置 schema 扩展：`Settings.web.database` / `Settings.desktop.app_data_dir`

#### 2.3.2 Stage 3.2 — Domain + Contracts（3–5 天）

Python `platform/contracts/` + `platform/domain/` 1:1 移植：

| 模块 | 文件 | 关键类型 |
| --- | --- | --- |
| contracts/job_commands | `ats-core/src/platform/contracts/job.rs` | JobCreated / JobCompleted / etc. |
| contracts/job_queries | 同上 | JobSummary / JobDetail |
| contracts/admin_commands | `ats-core/src/platform/contracts/admin.rs` | 各类 admin 操作 |
| contracts/admin_queries | 同上 | |
| contracts/runner_contracts | `ats-core/src/platform/contracts/runner.rs` | 长任务输入输出 |
| contracts/server_execution | 同上 | |
| domain/models | `ats-core/src/platform/domain/models.rs` | Job / Artifact / ExecutionProfile / ServerCredential |
| domain/execution_compatibility | `ats-core/src/platform/domain/compatibility.rs` | |
| domain/repositories（trait） | `ats-core/src/platform/domain/repositories/*.rs` | 7 个 repository trait |

#### 2.3.3 Stage 3.3 — Repositories 实现（双轨，2–2.5 周）

Repository trait 在 ats-core 中定义，sqlx 实现在 ats-web（Web 轨），filesystem
实现在 src-tauri 或 ats-core::project（Desktop 轨）。两轨并发开发，trait 设计保持
最小重叠（只暴露双端真正都用的方法）。

##### 2.3.3a — Web 轨（sqlx 实现，1–1.5 周）

每个 sqlx 实现含 `query!` / `query_as!` 宏 + 集成测试：

| Repository | 表 | LOC 估计 | 桌面有对等吗？ |
| --- | --- | --- | --- |
| JobRepository | platform_jobs / platform_job_subtasks | ~300 | ✅ 文件版 |
| AiExecutionRepository | ai_executions | ~150 | ✅ 文件版 |
| ArtifactRepository | artifacts | ~150 | ✅ 文件版 |
| ExecutionRoutingRepository | execution_routing | ~200 | ❌ Web 独占 |
| ServerCredentialAdminRepository | server_credentials | ~250 | ❌ Web 独占（桌面密钥走 credentials.json + keyring） |
| UploadedAssetRepository | uploaded_assets | ~150 | ❌ Web 独占（桌面文件由用户从磁盘选） |
| ServerExecutionRepository | server_executions | ~200 | ❌ Web 独占 |

每个配 2–4 个集成测试（CRUD + 边界）。

##### 2.3.3b — Desktop 轨（文件 repo 实现，3–5 天）

| Repository | 存储位置 | LOC 估计 |
| --- | --- | --- |
| FileJobRepository | `<project>/history/*.json` | ~200 |
| FileAiExecutionRepository | `<project>/history/<task>.json` 内嵌 | ~80 |
| FileArtifactRepository | `<project>/artifacts/<item_id>/*` | ~150 |
| FilePlanRepository | `<project>/plan.json` + `items/*.json` | ~120 |
| FileProjectMetaRepository | `<project>/project.json` | ~50 |

设计取向：
- 写入用 `tempfile + rename` 模式保证原子性（防 crash 时写一半）
- 列出操作用 `glob` / `read_dir`，加 LRU 缓存避免遍历开销
- 并发：单进程内用 `tokio::sync::RwLock` 包整个项目，跨进程靠 `.ats/lock`

#### 2.3.4 Stage 3.4 — Application Services（1–2 周）

镜像 Python `platform/application/services/`：

| Service | 职责 | LOC 估计 |
| --- | --- | --- |
| JobApplicationService | 任务提交 / 状态查询 / 取消 / 重试 | ~400 |
| ExecutionOrchestratorService | 调度长任务到 runner，分流 web/workstation | ~300 |
| ServerCredentialAdminService | admin 端凭据 CRUD + 健康检查 | ~250 |
| PlanArtifactService | 计划产物存储 | ~150 |
| PlatformFileStorageService | 文件存储抽象（本地 / 未来 S3） | ~120 |
| ServerWorkspaceService | 工作目录管理 | ~100 |
| UserCenterService | 用户视角任务历史 / refund summary | ~200 |
| RuntimeAuditService | 运行时审计日志 | ~150 |
| ServerQueuedJobWorkerService | 后台 worker 拉队列 | ~250 |
| ServerQueuedJobScanClaimService | 任务领取锁 | ~120 |

#### 2.3.5 Stage 3.5 — Runner Handlers（1–2 周，桌面端已完成 7/8）

镜像 Python `platform/runner/`，每个 handler 是一个长任务实现：

| Handler | 输入 | 输出 | 状态 |
| --- | --- | --- | --- |
| TextGenerateHandler | LLM prompt | text | ✅ 已落地 |
| CodeGenerateHandler | codegen request | C# 文件 | ✅ 已落地 |
| AssetGenerateHandler | asset request | C# + 图片 | ⏳ 依赖 image_gen 客户端 |
| BatchCustomCodeHandler | 批量 custom code | 多文件 | ✅ 已落地（fail_fast / 部分成功） |
| BuildProjectHandler | project root | dotnet publish 结果 | ✅ 已落地 |
| PackageProjectHandler | source_dir | zip artifact | ✅ 已落地（zip crate + walkdir） |
| SingleAssetPlanHandler | requirements | 单 item plan | ✅ 已落地（fence/prose 容错） |
| LogAnalysisHandler | build log | 诊断报告 | ✅ 已落地（log_path 或 inline text） |

**关键**：handler 需要进度回调。设计：
```rust
#[async_trait]
pub trait Handler {
    async fn execute(&self, input: Input, progress: ProgressSink) -> Result<Output, HandlerError>;
}
pub trait ProgressSink: Send + Sync {
    fn emit(&self, event: ProgressEvent);
}
```
Web 端 ProgressSink 推 WebSocket，Tauri 端推 `app_handle.emit`。

#### 2.3.6 Stage 3.6 — Routes / Commands（3–5 天）

把 `routers/platform_admin.py` 和 `platform_jobs.py` 拆成 web + tauri 双实现，~25 端点。

#### 2.3.7 Stage 3 验收

- [ ] 全部 sqlx repo（Web）+ file repo（Desktop）+ 10 service + 8 handler 单测覆盖
- [ ] 端到端 Web：前端登录 → 提交 text_generate 任务 → axum 路由 → service → handler → 进度通过 WS 推回 → 任务记录写入 platform_jobs
- [ ] 端到端 Desktop：Tauri 启动 → 新建/打开工程文件夹 → 提交 text_generate 任务 → IPC 路由 → service → handler → 进度通过 emit 推回 → 任务记录写入 `<project>/history/`
- [ ] cargo sqlx prepare 缓存提交，CI 跑 prepare 校验
- [ ] Web 的 PG schema 与现 Python 一致，原数据库可直连测试
- [ ] Desktop 的工程文件夹整体 zip → 复制到另一台机器 → 打开 → 状态完整恢复

---

### 2.4 Stage 2.2 — Knowledge 动态部分（1 周，依赖 stage 3 job 状态机）

| 子任务 | 状态 | 备注 |
| --- | --- | --- |
| ilspycmd 子进程发现 | ✅ stage 2.2.1 | discover_ilspycmd_in 纯函数 + discover_ilspycmd（PATH + ~/.dotnet/tools） |
| sts2.dll 反编译调度 | ✅ stage 2.2.1 | std::process + spawn_blocking + .cs 文件统计 |
| knowledge_manifest.json 读写 | ✅ stage 2.2.1 | schema_v1 + atomic rename + 缓存命中比较（path/size/mtime） |
| KnowledgeRefreshHandler | ✅ stage 2.2.1 | 套现有 Job 框架；force / 缓存命中早退 |
| BaseLib GitHub Releases API | ⏳ 下一轮 | reqwest 取最新 release / 下载 tarball |
| BaseLib 解压 + 反编译 | ⏳ 下一轮 | tar + ilspycmd |
| 知识包 ZIP 导出 | ⏳ 下一轮 | 复用 package_project 的 zip 调用 |
| 知识包导入 | ⏳ 下一轮 | 反向 |
| `sts2_code_facts_provider` 真实实现 | ⏳ 下一轮 | 走反编译产物，提取符号 / 类型事实（Python ~349 LOC） |
| 两壳路由暴露 status / start_refresh / get_refresh / export-pack / import | ⏳ 下一轮 | 提交端点已通过 Tauri command（submit_knowledge_refresh_job） |
| 前端 KnowledgeCard 加 Refresh 按钮 + 进度条 | ⏳ 下一轮 | |

---

### 2.5 其它中小模块（合计 2–3 周，可与 stage 3 并行）

| 模块 | Python LOC | 优先级 | 备注 |
| --- | --- | --- | --- |
| auth（JWT / session / email 验证） | ~250 | 高（web 必备） | 注意 web role 才需，桌面跳过 |
| me_router（用户资料） | ~200 | 高 | |
| approval | ~80 | 中 | |
| workflow | ~120 | 中 | |
| build | ~80 | 中 | |
| batch_workflow router | ~150 | 中 | |
| log_analyzer | ~120 | 低 | |
| mod_analyzer | ~100 | 低 | |
| workstation_capabilities | ~60 | 中 | |
| project_utils（中文路径） | ~80 | 中 | |
| image-proc（rembg via ort） | ~150 | 中 | 模型权重首启下载策略 |

---

### 2.6 Stage 6 — 前端业务页（4–6 周，与 stage 3 并行）

按依赖序：

#### 2.6.1 共享基础（3–5 天）
- zustand store 结构（Q4 决策后定）
- 全局错误边界、Loading / Skeleton 组件
- API 错误统一处理（401 跳登录、429 提示等）
- 主题切换（沿用现 frontend/index.css 设计 token）
- 路由：react-router-dom 7 配置 + `__IS_TAURI__` 守卫

#### 2.6.2 用户区（2 周）
- `features/auth/`（注册 / 登录 / 找回密码）
- `features/user-center/`（任务历史、退款汇总、任务详情）
- `features/workspace/`（项目工作区、执行模式选择）
- `features/single-asset/`（单资产生成）
- `features/batch-generation/`（批量生成审查、execution bundles UI）

#### 2.6.3 平台/管理区（2 周）
- `pages/admin/`（11 个页面）：Audit / CredentialHealth / ExecutionProfiles / Executions / KnowledgePacks / Layout / Overview / Refunds / Runtime / ServerCredentials / Users
- `pages/BatchMode.tsx` / `SettingsPage.tsx` / `AdminRuntimeAuditPage.tsx`

#### 2.6.4 工具区（3–5 天）
- `features/log-analysis/`
- `features/mod-editor/`
- `features/platform-run/`

#### 2.6.5 验收
- 每个 feature 至少有 happy path 截图测试
- E2E 冒烟脚本（playwright 可选）至少跑通 4 条核心链路：登录、提交任务、查询历史、admin 审计

---

### 2.7 Stage 7 — Deploy CLI（1 周）

| 子命令 | 替代 | 备注 |
| --- | --- | --- |
| `ats deploy` | tools/latest/deploy-app.ps1 | docker compose 调度 |
| `ats package` | package-app.ps1 | 打 tarball + Tauri bundle 整合 |
| `ats logs` | logs-app.ps1 | docker compose logs + grep |
| `ats stop` | stop-app.ps1 | docker compose down |
| `ats install` | （新增） | 首次部署引导 |
| `ats migrate` | （新增） | sqlx migrate run 包装 |

模板内嵌：`compose.app.yml` 通过 include_str! 进二进制。

---

### 2.8 Stage 5 — 装配收口（3–5 天，stage 3+4 都到位后做）

- ats-web 完整 router 树（health / knowledge / planning / codegen / llm / platform / admin / auth）
- src-tauri 完整 invoke_handler 列表
- 启动期 ordered lifecycle：
  - load config → ping db → seed default execution_profiles → start queue worker → start image-proc prewarm → mount routes → bind listener
- 优雅停机：`tokio::signal::ctrl_c` + worker.stop() + db.close()
- 健康检查全字段填充（db / llm / image-proc / queue worker 各分项 status）

---

### 2.9 Stage 8 — 切换 main（1 天）

1. 在 `rust` 分支冒烟覆盖：登录 / 提交任务 / 流式回调 / admin 审计 / 知识库刷新 / 部署
2. 给 Python 代码打 tag `python-final`
3. `git mv rust/* .` + 删除 `backend/` `frontend/` `tools/`
4. 合并 `rust` → `main` 通过 PR（包含 deploy 配置切换）
5. CI 切换到 cargo + npm 流水线

---

## 3. 时间表（单人全职估）

| 阶段 | 估时 | 累计 |
| --- | --- | --- |
| ✅ 0 + 1 + 2.1 + 2.3 + 2.3.x + 2.4 已完成 | 已花 ~6 个 session | — |
| Stage 4 — LLM 客户端 | 1.5 周 | 1.5 周 |
| Q1/Q3 决策（并行，几小时） | — | 1.5 周 |
| Stage 3.1a/b — Web DB + Desktop 工程文件夹存储 | 1.5 周 | 3 周 |
| Stage 3.2 — Domain + Contracts | 4 天 | 3.5 周 |
| Stage 3.3 — Repositories（双轨：sqlx + file） | 2 周 | 5.5 周 |
| Stage 3.4 — Services | 1.5 周 | 7 周 |
| Stage 3.5 — Runner Handlers | 1.5 周 | 8.5 周 |
| Stage 3.6 — Routes/Commands | 4 天 | 9 周 |
| Stage 2.2 — Knowledge 动态 | 1 周 | 10 周 |
| 其它中小模块（合计） | 2.5 周 | 12.5 周 |
| Stage 6 — 前端业务页（含 Project 管理 UI） | 5 周（与 3 并行 50%） | 实际累计 14.5 周 |
| Stage 5 — 装配收口 | 4 天 | 15 周 |
| Stage 7 — Deploy CLI | 1 周 | 16 周 |
| Stage 8 — 切换 main | 1 天 | 16 周 |

**单人全职合计 ≈ 16 周（≈4 个月）**。工程文件夹方案相比 SQLite 多了 ~3 天（文件 repo
工作量 + Project 管理 UI），换取桌面端零 DB 依赖、可整体打包分享。

考虑兼职 / 中断 / 决策耗时，**保守估 6 个月**。可压缩路径：
- 跳过 admin 多页（只保留 Overview）能省 1.5 周
- 跳过 log_analyzer / mod_analyzer 能省 1 周
- 跳过 batch_workflow / approval 能省 1 周

---

## 4. 决策清单（按紧迫度排）

### 4.1 必须立刻决策

| Q | 阻塞 | 状态 | 决策窗口 |
| --- | --- | --- | --- |
| Q1 — 桌面端 DB？ | Stage 3.1 | **✅ 已决：B — 工程文件夹模式**（详 §2.3.0） | — |
| Q3 — 远端 workstation 保留？ | Stage 3.4 | **B**：保留 `--mode workstation` | Stage 3.4 启动前 |

### 4.2 可拖到对应阶段再决策

| Q | 阻塞 | 当前倾向 | 决策窗口 |
| --- | --- | --- | --- |
| Q2 — 桌面端暴露 admin 路由？ | Stage 3.6 | 否 | Stage 3.6 |
| Q4 — zustand store 重设计 vs 镜像？ | Stage 6.1 | 重设计 | Stage 6.1 |
| Q5 — runtime config schema 字段对齐 Python？ | Stage 3.1 | 是 | Stage 3.1 |
| Q6 — CI 接入时机？ | — | 现在 | 建议本周 |
| Q7 — 签名/公证？ | Stage 8 | 先发不签名版 | Stage 8 |
| Q8 — Tauri 自动更新？ | — | 后置 | Stage 8 后 |

---

## 5. 风险与应对

### 5.1 流出风险

| 风险 | 当前状态 | 应对 |
| --- | --- | --- |
| Python 端持续接收业务变更，rust 分支 diff 越拖越大 | 现 5efe6fe..main 已落后 0 commit | 每 stage 结束尝试合并到 main 子目录，逐步压缩 diff |
| LLM SDK 生态弱，自己造轮子工作量被低估 | 未启动 | 严格只支持 Anthropic + OpenAI，其它后置；用 wiremock 单测代替真实调用 |
| sqlx 编译期类型检查在 CI 需要 PG 实例 | 未启动 | 采用 offline 模式（`.sqlx/` 缓存）；本地 prepare + 提交缓存；CI 校验 |
| 中文路径 / 编码 Bug | 已踩 1 次（PS5.1 vs UTF-8） | 全局 CLAUDE.md 已更新；新代码注释/字符串能用英文则用英文 |
| Tauri v2 + WebView2 兼容 | 已踩 1 次（icon 缺失） | 现已配齐图标；Win7/8 暂不支持，可接受 |
| workspace 编译慢 | 当前 cargo check ~2.5s（增量），全量 ~30s | 接 CI 后用 sccache + cargo nextest；feature flag 让可选依赖（如 ort）默认 off |

### 5.2 行动风险（"做着做着发现做错了"）

| 风险 | 减灾 |
| --- | --- |
| Stage 3 platform 拆分过细，模块边界要回炒 | 每个子阶段结束做一次 cargo clippy 全 workspace；mod 边界用 pub(crate) 限制 |
| 前端 store 设计与后端 API 错位 | 每个 feature 启动前先把 tauriApi.ts 签名补齐，再写 UI |
| LLM 流式响应在桌面端用 emit，web 端用 WS，行为漂移 | 设计 `ProgressSink` trait 统一接口；双端 happy path 写对比测试 |

---

## 6. 验收门（每阶段必须过）

每个 stage 完成后必须满足：

- [ ] `cargo test --workspace` 全过
- [ ] `cargo clippy --workspace -- -D warnings` 无 warning
- [ ] `npx tsc --noEmit` 无错误
- [ ] `npm run build:web` 通过
- [ ] 端到端：至少一条新加链路从前端 → API → core → 落到磁盘/DB 可见
- [ ] 本计划文档更新进度表
- [ ] git commit message 包含验收要点

---

## 7. 紧接着推荐做的事（按顺序）

桌面端 Stage 3.5 已完成 7/8（asset_generate 留待 image_gen 客户端）。下一步候选：

1. **Stage 2.2 — knowledge 动态**（约 1 周）：ilspycmd 子进程 + BaseLib GitHub Releases
   下载 + KnowledgeRefreshHandler（套用现有 platform Job 框架）。会让前端
   KnowledgeCard 多一个 "Refresh" 按钮。
2. **image_gen + asset_generate handler**（约 0.5 + 0.5 周）：先做文生图客户端
   （火山/BFL）+ rembg 走 `ort`，再补齐 asset_generate handler，凑齐 stage 3.5
   全 8 个 handler。
3. **Stage 6 前端业务页**（约 4-6 周，与后端并行无冲突）：features/auth /
   single-asset / batch-generation / log-analysis / mod-editor 等。
4. **Q3 决策 + Stage 3.1a/3.3a Web sqlx 启动**（保留 Web 远端 workstation 模式）。
5. **CI 接入**（Q6）：cargo test / clippy（需先做一次全 workspace pedantic
   清理）/ tsc / vite build。
6. **Stage 5 / 7 / 8** 收尾。

---

## 8. 关键 commit 索引（便于 rebase / 回溯）

- 骨架：`8c8bf3c`
- config：`6628f26` → `7ee0b57`（walk-up 修）
- knowledge 静态：`0600b80`
- planning 主体：`334f070` → `204ca72`（execution_bundles）
- prompting + knowledge contracts：`1160eaf`
- sts2 providers：`750bea7`
- codegen prompt assembler：`7aaeb3a` → `a120309`（两壳接口 + 前端）
- LLM：`d626f89`（Anthropic 非流式/流式 + retry）→ `c983d98`（前端流式集成）
- 工程文件夹：`e8aee5c`（ProjectFolder / AppDataPaths / RecentProjects）
- Platform Job 桌面端：`adf33cf`（Job/JobKind/JobStatus + FileJobRepo + JobApplicationService + text_generate）
- Stage 3.5 第一轮：`1b8e2bf`（code_generate + build_project）
- Stage 3.5 第二轮：`519aa4f`（handlers/ 子目录重构 + log_analysis / package_project / batch_custom_code / single_asset_plan，+25 测）
- Stage 2.2.1：本轮（ilspycmd 子进程 + manifest + KnowledgeRefreshHandler，+17 测）
