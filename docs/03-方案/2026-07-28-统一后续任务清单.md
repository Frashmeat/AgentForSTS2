# 2026-07-28 - 统一后续任务清单

> 文档定位：本文是旧 Trellis 任务清理后的唯一后续任务入口，集中记录当前仍需执行、需要补齐或需要重新决策的工作。
>
> 当前范围：`rust` 分支的 Rust/Tauri 桌面端 MVP 收口。
>
> 事实入口：[`2026-07-27-当前进度说明`](../02-现状/2026-07-27-当前进度说明.md)。
>
> 最后更新：2026-07-29

## 1. 使用规则

- 本文是后续工作的统一队列；不再以 2026 年 4 月遗留的 `.trellis/tasks/` 状态判断当前进度。
- 开始实际工作时，从本文选择一个条目，新建范围明确的 Trellis task；不要重新激活已归档旧任务。
- “历史已完成”只表示旧 Python/Web 路线曾经交付，不表示当前 Rust 分支包含同名 Web/SaaS 能力。
- Web/SaaS 阶段若重新启动，应根据 Rust 当前架构重新设计和立项。

## 2. 当前活动 Trellis 任务

当前 `.trellis/tasks/` 只保留一个活动任务：

| 任务 | 优先级 | 状态 | 当前范围 |
| --- | --- | --- | --- |
| [`07-29-game-pack-stage1`](../../.trellis/tasks/07-29-game-pack-stage1/prd.md) | P0 | in_progress | 落实 ADR 0004；先以 Gate 0 完成 STS2 真实基线，再逐维度迁移到最小 STS2 Game Pack |

任务整理规则：

- `07-28-real-game-load-asset-quality` 已由当前任务接管并以 `superseded` 状态归档；其 P0-1/P0-2 证据保留，manifest、新候选和人工复验转入 Gate 0。
- 当前任务内部按 Gate 0 至 Gate 7 顺序推进，不为各维度建立平行 Trellis 任务。
- 现有 22 个历史/被接管任务已集中在 `.trellis/tasks/archive/2026-07/`，不重新激活。
- 多游戏 Game Pack 架构以 [ADR 0004](../04-决策/0004-generic-mod-pipeline-and-game-pack.md) 为边界；第一阶段只定义 STS2 已验证能力需要的最小 schema。
- 当前任务完成、STS2 自动与人工复验通过并归档后，再从下方队列选择下一项。

## 3. 当前执行顺序

| 顺序 | 工作包 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| 1 | 知识库全量事实索引与 GUI E2E | 已完成 | 3,425 个文件不再被固定上限截断；`NSettingsScreen` 返回游戏事实；GUI 成功及 BaseLib 401/403 可行动错误场景通过 |
| 2 | 通用 Mod 流水线与 STS2 Game Pack Stage 1 | 进行中；行为与图片自动门禁已完成，Gate 0 待 manifest、新候选和人工复验 | 先闭环 STS2 真实基线，再以职责清单、最小 schema、新旧等价和逐维度唯一真相源完成 ADR 0004 第一阶段 |
| 3 | GUI 可行动错误提示 | Godot 错误已通过，其余待验收 | LLM、image、knowledge、package 的配置、鉴权、网络和写盘失败都能指导用户修复或重试 |
| 4 | baseline 与 ML 安装版 | 待验收 | 安装、启动和基本 job 通过；ML 首次 prewarm、ONNX Runtime 准备和 fallback 符合预期 |
| 5 | 双进程工程锁 | 单元测试已通过，待真实双进程验证 | 持锁拒绝、退出后恢复、常驻锁文件不误判三种场景通过 |
| 6 | 发布候选验证 | 等待前五项清零 | workspace 测试/clippy、前端生产构建、Tauri baseline/ML 构建和 Docker 日志分类完成 |
| 7 | 开发规范补全 | P1，发布收口后执行 | 前后端规范反映当前 Rust/Tauri/React 实现，包含真实示例、禁止模式和测试命令 |

执行约束：

- 默认一次只建立一个活动 Trellis task；完成、验证、提交和归档后再进入下一项。
- 第 1 项是当前最高优先级，因为已经证明固定 2,000 文件截断会遗漏真实游戏类型。
- 第 2 至第 5 项都是发布前验收，不扩大到 Web/SaaS、自动更新、签名或跨平台打包。
- P2 统一弹窗仅在满足触发条件时重新评估，不进入当前固定执行序列。

已经完成、不再重复进入队列的主链包括：`GodotPath` 配置、`local.props` 同步、single asset 生成、compile gate、GUI build/package、最新 DLL 全量事实索引、知识任务统一查询路由和知识 GUI 成功/401/403 E2E。

## 4. P0 - 桌面端 MVP 验收

### P0-1 真实 single asset 主链路

目标：使用真实配置完成 `single_asset_plan -> asset_generate/code_generate -> build/package`，确认工程目录中的计划、源码、图像、artifact、history 和 audit 一致。

验收条件：

- [x] 真实 LLM 和 image generation 配置能完成一次单资产生成。
- [x] `Generated/`、`artifacts/`、`.ats/` 中的结果可追溯且相互一致。
- [x] 任务期间切换页面后仍能恢复并展示进度和结果。

2026-07-28 修复后核心服务链已在全新隔离工程通过：4 个 job 全部 completed、12 条 audit 一致；真实 LLM/图片生成产生可编译 C#、双语本地化和三张正式遗物资源；compile gate、正式 publish、package 与 PCK 目录复验均通过，不需要人工修改生成内容。同日又由真实 Tauri GUI 完成 delayed plan 跨页恢复、asset、build 和 package，最终 suite 为 3/3 通过。

本项剩余阻断：

- 2026-07-28 已在真实游戏中加载 BaseLib 3.3.8 和 `E2ESingleRelic`；DLL、PCK、initializer 加载通过，中文正确，无卡死、报错弹窗或可见资源缺失。
- `BeforeCombatStart + GainEnergy` 的模板、当前源码证据注入和写盘前语义门禁已于 2026-07-29 完成针对性自动测试；尚待新候选生成、compile gate/build/package 和用户真实游戏行为复验。
- normal 和 big 的既有候选在游戏内有明显噪点；质量报告/拒绝门禁及 relic normal/outline/big 独立派生已于 2026-07-29 完成针对性自动测试，尚待新候选生成、文件级检查和用户真实游戏视觉复验。
- manifest 仍使用旧式 BaseLib dependency 且未声明 `min_game_version`，当前可运行但有未来兼容风险。
- 详细证据、根因和复验标准见 [`真实游戏加载与资产质量问题审查`](../05-审查与验证/进行中/2026-07-28-真实游戏加载与资产质量问题审查.md)。

`.trellis/tasks/archive/2026-07/07-28-gui-e2e-godot-path/` 已完成 `GodotPath` 产品配置、受控 `local.props` 同步和真实桌面 GUI E2E，并提交为 `a326ed1c`；真实游戏加载不纳入该任务。

证据见 [`2026-07-28 single asset E2E 验证记录`](../05-审查与验证/已经完成/2026-07-28-single-asset-E2E验证记录.md)和[`2026-07-28 GUI E2E 与 GodotPath 验证记录`](../05-审查与验证/已经完成/2026-07-28-GUI-E2E与GodotPath验证记录.md)。

### P0-2 错误暴露路径

目标：验证 LLM、图像生成、知识库、构建和打包失败时，GUI 能提供可行动的信息。

验收条件：

- [ ] 配置缺失、鉴权失败、网络失败和上游返回异常均有明确提示。
- [x] 失败任务状态、history 与 audit 记录一致。
- [ ] 用户能够从提示中判断需要修改配置、重试还是检查外部环境。

当前已证明 Godot 文件不存在和未配置时 GUI 会给出可行动提示，且清空后 build 在创建 job 前拒绝；知识 BaseLib 401/403 也已通过 GUI E2E。LLM、image 和 package 错误场景仍未覆盖。

知识核心服务与 GUI 已覆盖两种外部错误：401 映射为 token 无效或过期并指向配置位置，403 rate limit 映射为配置有效 token 或等待恢复。handler 保留 game 刷新结果并把 BaseLib 降级为 warning；原始响应体、token 和 stub 敏感内容不会进入 GUI。

### P0-3 真实工程构建

目标：使用真实 STS2/Godot 路径验证生成工程能够执行 `dotnet publish`。

验收条件：

- [x] `local.props` 或本地路径覆盖正确生效。
- [x] Harmony、STS2 和 Godot 依赖能够被项目解析。
- [x] 构建输出可进入后续 package 流程。

2026-07-28 首次真实 `dotnet publish` 因模板默认 `GodotPath` 不存在而停止。随后使用官方 4.5.1 可执行文件完成修复验证；自动生成内容未经人工修改即可通过 compile gate 与正式 build/package。PCK export filter 已收紧并由目录表复验；`GodotPath` 现已进入产品 Settings、config、工程 `local.props` 和 build 前同步链。

### P0-4 安装版与 ML 首次运行

目标：分别验收 baseline 和 `-MlRembg` 安装版。

验收条件：

- [ ] 安装、启动、创建/打开工程和基本 job 操作通过。
- [ ] ML 版本首次 prewarm 能准备 ONNX Runtime，并能清楚暴露失败原因。
- [ ] 未启用或无法启用 ML 时，fallback 行为符合预期。

### P0-5 双进程工程锁

目标：手工验证两个独立桌面进程不能同时写入同一工程。

验收条件：

- [ ] 第一进程持锁时，第二进程得到稳定的工程占用提示。
- [ ] 第一进程正常或异常退出后，第二进程能够重新打开工程。
- [ ] 常驻 `.ats/lock` 文件不会造成假死锁。

### P0-6 最新游戏版本知识库兼容

目标：让知识刷新和事实查询完整覆盖 Steam build `23811903`（游戏 `v0.107.1`）及后续同规模版本。

验收条件：

- [x] 最新 `sts2.dll` 可真实反编译，manifest stale/cache 判定正确。
- [x] BaseLib 本地反编译后知识状态为 `fresh`，关键 card/relic/power/character 查询可用。
- [x] facts provider 不再因固定 2,000 文件上限遗漏后段游戏类型；`NSettingsScreen` 精确查询返回游戏事实。
- [x] 有效 GitHub 凭据下在线 BaseLib `v3.3.8` 下载和反编译通过。
- [x] Tauri GUI knowledge job 的成功、限流和失效 token 提示经过 E2E。

当前真实输入产生 3,425 个 C# 文件；`Sts2CodeFactsProvider` 现按路径确定性排序并扫描，安全上限为 10,000，后段 `NSettingsScreen` 与五个核心 symbol 均返回游戏事实。Tauri GUI 隔离 suite 为 4/4 通过，并修复了知识任务写入全局仓库却无法从工程级任务列表观察的路由缺陷；有效 GitHub 凭据下又完成 BaseLib `v3.3.8` 的真实在线下载和反编译。详细证据见 [`2026-07-28 最新游戏版本知识库验证记录`](../05-审查与验证/已经完成/2026-07-28-最新游戏版本知识库验证记录.md)。

### P0-7 发布候选验证

目标：在 P0-1 至 P0-6 阻断项清零后，形成桌面 MVP 发布候选基线。

建议验证：

- [ ] `cargo test --workspace`
- [ ] workspace 级 clippy
- [ ] 前端测试、TypeScript 检查和生产构建
- [ ] 完整 Tauri baseline/ML 构建
- [ ] 补取 `scripts/ci-local.ps1` 的 Docker 原始失败日志并完成分类

上述全量或重型验证执行前仍按项目规则说明必要性并取得确认。

## 5. P1 - 开发规范补全

来源：已归档 `00-bootstrap-guidelines`。

当前 `.trellis/spec/frontend/` 与 `.trellis/spec/backend/` 只有质量、错误和日志等少量专题具备一定项目事实，其余文件仍是简短骨架，索引多数标记为 `To fill`。

验收条件：

- [ ] 根据当前 Rust/Tauri、React 和轻量 Web 代码重写前后端规范索引。
- [ ] 每份适用规范包含 2 至 3 个当前代码示例和真实文件路径。
- [ ] 明确禁止模式、常见错误、测试命令和适用边界。
- [ ] 删除仅针对旧 Python/Web 架构且不再适用的模板描述。

该项不阻断桌面 MVP 功能验收，但应在下一轮较大开发前完成。

## 6. P2 - 统一弹窗框架按需重评估

来源：已归档 `04-29-unified-dialog-framework`。

旧任务只完成了方案和 `ConfirmDialog` 局部修复，没有实现计划中的 `AppDialog/AppDrawer`；随后旧 `frontend/src` 被 Rust/Tauri 前端平铺重写替代，因此不直接移植旧方案。

触发条件：

- 当前 `src/` 出现 3 个以上重复弹窗/抽屉壳层；或
- 新增高风险确认流程，需要统一焦点、键盘关闭和可访问性行为。

触发后重新审查当前 UI 组件，再决定是否建立 `AppDialog/AppDrawer`，不要按旧路径直接实现。

## 7. 已归档旧任务处置表

| 旧任务 | 处置 | 证据或去向 |
| --- | --- | --- |
| `00-bootstrap-guidelines` | 未完成，已合并 | 本文 P1 |
| `04-15-server-credential-create` | 历史已完成 | `158834a5` |
| `04-15-server-credential-manage` | 历史已完成 | `158834a5` |
| `04-15-server-executor-routing` | 历史已完成 | `158834a5`、`358db3bd` |
| `04-20-server-deploy-registry-v1` | 历史已完成 | `2abcc27e` |
| `04-20-server-deploy-target-lock-v1` | 历史已完成 | `a999f99f` |
| `04-20-server-queued-worker-v1` | 历史已完成 | `8a4c7ae1`、`f77f1367` |
| `04-20-server-workspace-lock-v1` | 历史已完成 | `af15437b`、`95a95f09` |
| `04-22-knowledge-code-first-stage1` | 历史已完成 | `ff820750`；原 PRD 验收项全勾选 |
| `04-23-settings-auto-save` | 历史已完成 | `5ac1757d`、`977bfb17` |
| `04-27-frontend-status-notice-errors` | 历史已完成 | `e59f34b4` |
| `04-27-tools-menu-separation` | 历史已完成 | `e59f34b4` |
| `04-29-unified-dialog-framework` | 未实现且被路线替代 | 本文 P2 |
| `04-29-web-workstation-execution-split` | 历史已完成 | `19788339` 至 `78ed63f4` 的分步实现 |

历史完成项对应的旧 Python/Web 源码后来由 `36039114` 的 Rust 平铺重写移除。该事实不回退历史完成结论，也不把这些能力描述为当前 Rust 分支能力。

## 8. 明确不进入当前队列

- Web auth/admin/users/tokens。
- SQLx/PostgreSQL/migrations 与平台级 jobs。
- Rust Web/SaaS 版服务器凭据池、执行调度和 Workstation 控制面。
- new-api 管理集成、自动更新、签名公证和 Mac/Linux 打包。

这些事项只有在用户决定启动相应阶段后才重新建立任务和验收标准。
