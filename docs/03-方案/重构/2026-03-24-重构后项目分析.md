# AgentTheSpire 重构后项目分析

## 重构目标

重构后的 AgentTheSpire 不再以 `routers / agents / image / llm / approval` 这种技术分层作为主边界，而是转为：

- 后端以领域模块组织：`workflow / planning / image / codegen / approval / build / knowledge`
- 共享能力沉淀到 `shared/kernel + shared/contracts + shared/infra`
- 外部系统通过 adapter 接入，不让业务模块直接依赖 Claude、Codex、BFL、ComfyUI 等具体实现
- 单资产与批量流程共用一个统一工作流编排内核
- 前端从页面直连 WebSocket，重构为 `features + shared/ws + shared/types`

---

## 重构后整体架构

```mermaid
graph TB
    User["用户"]

    subgraph Frontend["前端 React + TypeScript"]
        APP["app 页面装配层"]
        FEAT["features 业务功能层\nsingle-asset / batch-generation / mod-editor / log-analysis"]
        FWS["shared/ws 统一传输层"]
        FAPI["shared/api REST 客户端"]
        FTYPES["shared/types 共享契约"]
    end

    subgraph Backend["后端 FastAPI + Python"]
        subgraph API["API 适配层"]
            R1["workflow router"]
            R2["batch workflow router"]
            R3["approval router"]
            R4["config router"]
        end

        subgraph Shared["shared 共享层"]
            KERNEL["kernel\nResult / Error / Event / ID"]
            CONTRACTS["contracts\n事件协议 / DTO / 状态模型"]
            INFRA["infra\nconfig / logging / subprocess / fs / http / llm adapters"]
        end

        subgraph Modules["modules 领域模块"]
            WF["workflow\n统一编排内核"]
            PLAN["planning\n需求拆解 / 依赖排序"]
            IMG["image\nprompt 优化 / 生图 / 后处理"]
            CODE["codegen\nprompt 组装 / 代码生成 / 产物写入"]
            APV["approval\n审批 / 风险 / 执行编排"]
            BLD["build\n编译 / 产物收集"]
            KNOW["knowledge\nSTS2 文档知识源"]
        end

        COMP["composition\ncontainer / registry"]
    end

    subgraph External["外部依赖"]
        Claude["Claude CLI"]
        Codex["Codex CLI"]
        LiteLLM["LiteLLM"]
        BFL["BFL"]
        JM["即梦"]
        WX["万相"]
        CUI["ComfyUI"]
        DOTNET["dotnet build/publish"]
    end

    subgraph Output["输出"]
        MOD["Mod 项目产物"]
        BUILD["构建产物"]
    end

    User --> Frontend
    APP --> FEAT
    FEAT --> FWS
    FEAT --> FAPI
    FEAT --> FTYPES

    FWS --> R1
    FWS --> R2
    FAPI --> R3
    FAPI --> R4

    R1 --> WF
    R2 --> WF
    R3 --> APV
    R4 --> INFRA

    WF --> PLAN
    WF --> IMG
    WF --> CODE
    WF --> APV
    WF --> BLD

    CODE --> KNOW
    PLAN --> CONTRACTS
    IMG --> CONTRACTS
    CODE --> CONTRACTS
    APV --> CONTRACTS
    BLD --> CONTRACTS

    Modules --> KERNEL
    Modules --> CONTRACTS
    Modules --> COMP
    COMP --> INFRA

    INFRA --> Claude
    INFRA --> Codex
    INFRA --> LiteLLM
    INFRA --> BFL
    INFRA --> JM
    INFRA --> WX
    INFRA --> CUI
    INFRA --> DOTNET

    CODE --> MOD
    BLD --> BUILD
```

## 统一工作流编排模型

```mermaid
flowchart TD
    A["用户提交请求"] --> B["API Router\n解析请求 / 建立连接"]
    B --> C["Workflow Engine\n创建 WorkflowContext"]
    C --> D["选择执行策略"]

    D --> E["单资产线性流程"]
    D --> F["批量 DAG 流程"]

    E --> G["Planning Step"]
    F --> G

    G --> H["Image Step\nPrompt 优化 / 生图 / 后处理"]
    H --> I["Approval Step\n生成待审批动作或自动通过"]
    I --> J["Codegen Step\n组装 Prompt / 调用 AgentBackend"]
    J --> K["Build Step\n编译 / 收集产物"]
    K --> L["Done"]

    C --> M["Event Publisher"]
    G --> M
    H --> M
    I --> M
    J --> M
    K --> M

    M --> N["WebSocket Event Stream"]
    N --> O["前端统一事件总线"]
```

## 模块依赖边界

```mermaid
graph LR
    subgraph DomainModules["领域模块"]
        WF["workflow"]
        PLAN["planning"]
        IMG["image"]
        CODE["codegen"]
        APV["approval"]
        BLD["build"]
        KNOW["knowledge"]
    end

    subgraph Shared["共享层"]
        K["kernel"]
        C["contracts"]
    end

    subgraph Infra["基础设施层"]
        LLMI["llm adapters"]
        IMGI["image providers"]
        EXE["executor / subprocess / fs"]
        CFG["config / logging"]
    end

    WF --> K
    WF --> C
    PLAN --> K
    PLAN --> C
    IMG --> K
    IMG --> C
    CODE --> K
    CODE --> C
    APV --> K
    APV --> C
    BLD --> K
    BLD --> C
    KNOW --> K
    KNOW --> C

    WF -.调用端口.-> IMG
    WF -.调用端口.-> CODE
    WF -.调用端口.-> APV
    WF -.调用端口.-> BLD
    CODE -.读取上下文.-> KNOW
    IMG -.通过端口.-> IMGI
    CODE -.通过端口.-> LLMI
    APV -.通过端口.-> EXE
    BLD -.通过端口.-> EXE

    Infra --> CFG
```

## 前端重构后结构

```mermaid
graph TB
    subgraph Frontend["frontend/src"]
        APP["app\n页面与路由装配"]
        subgraph Features["features"]
            S1["single-asset"]
            S2["batch-generation"]
            S3["mod-editor"]
            S4["log-analysis"]
        end
        subgraph Shared["shared"]
            API["api"]
            WS["ws"]
            TYPES["types"]
            UI["ui"]
            STORE["store"]
        end
    end

    APP --> S1
    APP --> S2
    APP --> S3
    APP --> S4

    S1 --> WS
    S1 --> API
    S1 --> TYPES

    S2 --> WS
    S2 --> API
    S2 --> TYPES

    S3 --> API
    S4 --> API

    WS --> TYPES
    API --> TYPES
    STORE --> TYPES
```

## 重构收益

```mermaid
mindmap
  root((重构收益))
    模块化
      领域边界清晰
      文件职责单一
      更容易定位问题
    可扩展
      可新增图像供应商
      可新增 AgentBackend
      可新增审批策略
      可新增工作流类型
    解耦
      业务不直连 CLI
      Router 不承载流程
      前端不直连原始事件
    可测试
      模块可独立测试
      工作流可做步骤级测试
      适配器可做替身注入
    可迁移
      新旧路径可并存
      可通过 feature flag 切换
      支持渐进替换
```

## 结论

重构后的 AgentTheSpire 会从“多个大文件串联外部能力”的形态，转成“统一编排内核驱动多个领域模块”的形态。

最关键的设计判断是：

- 把 `workflow` 提升为系统中心
- 把 `llm / image provider / executor / build` 下沉为 adapter
- 把前端状态和传输层从页面中剥离
- 用共享契约统一前后端和单资产、批量两条链路
