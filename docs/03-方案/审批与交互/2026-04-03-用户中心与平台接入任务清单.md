# 2026-04-03 用户中心与平台接入任务清单

> 规则：每完成一项任务，都要同步更新相关设计、实施计划与接口说明。

## 执行顺序

1. 先完成 `web-backend` 独立部署基础收口
2. 再补 `/api/auth/me` 与 `/api/me/*`
3. 再进入用户中心、认证页、平台历史与分流逻辑实现

说明：

- 第 1 步是本轮先行实施项
- 第 1 步未完成前，不进入用户中心前端主链的大面积实现
- 若需要提前改动后续代码，只允许做不影响顺序的准备性整理，不视为主链开始

## 当前进度

- [x] Task 0.1：收口 `web` 角色路由边界与独立入口
- [x] Task 0.2：收口 `web` 角色配置与容器装配
- [x] Task 0.3：补独立部署脚本与说明
- [x] Task 0.4：把用户中心主链默认指向 `web-backend`

## 主链进度

- [x] Task 1：认证领域与服务骨架
- [x] Task 2：认证持久化与配置入口
- [x] Task 3：认证接口与当前用户接口
- [x] Task 4：当前用户视角平台 facade
- [x] Task 4.1：前端默认当前用户查询入口
- [x] Task 5：返还聚合进入普通用户视图
- [x] Task 6：路由与全局会话状态底座
- [x] Task 7：认证 API 与认证页面骨架
- [x] Task 8：头部账号入口与认证路由挂载
- [x] Task 9：用户中心总览页
- [x] Task 9.1：平台历史与本地历史边界
- [x] Task 10：任务详情页与返还摘要
- [x] Task 11：本机 AI 可用性健康状态接口
- [x] Task 12：统一执行分流弹窗
- [x] Task 13：服务器模式下的创建任务与确认开始
- [x] Task 14：文档与接口说明更新

## 本次必须完成

- [x] 先行实施且优先完成：`web-backend` 独立部署基础收口
- [x] 前置门槛：补齐 `/api/auth/me` 与 `/api/me/*`，否则前后端身份边界不成立
- [x] 认证主链最小闭环：注册、登录、邮箱验证、找回密码、当前用户接口
- [x] 前端停止在新页面直传裸 `user_id`
- [x] 补 `/api/me/*` 当前用户视角平台查询接口
- [x] 用户中心单页：用户信息、统一次数池、平台任务列表
- [x] 任务详情页：子项列表、返还摘要、净消耗
- [x] 返还信息下沉到普通用户可见层
- [x] 本机 AI 配置健康状态接口
- [x] 统一的“本机执行 / 服务器执行”分流弹窗
- [x] 服务器模式下的“创建任务 -> 确认开始”前置确认
- [x] 平台历史与工作站本地历史边界隔离

## 本次重点验证

- [x] 验证 Phase 0 完成前，未提前进入用户中心前端主链实现
- [x] 验证 `main_web.py` 可独立启动且只挂载 web/平台/认证相关路由
- [x] 验证新增认证页、用户中心页、平台历史页不再直接传裸 `user_id`
- [x] 认证接口定向测试
- [x] 当前用户视角平台查询定向测试
- [x] 返还聚合查询定向测试
- [x] 用户中心页面定向测试
- [x] 任务详情页定向测试
- [x] 分流弹窗与登录回跳定向测试
- [x] 验证 BYOK 不进入平台历史

## 当前验证结论

- 已在主工作区合并后重跑定向验证：
  - 后端：`python -m pytest --import-mode=importlib ... -q`，`11 passed`
  - 前端：`node --test frontend/tests/meApi.test.ts frontend/tests/configApi.test.ts frontend/tests/executionModeDialog.test.ts frontend/tests/platformRunFlow.test.ts frontend/tests/serverFlowGate.test.ts`，`11/11 passed`
- `build` 与全量测试未在本轮自动执行

## 本次不做，转后续规划

- 真实邮件发送能力
- 更完整的账号安全加固
- 工作站本地历史页面
- 独立 Web 前端站点
- 商业化套餐与后台计费规则配置
- 更细粒度的返还判定矩阵
