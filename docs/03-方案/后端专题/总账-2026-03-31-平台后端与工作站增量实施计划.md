# 平台后端与工作站增量实施总账 Implementation Ledger

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 `FastAPI` 单体中建立平台模式的持久化任务主链、服务层、步骤协议和用户/管理员 API，并在不重写现有入口的前提下逐步把旧路由迁移为薄传输层。

**Architecture:** 采用“单写新主链 + flag 控制入口与兼容投影”的迁移方式。新的 `job -> job_item -> ai_execution` 是平台模式唯一事实源，兼容性通过 router 委托、DTO 适配和统一事件模型处理，不做旧/新数据库事实双写。当前仓库缺少数据库与 migration 基础设施，因此先补 ORM / migration / session 基座，再落 schema、repository、service、runner 和平台 API。

**Tech Stack:** Python, FastAPI, Uvicorn, Pydantic, SQLAlchemy 2.x, Alembic, PostgreSQL, pytest, pytest-asyncio

---

## 2026-04-14 当前状态说明

- 当前代码已经落地平台主链主体实现，不再处于“等待启动”的计划阶段。
- 已落地的主线事实包括：
  - `backend/app/modules/platform/` 已形成 `contracts / domain / infra / application / runner` 分层结构。
  - `backend/migrations/versions/20260331_01_platform_job_chain.py` 已落地平台主链 migration。
  - `backend/routers/platform_jobs.py` 与 `backend/routers/platform_admin.py` 已提供用户侧与管理员侧入口。
  - `backend/tests/platform/` 已覆盖 migration、repository、service、runner、router 与 e2e 主链验证。
- 因此本文当前角色已调整为“平台后端主线总账与历史推进记录”：
  - 文中早期 `- [ ]` 勾选框很多没有逐项回填，但不能再直接解读为“功能尚未实现”。
  - 后续执行时，应把本文作为总控口径与历史决策参考，而不是新的单任务起手文档。
- 当前仍在持续推进的平台相关增量，主要以下游专题文档为准：
  - `知识库运行时唯一真源`
  - `服务器模式 CLI / 模型选择与凭据池草案`
  - 其他 2026-04 以后新增的专题文档

## 2026-04-10 当前状态说明

- 本文保留 2026-03-31 立项与推进历史，用于追踪平台主链演进过程。
- 截至 2026-04-10，运行时边界已经收口为 `workstation + web` 两角色；`full` 不再作为当前实现基线。
- 文中涉及 migration flags、compat bridge、compat tests 的内容，均应视为历史过渡方案，不再代表当前代码状态。
- 当前有效收口方案请同时参考：
  - `docs/03-方案/后端专题/已经完成-2026-04-10-兼容层收口与审查缺陷修复实施计划.md`
  - `docs/03-方案/后端专题/已经完成-2026-04-10-后端双角色与服务器内部执行器边界收口方案.md`

## 审阅后修订结论

- 当前仓库没有成熟的旧任务数据库主链，也没有现成 `migrations` 体系，因此不采用旧方案中的 `dual-write + read by flag`。
- 当前后端已经形成 `app/modules/<domain>/application|domain|infra` 的分层习惯；新平台模块必须沿用这一结构，避免另起一套风格。
- 平台模式第一阶段直接纳入管理员接口和步骤协议，不推迟到第二阶段。
- 细颗粒度状态机直接冻结到数据库和代码，不再在实现期收缩为简版枚举。
- 第一阶段不落 `job_item.last_ai_execution_id` 实体字段，避免制造难以保证一致性的“伪最新引用”。
- 当前阶段计费和配额按“次数”实现，底层字段命名保持可扩展，但数据库数值类型优先使用整数。

## 2026-04-09 补充范围说明

- 本次工作站知识库版本治理属于平台后端主链外的本地工作站增量功能，不改动 `job -> job_item -> ai_execution` 平台事实主链。
- 实现范围限定在 workstation/full 本地运行时：
  - 补齐知识库状态检查
  - 提供手动更新任务入口
  - 提供知识库来源/位置说明弹窗
  - 把 codegen 的 Baselib / 游戏反编译路径切到 `runtime/knowledge` 本地缓存优先
- 该增量功能不改变平台数据库 schema、平台任务计费主链和管理员 API 边界。

## 2026-04-10 补充范围说明

- 本次 STS2 项目路径配置同步修复同样属于 workstation/full 本地运行时增量修复，不改动平台数据库 schema、平台任务计费主链和管理员 API 边界。
- 修复范围限定在：
  - `local.props` 生成模板字段纠正
  - 设置页 `sts2_path` / `godot_exe_path` 到项目 `local.props` 的受控同步
  - Build & Deploy 构建前兜底同步
  - 相关测试与文档口径收口

## Decision Freeze

- 平台模式唯一事实主链为 `job -> job_item -> ai_execution`。
- 第一阶段状态枚举采用细颗粒度版本：
  - `job.status`: `draft`, `queued`, `running`, `partial_succeeded`, `succeeded`, `failed`, `quota_exhausted`, `cancelling`, `cancelled`
  - `job_item.status`: `pending`, `ready`, `running`, `succeeded`, `failed_business`, `failed_system`, `quota_skipped`, `cancelled_before_start`, `cancelled_after_start`
  - `ai_execution.status`: `created`, `dispatching`, `running`, `succeeded`, `failed_business`, `failed_system`, `completed_with_refund`
- 创建任务与开始执行分离；`draft -> queued/running` 是真实业务阶段，不用简化状态替代。
- 管理员接口属于第一阶段基线范围。
- 步骤协议属于第一阶段基线范围。
- `workflow_version` 落在 `jobs`。
- `step_protocol_version`、`result_schema_version` 落在 `ai_executions`。
- `job_item.last_ai_execution_id` 第一阶段不落库。
- 配额、计费、返还第一阶段使用整数次数口径。
- 兼容迁移方式冻结为：
  - 新平台表单写
  - 旧 router 保留
  - 新旧入口通过 flag 控制
  - 旧响应若需兼容，从新主链投影，不反向维护旧数据库事实

## Decision Gates

- Gate A: 未补齐数据库基础设施前，不开始 schema 和 repository 实现。
- Gate B: 未冻结平台 DTO、事件 contract、步骤协议字段前，不开始 router 接口实现。
- Gate C: 未跑通平台用户侧查询和管理员查询前，不把旧 `workflow` / `batch_workflow` 改成新委托。
- Gate D: 未稳定轮询事件流前，不启用统一 WebSocket 事件 contract。

## File Map

- `backend/requirements.txt`
  责任：补齐 ORM、migration、PostgreSQL 驱动依赖。
- `backend/main.py`
  责任：注册 legacy routers，预埋平台用户/管理员路由挂载点。
- `backend/routers/workflow.py`
  责任现状：单资产 WebSocket 全流程；目标：薄传输层 + 新 service/runner 委托。
- `backend/routers/batch_workflow.py`
  责任现状：批量 WebSocket 编排；目标：薄传输层 + 新 service/runner 委托。
- `backend/routers/build_deploy.py`
  责任现状：构建发布入口；目标：调用平台 facade。
- `backend/routers/approval_router.py`
  责任现状：审批入口；目标：调用平台 facade。
- `backend/routers/config_router.py`
  责任现状：配置入口；目标：暴露平台 rollout 状态和配置视图。
- `backend/app/composition/container.py`
  责任：平台 persistence、service、runner 的注入根。
- `backend/app/shared/infra/config/settings.py`
  责任：平台 migration/rollout flags 与 DB 配置。
- `backend/app/shared/infra/feature_flags.py`
  责任：把平台 rollout flags 解析成可消费结构。
- `backend/app/shared/infra/db/*.py`
  责任：SQLAlchemy engine、session、base、metadata。
- `backend/app/modules/platform/contracts/*.py`
  责任：用户/管理员 DTO、统一事件 contract、步骤协议 contract。
- `backend/app/modules/platform/domain/models/*.py`
  责任：平台领域模型与枚举。
- `backend/app/modules/platform/domain/repositories/*.py`
  责任：平台读写仓储接口。
- `backend/app/modules/platform/application/services/*.py`
  责任：任务生命周期、查询、额度计费、事件、审批/构建 facade。
- `backend/app/modules/platform/infra/persistence/models/*.py`
  责任：ORM 映射。
- `backend/app/modules/platform/infra/persistence/repositories/*.py`
  责任：仓储实现。
- `backend/app/modules/platform/runner/*.py`
  责任：workflow registry、step dispatcher、execution adapter、runner facade。
- `backend/migrations/*`
  责任：Alembic 环境、版本脚本、后续 schema 演进。
- `backend/tests/platform/*`
  责任：contracts、config、container、migration、repository、service、runner、router 测试。

## 实施范围分期

### Phase 1: 基线冻结与基础设施搭建

目标：
- 冻结平台后端实现基线
- 补齐依赖、DB 配置、migration 基础设施
- 建立平台模块骨架与 contract

### Phase 2: 主链 schema 与 repository

目标：
- 落 `jobs`、`job_items`、`ai_executions`、额度链、事件链、产物链
- 建立读写仓储边界

### Phase 3: service、runner、步骤协议

目标：
- 把生命周期编排、额度计费、事件写入、执行调度从 router 中下沉
- 建立平台步骤协议和 runner 主链

### Phase 4: 用户侧与管理员侧平台 API

目标：
- 新增 `/api/platform/*` 和 `/api/admin/*`
- 保证用户/管理员可见性边界清晰

### Phase 5: legacy router 委托与定向验证

目标：
- 旧入口继续可用
- 旧 `workflow` / `batch_workflow` / `build_deploy` / `approval_router` 逐步变薄

## Chunk 1: 基线、依赖与基础设施

### Task 1: 冻结实现基线并落盘

**Files:**
- Create: `docs/90-归档/后端专题/已经完成-2026-03-31-平台后端与工作站增量实施基线.md`
- Reference: `docs/03-方案/后端专题/已经完成-2026-03-27-后端设计详情.md`
- Reference: `docs/03-方案/后端专题/已经完成-2026-03-27-SQL-DDL草案.md`
- Reference: `docs/03-方案/后端专题/已经完成-2026-03-27-仓储接口草案.md`

- [ ] **Step 1: 写一份基线文档**
  明确状态机、版本字段落点、整数配额口径、管理员接口范围、步骤协议范围、单写新主链策略。

- [ ] **Step 2: 列出实现期不再开放的争议项**
  把 `job_item.last_ai_execution_id` 不落库、第一阶段不做旧事实双写等结论写清楚。

- [ ] **Step 3: 人工检查文档与当前计划一致**
  对照本计划的 `Decision Freeze`，逐项核对。

**Tests:**
- 无代码测试

**Commands:**
- 无

**Expected:**
- 后续实现人员不需要再回到多份草案中做二次裁决。

### Task 2: 初始化数据库依赖与基础设施

**Files:**
- Modify: `backend/requirements.txt`
- Create: `backend/app/shared/infra/db/__init__.py`
- Create: `backend/app/shared/infra/db/base.py`
- Create: `backend/app/shared/infra/db/session.py`
- Create: `backend/alembic.ini`
- Create: `backend/migrations/env.py`
- Create: `backend/migrations/script.py.mako`
- Create: `backend/migrations/versions/.gitkeep`
- Create: `backend/tests/platform/config/test_db_settings.py`
- Create: `backend/tests/platform/persistence/test_db_session_bootstrap.py`

- [ ] **Step 1: 写依赖相关 failing tests**
  验证 `Settings` 能解析 DB 配置，session 工厂能在未连接数据库时正确初始化。

- [ ] **Step 2: 在 `requirements.txt` 增加依赖**
  增加 `SQLAlchemy`、`alembic`、`psycopg[binary]`。

- [ ] **Step 3: 写 DB 基础设施最小实现**
  新增 declarative base、metadata、engine/session 工厂。

- [ ] **Step 4: 初始化 Alembic 目录**
  让后续 migration 有标准入口，而不是临时脚本。

- [ ] **Step 5: 运行定向测试**
  仅跑 DB settings / bootstrap 测试，不跑全量。

**Tests:**
- `backend/tests/platform/config/test_db_settings.py`
- `backend/tests/platform/persistence/test_db_session_bootstrap.py`

**Commands:**
- `pytest backend/tests/platform/config/test_db_settings.py -q`
- `pytest backend/tests/platform/persistence/test_db_session_bootstrap.py -q`

**Expected:**
- 仓库具备可用的 DB 和 migration 基础设施。

### Task 3: 扩展 settings、feature flags 与容器骨架

**Files:**
- Modify: `backend/app/shared/infra/config/settings.py`
- Modify: `backend/app/shared/infra/feature_flags.py`
- Modify: `backend/app/composition/container.py`
- Modify: `backend/main.py`
- Create: `backend/tests/platform/config/test_platform_migration_flags.py`
- Create: `backend/tests/platform/container/test_platform_container.py`

- [ ] **Step 1: 写 settings/flag failing tests**
  验证新增 flag 默认关闭，且不破坏现有 `workflow` migration flags。

- [ ] **Step 2: 扩展平台开关**
  增加 `platform_jobs_api_enabled`、`platform_service_split_enabled`、`platform_runner_enabled`、`platform_events_v1_enabled`、`platform_step_protocol_enabled`。

- [ ] **Step 3: 在容器中注册平台占位项**
  为 repositories、services、runner、session factory 预留解析入口。

- [ ] **Step 4: 在 `main.py` 预留平台路由挂载点**
  默认关闭，不影响当前启动。

- [ ] **Step 5: 跑定向测试**

**Tests:**
- `backend/tests/platform/config/test_platform_migration_flags.py`
- `backend/tests/platform/container/test_platform_container.py`

**Commands:**
- `pytest backend/tests/platform/config/test_platform_migration_flags.py -q`
- `pytest backend/tests/platform/container/test_platform_container.py -q`

**Expected:**
- 平台模块可被容器解析，平台路由注册具备开关入口。

### Task 4: 建立平台 contracts 与模块骨架

**Files:**
- Create: `backend/app/modules/platform/__init__.py`
- Create: `backend/app/modules/platform/contracts/__init__.py`
- Create: `backend/app/modules/platform/contracts/job_commands.py`
- Create: `backend/app/modules/platform/contracts/job_queries.py`
- Create: `backend/app/modules/platform/contracts/admin_queries.py`
- Create: `backend/app/modules/platform/contracts/events.py`
- Create: `backend/app/modules/platform/contracts/runner_contracts.py`
- Create: `backend/app/modules/platform/domain/__init__.py`
- Create: `backend/app/modules/platform/domain/models/__init__.py`
- Create: `backend/app/modules/platform/domain/models/enums.py`
- Create: `backend/app/modules/platform/domain/repositories/__init__.py`
- Create: `backend/app/modules/platform/application/__init__.py`
- Create: `backend/app/modules/platform/application/services/__init__.py`
- Create: `backend/app/modules/platform/infra/__init__.py`
- Create: `backend/app/modules/platform/infra/persistence/__init__.py`
- Create: `backend/app/modules/platform/infra/persistence/models/__init__.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/__init__.py`
- Create: `backend/app/modules/platform/runner/__init__.py`
- Create: `backend/tests/platform/contracts/test_platform_contracts.py`

- [ ] **Step 1: 写 contract failing tests**
  覆盖 create/start/cancel、用户列表/详情/items/events/quota、管理员 executions/refunds/audit、事件 payload、步骤协议 payload。

- [ ] **Step 2: 定义平台枚举和 DTO**
  让细颗粒度状态机和用户/管理员可见性边界进入代码。

- [ ] **Step 3: 定义步骤协议最小字段**
  至少包括 `workflow_version`、`step_protocol_version`、`step_type`、`step_id`、`job_id`、`job_item_id`、`input_payload`、`result_schema_version`。

- [ ] **Step 4: 运行 contract 测试**

**Tests:**
- `backend/tests/platform/contracts/test_platform_contracts.py`

**Commands:**
- `pytest backend/tests/platform/contracts/test_platform_contracts.py -q`

**Expected:**
- 平台 contract 和枚举冻结为代码事实。

## Chunk 2: Schema 与仓储

### Task 5: 落平台主链 schema 与 ORM 映射

**Files:**
- Create: `backend/app/modules/platform/infra/persistence/models/job.py`
- Create: `backend/app/modules/platform/infra/persistence/models/job_item.py`
- Create: `backend/app/modules/platform/infra/persistence/models/ai_execution.py`
- Create: `backend/app/modules/platform/infra/persistence/models/execution_charge.py`
- Create: `backend/app/modules/platform/infra/persistence/models/quota_account.py`
- Create: `backend/app/modules/platform/infra/persistence/models/quota_bucket.py`
- Create: `backend/app/modules/platform/infra/persistence/models/usage_ledger.py`
- Create: `backend/app/modules/platform/infra/persistence/models/artifact.py`
- Create: `backend/app/modules/platform/infra/persistence/models/job_event.py`
- Create: `backend/migrations/versions/20260331_01_create_platform_job_chain.py`
- Create: `backend/tests/platform/migrations/test_platform_job_chain_schema.py`

- [x] **Step 1: 写 schema failing tests**
  覆盖表存在、关键索引、幂等唯一约束、细颗粒度状态字段。

- [x] **Step 2: 写 ORM models**
  映射主链、额度链、事件链和产物链。

- [x] **Step 3: 写首个 migration**
  创建 9 张平台主表以及必需索引、唯一约束、外键。

- [x] **Step 4: 明确版本字段与整数额度字段**
  `jobs.workflow_version`、`ai_executions.step_protocol_version`、`ai_executions.result_schema_version`、额度/计费整数字段。

- [x] **Step 5: 跑 migration 定向测试**

**Tests:**
- `backend/tests/platform/migrations/test_platform_job_chain_schema.py`

**Commands:**
- `pytest backend/tests/platform/migrations/test_platform_job_chain_schema.py -q`

**Expected:**
- 数据库层已经能表达平台任务、配额、事件和产物闭环。

### Task 6: 实现 repository 接口与第一版查询投影

**Files:**
- Create: `backend/app/modules/platform/domain/repositories/job_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/job_query_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/quota_query_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/ai_execution_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/execution_charge_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/quota_account_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/usage_ledger_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/artifact_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/job_event_repository.py`
- Create: `backend/app/modules/platform/domain/repositories/admin_query_repositories.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/job_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/job_query_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/quota_query_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/ai_execution_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/execution_charge_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/quota_account_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/usage_ledger_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/artifact_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/job_event_repository_sqlalchemy.py`
- Create: `backend/app/modules/platform/infra/persistence/repositories/admin_query_repositories_sqlalchemy.py`
- Modify: `backend/app/composition/container.py`
- Create: `backend/tests/platform/repositories/test_job_repository.py`
- Create: `backend/tests/platform/repositories/test_execution_and_quota_repositories.py`
- Create: `backend/tests/platform/repositories/test_job_query_repositories.py`

- [x] **Step 1: 写 repository failing tests**
  覆盖幂等查询、bucket 锁定、事件追加顺序、用户/管理员可见性分离。

- [x] **Step 2: 实现写仓储**
  先做 `JobRepository`、`AIExecutionRepository`、`ExecutionChargeRepository`、`QuotaAccountRepository`、`UsageLedgerRepository`、`JobEventRepository`。

- [x] **Step 3: 实现读仓储**
  完成用户 list/detail/items/events/quota 和管理员 executions/refunds/audit。

- [x] **Step 4: 把仓储注入到容器**

- [x] **Step 5: 跑 repository 定向测试**

**Tests:**
- `backend/tests/platform/repositories/test_job_repository.py`
- `backend/tests/platform/repositories/test_execution_and_quota_repositories.py`
- `backend/tests/platform/repositories/test_job_query_repositories.py`

**Commands:**
- `pytest backend/tests/platform/repositories/test_job_repository.py -q`
- `pytest backend/tests/platform/repositories/test_execution_and_quota_repositories.py -q`
- `pytest backend/tests/platform/repositories/test_job_query_repositories.py -q`

**Expected:**
- service 层可以完全通过 repository 协作，而不是在 router 里拼 SQL/状态。

## Chunk 3: service、runner 与步骤协议

### Task 7: 实现平台 service 主链

**Files:**
- Create: `backend/app/modules/platform/application/services/job_application_service.py`
- Create: `backend/app/modules/platform/application/services/job_query_service.py`
- Create: `backend/app/modules/platform/application/services/execution_orchestrator_service.py`
- Create: `backend/app/modules/platform/application/services/quota_billing_service.py`
- Create: `backend/app/modules/platform/application/services/event_service.py`
- Create: `backend/app/modules/platform/application/services/admin_query_service.py`
- Create: `backend/app/modules/platform/application/services/approval_facade_service.py`
- Create: `backend/app/modules/platform/application/services/build_deploy_facade_service.py`
- Modify: `backend/app/composition/container.py`
- Create: `backend/tests/platform/services/test_job_application_service.py`
- Create: `backend/tests/platform/services/test_execution_orchestrator_service.py`
- Create: `backend/tests/platform/services/test_job_query_service.py`
- Create: `backend/tests/platform/services/test_quota_billing_service.py`

- [x] **Step 1: 写 service failing tests**
  覆盖 create/start/cancel、配额耗尽、自动返还、管理员/用户查询边界。

- [x] **Step 2: 实现 `JobApplicationService`**
  打通创建草稿、确认开始、受理取消。

- [x] **Step 3: 实现 `QuotaBillingService`**
  封装 reserve/capture/refund，按冻结的 v1 refund matrix 实现。

- [x] **Step 4: 实现 `ExecutionOrchestratorService`**
  在启动 execution 前检查取消与配额，创建 execution，回写 job/job_item 聚合。

- [x] **Step 5: 实现 `JobQueryService` 和 `AdminQueryService`**

- [x] **Step 6: 跑 service 定向测试**

**Tests:**
- `backend/tests/platform/services/test_job_application_service.py`
- `backend/tests/platform/services/test_execution_orchestrator_service.py`
- `backend/tests/platform/services/test_job_query_service.py`
- `backend/tests/platform/services/test_quota_billing_service.py`

**Commands:**
- `pytest backend/tests/platform/services/test_job_application_service.py -q`
- `pytest backend/tests/platform/services/test_execution_orchestrator_service.py -q`
- `pytest backend/tests/platform/services/test_job_query_service.py -q`
- `pytest backend/tests/platform/services/test_quota_billing_service.py -q`

**Expected:**
- 平台生命周期规则离开 router 也能独立测试。

### Task 8: 实现 runner 与步骤协议适配

**Files:**
- Create: `backend/app/modules/platform/runner/workflow_registry.py`
- Create: `backend/app/modules/platform/runner/workflow_runner.py`
- Create: `backend/app/modules/platform/runner/step_dispatcher.py`
- Create: `backend/app/modules/platform/runner/execution_adapter.py`
- Create: `backend/app/modules/platform/runner/build_deploy_adapter.py`
- Create: `backend/app/modules/platform/runner/approval_adapter.py`
- Modify: `backend/app/composition/container.py`
- Create: `backend/tests/platform/runner/test_workflow_registry.py`
- Create: `backend/tests/platform/runner/test_workflow_runner.py`
- Create: `backend/tests/platform/runner/test_execution_adapter.py`

- [x] **Step 1: 写 runner failing tests**
  覆盖 workflow 选择、step dispatch、错误分类、事件顺序。

- [x] **Step 2: 实现 workflow registry**
  把 `job_type` / `item_type` 映射到共享 workflow 定义。

- [x] **Step 3: 实现 step dispatcher 与 execution adapter**
  让 service 通过 runner 调用现有 image/code/build/approval 资产，而不是直接 import router 内逻辑。

- [x] **Step 4: 把步骤协议接到统一事件模型**

- [x] **Step 5: 跑 runner 定向测试**

**Tests:**
- `backend/tests/platform/runner/test_workflow_registry.py`
- `backend/tests/platform/runner/test_workflow_runner.py`
- `backend/tests/platform/runner/test_execution_adapter.py`

**Commands:**
- `pytest backend/tests/platform/runner/test_workflow_registry.py -q`
- `pytest backend/tests/platform/runner/test_workflow_runner.py -q`
- `pytest backend/tests/platform/runner/test_execution_adapter.py -q`

**Expected:**
- 现有执行能力被平台 runner 复用，步骤协议进入代码主链。

## Chunk 4: 平台 API 与 legacy router 委托

### Task 9: 新增平台用户侧与管理员侧 API

**Files:**
- Create: `backend/routers/platform_jobs.py`
- Create: `backend/routers/platform_admin.py`
- Modify: `backend/main.py`
- Modify: `backend/app/composition/container.py`
- Create: `backend/tests/platform/routers/test_platform_jobs_http.py`
- Create: `backend/tests/platform/routers/test_platform_admin_http.py`

- [x] **Step 1: 写 HTTP contract failing tests**
  覆盖创建、开始、取消、列表、详情、items、events、quota、admin executions/refunds/audit。

- [x] **Step 2: 实现用户侧平台 API**

- [x] **Step 3: 实现管理员侧平台 API**

- [x] **Step 4: 用 flag 控制挂载**

- [x] **Step 5: 跑平台 HTTP 定向测试**

**Tests:**
- `backend/tests/platform/routers/test_platform_jobs_http.py`
- `backend/tests/platform/routers/test_platform_admin_http.py`

**Commands:**
- `pytest backend/tests/platform/routers/test_platform_jobs_http.py -q`
- `pytest backend/tests/platform/routers/test_platform_admin_http.py -q`

**Expected:**
- 新平台 API 可独立使用，不需要先改旧 WebSocket 入口。
- 2026-03-31 已完成 `backend/routers/platform_jobs.py` 与 `backend/routers/platform_admin.py`，并通过定向 HTTP 测试。

### Task 10: 把 legacy routers 迁移为薄传输层

**Files:**
- Modify: `backend/routers/workflow.py`
- Modify: `backend/routers/batch_workflow.py`
- Modify: `backend/routers/build_deploy.py`
- Modify: `backend/routers/approval_router.py`
- Modify: `backend/routers/config_router.py`
- Modify: `backend/app/composition/container.py`
- Modify: `backend/app/shared/infra/config/settings.py`
- Create: `backend/tests/platform/routers/test_workflow_router_compat.py`
- Create: `backend/tests/platform/routers/test_batch_router_compat.py`
- Create: `backend/tests/platform/routers/test_build_deploy_router_compat.py`
- Create: `backend/tests/platform/routers/test_approval_and_config_router_compat.py`

- [x] **Step 1: 写 compat failing tests**
  验证 flag 关闭时行为不变，flag 打开时走新 service/runner。

- [x] **Step 2: 改 `workflow.py`**
  把创建/执行编排尽量迁出，保留 WebSocket 协议适配。

- [x] **Step 3: 改 `batch_workflow.py`**
  把批量任务、审批、quota、item 状态推进迁到平台主链。

- [x] **Step 4: 改 `build_deploy.py` / `approval_router.py` / `config_router.py`**
  统一走 facade 或 query service。

- [x] **Step 5: 跑 compat 定向测试**

**Tests:**
- `backend/tests/platform/routers/test_workflow_router_compat.py`
- `backend/tests/platform/routers/test_batch_router_compat.py`
- `backend/tests/platform/routers/test_build_deploy_router_compat.py`
- `backend/tests/platform/routers/test_approval_and_config_router_compat.py`

**Commands:**
- `pytest backend/tests/platform/routers/test_workflow_router_compat.py -q`
- `pytest backend/tests/platform/routers/test_batch_router_compat.py -q`
- `pytest backend/tests/platform/routers/test_build_deploy_router_compat.py -q`
- `pytest backend/tests/platform/routers/test_approval_and_config_router_compat.py -q`

**Expected:**
- 旧入口仍可工作，但核心业务规则已经不再驻留在 router 中。
- 2026-03-31 已新增 compat tests，并通过 `13 passed` 的 Task 10 定向兼容验证。

## Chunk 5: 交付检查与回滚边界

### Task 11: 补 rollout 清单与定向集成验证

**Files:**
- Create: `docs/superpowers/checklists/platform-backend-rollout.md`
- Create: `backend/tests/platform/e2e/test_platform_job_lifecycle.py`
- Create: `backend/tests/platform/e2e/test_platform_rollout_flags.py`

- [x] **Step 1: 写 lifecycle/e2e failing tests**
  覆盖 create -> start -> execute -> finish、cancel、quota exhausted、refund、events。

- [x] **Step 2: 写 rollout flag 测试**
  覆盖 legacy only、platform API enabled、service split enabled、runner enabled、events v1 enabled。

- [x] **Step 3: 写 operator checklist**
  明确开关启用顺序和回退顺序。

- [x] **Step 4: 跑定向平台测试集**
  只跑 `backend/tests/platform`，不自动跑全量测试、不自动跑 build。

**Tests:**
- `backend/tests/platform/e2e/test_platform_job_lifecycle.py`
- `backend/tests/platform/e2e/test_platform_rollout_flags.py`

**Commands:**
- `pytest backend/tests/platform/e2e/test_platform_job_lifecycle.py -q`
- `pytest backend/tests/platform/e2e/test_platform_rollout_flags.py -q`
- `pytest backend/tests/platform -q`

**Expected:**
- 平台主链和 rollout flags 有最小集成保障。
- 2026-04-01 已补齐 `quota exhausted` / `refund` e2e 覆盖，并通过：
  - `python -m pytest backend/tests/platform/e2e/test_platform_job_lifecycle.py -q`
  - `python -m pytest backend/tests/platform/e2e/test_platform_rollout_flags.py -q`

## 首批改造清单

首批只做以下内容，不跨到 schema/service/router 委托：

1. `Task 1`: 基线文档落盘
2. `Task 2`: 数据库依赖与基础设施
3. `Task 3`: settings、feature flags、容器骨架
4. `Task 4`: 平台 contracts 与模块骨架

首批完成标准：

- 仓库已有 DB / migration 基础设施
- 平台模块目录已经成形
- DTO / event / step protocol contract 已冻结为代码
- 新平台 flags 已接入但默认关闭
- `main.py` 已能挂载平台路由占位而不影响现状
- 不要求跑 build，不要求跑全量测试

## 风险与防线

- 风险：当前仓库没有既有 DB 栈，首批就要补基础设施。
  防线：先只打通基础设施和 contract，不在同一批引入 schema/service/router 逻辑。

- 风险：细颗粒度状态机会放大 service 和事件映射复杂度。
  防线：先把状态枚举冻结到 `contracts` 和 `domain/models/enums.py`，再写 schema。

- 风险：步骤协议和 legacy WebSocket 合同并存，容易出现事件顺序漂移。
  防线：统一事件模型先落 `contracts/events.py`，legacy router 只做适配，不重复发明结构。

- 风险：管理员接口一期纳入后，查询边界容易和用户接口混淆。
  防线：管理员 DTO、查询仓储、router 单独成组，不复用用户视图对象。

## Rollout Order

1. 落基线文档、依赖、DB 基础设施和 flags。
2. 落平台 contracts 与模块骨架。
3. 落 schema 与 repositories。
4. 落 services 与 runner。
5. 落平台用户 API 与管理员 API。
6. 最后把 legacy routers 逐步改成薄委托。

## 执行提醒

- 默认只做定向验证，不自动跑 build 或全量测试。
- 若执行过程中发现 SQLAlchemy / Alembic 版本或 DB 驱动与环境冲突，应先停在基础设施阶段处理，不要继续推进后续 schema/service 工作。
- 若步骤协议字段集在实现期被迫扩大，必须先回写基线文档，再继续实现。

## 2026-04-07 补充约束

- 工作站五类工作流日志统一收敛到共享日志条目模型，允许在 `stream` / `agent_stream` / `item_agent_stream` 上附带 `source`、`channel`、`model` 元信息。
- 前端日志展示统一为单面板双标签：默认显示 `优化后输出`，允许用户切换到 `原始输出`。
- 模型显示规则固定为“展示当前实际调用模型名，不展示整份配置，不泄露无关敏感设置”。
- 本次补充属于工作站展示层与兼容 websocket 契约增强，不改变平台任务主链、步骤协议版本字段和数据库事实源。

## 2026-04-09 补充约束

- 单资产工作流的 `Code Agent` 面板首轮改为“执行播报”布局；该布局只覆盖代码生成阶段，不追溯生图阶段，也不要求同日扩展到批量、Mod 修改、构建部署与日志分析页面。
- `执行播报` 仍然从共享日志条目模型派生，禁止 AI 二次改写原始输出；首轮只允许做固定 6 步进度、单段状态说明、摘要说明和技术细节折叠。
- 代码生成固定步数冻结为：
  1. `理解任务`
  2. `扫描项目`
  3. `定位改动点`
  4. `生成修改方案`
  5. `写入代码`
  6. `自检收尾`
- 顶部状态头固定展示 `当前模型`、当前模式（`执行播报 / 原始输出`）和 `X / 6 已完成`；其中 `X / 6` 属于代码生成展示层进度，不得冒充真实数据库状态字段。
- `原始输出` 继续保留完整原始文本回放能力；`执行播报` 的职责是降低 log 感、提升阅读节奏，而不是替代排障视图。

## 2026-04-08 补充约束

- `tools/latest/deploy-docker.ps1` 以本机模式拉起的 `workstation` / `frontend` 服务，必须把 PID 写入 `release/runtime/local-deploy-state.json`，不能只依赖日志窗口存在与否。
- mixed deploy 的日志终端仅作为查看器，不承担进程生命周期管理；关闭日志终端不等于停止后台服务。
- 仓库必须提供显式停机入口 `tools/latest/stop-deploy.ps1`，按状态文件停止本地服务并清理日志窗口记录。
- `tools/latest/deploy-docker.ps1 hybrid` 在默认联动本机 `web-backend` 且需要刷新默认 `agentthespire-web-release` 时，必须先对该 Compose 项目执行保护性 `down --remove-orphans`，避免在运行中的 Compose 项目上直接重写 release 目录。
- `tools/latest/deploy-docker.ps1 web` 必须把 `SPIREFORGE_AUTH_SESSION_SECRET` 与 `SPIREFORGE_SERVER_CREDENTIAL_SECRET` 收口到 `release/runtime/.env`，由 Compose 注入容器；不得继续把 `auth.session_secret` 作为 `runtime/web.config.json` 的持久化字段。
- 同名 `web release` 重新打包时，必须保留既有 `runtime/.env`，避免 secret 漂移；正式部署前还必须把 release 内 `docker-compose.yml` 刷新为当前模板，避免旧 release 遗留的 Compose 环境注入方式继续生效。
- `workstation` 本地 Python 运行时缓存固定落在 `release/runtime/python-runtime/workstation`，并按 `requirements.txt` 指纹与启动 Python 路径做最小失效判断，避免每次重打包后重复准备依赖。
- `package-release.ps1` 生成发布 zip 时，默认排除 `release/runtime/python-runtime` 本地缓存目录；该缓存只用于本地部署复用，不再作为归档发布内容进入 zip。
- 工作站应用级配置真源冻结为 `runtime/workstation.config.json`；后续实现不得再把仓库根目录 `config.json`、`services/workstation/config.json` 或历史 Docker 派生配置作为并列运行时真源。
- `services/workstation/config.json` 如在迁移期仍需保留，只能作为兼容占位或派生镜像，不能继续拥有独立配置所有权。
- Codex CLI / Claude CLI 的用户全局配置属于外部环境边界，不属于项目配置真源；后续设置摘要与运行时诊断必须把“项目真源”和“CLI 外部来源”分开表达。
- Windows 下 Claude CLI 的 agent 调用不得再经由 `claude.cmd` 批处理包装层直接承载长 prompt；实现必须优先走 `node + .../claude-code/cli.js` 或等效非 `cmd.exe` 路径，避免命令行长度上限导致 `The command line is too long.`。
- Windows 下 Claude CLI 的规划/文本补全链路也必须复用与 agent 调用一致的 launcher 解析逻辑；禁止直接使用 `subprocess(["claude", ...])` 裸启动，避免 `WinError 2` 与批处理包装层兼容问题。
- 设置面板中的 `CLI 模型（可选）` 在 `agent_cli` 模式下必须对所选代理后端真实生效；当前基线要求 Codex CLI 与 Claude CLI 都透传该字段，而不是只在 UI 层保存。

Plan complete and saved to `docs/03-方案/后端专题/总账-2026-03-31-平台后端与工作站增量实施计划.md`. Ready to execute?


