## 1. 前端访问层收口

- [x] 1.1 为前端补充运行时 `workstation` / `web` HTTP endpoint 解析能力
- [x] 1.2 为前端补充运行时 `workstation` WebSocket endpoint 解析能力
- [x] 1.3 将工作台 HTTP API 调用点从默认同源切换为显式 `workstation` 目标
- [x] 1.4 将工作台 WebSocket 封装统一切换到新的 WS 解析层
- [x] 1.5 为 endpoint 缺失或错误场景补充显式前端错误提示
- [x] 1.6 完成前端访问层任务后提交一次独立 commit

## 2. 运行时配置注入

- [x] 2.1 为独立前端模板增加运行时 endpoint 配置注入方案
- [x] 2.2 明确运行时配置文件格式与默认生成位置
- [x] 2.3 完成运行时配置注入任务后提交一次独立 commit

## 3. 本地启动器与发行结构

- [x] 3.1 设计“单安装包 + 两个本地进程”的发行目录结构
- [x] 3.2 增加本地启动器，负责端口检查、启动 workstation、生成前端运行时配置、启动本地静态前端服务并打开浏览器
- [x] 3.3 调整 release 模板，明确前端静态资源、workstation 与 launcher 的归档方式
- [x] 3.4 完成本地启动器与发行结构任务后提交一次独立 commit

## 4. 后端跨域与会话边界

- [x] 4.1 收敛 `workstation` / `web` 运行时来源配置，支持独立前端来源
- [x] 4.2 复核独立前端下的 Web 会话恢复、登录与登出边界
- [x] 4.3 明确第一版仅支持本机或 LAN 可达 `workstation-backend` 的运行约束
- [ ] 4.4 完成后端跨域与会话边界任务后提交一次独立 commit

## 5. 文档、验证与验收

- [ ] 5.1 为前端 endpoint 解析与工作台/平台分流补充针对性测试
- [ ] 5.2 为独立前端下的工作台 WebSocket 连接补充针对性测试
- [ ] 5.3 更新仓库文档，说明兼容态、工作站托管态与独立前端态的差异
- [ ] 5.4 以“独立前端 + workstation + web”场景完成验收步骤记录
- [ ] 5.5 完成文档、验证与验收任务后提交一次独立 commit
