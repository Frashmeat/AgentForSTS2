## 1. OpenSpec 与运行时配置契约

- [ ] 1.1 完成 `align-cli-runtime-config` 的 proposal / design / spec 文档
- [ ] 1.2 明确 Claude CLI / Codex CLI 的“保存值、运行时生效值、外部覆盖来源”语义边界

## 2. 后端 CLI 运行时配置对齐

- [ ] 2.1 为 Codex CLI 补充受控配置注入与用户全局配置隔离策略
- [ ] 2.2 为 Claude CLI 补充 `model` 等关键参数传递，并固定受控设置来源
- [ ] 2.3 为共享运行时摘要增加后端组装逻辑，输出保存值、生效值与覆盖告警
- [ ] 2.4 为后端配置对齐补充定向测试

## 3. 设置接口与前端可观测性

- [ ] 3.1 扩展设置接口返回结构，补充运行时生效摘要
- [ ] 3.2 调整设置页，展示保存值、生效值与覆盖风险提示
- [ ] 3.3 为前端设置摘要展示补充定向测试

## 4. 验证与收口

- [ ] 4.1 验证 Claude / Codex 在存在用户全局 CLI 配置时仍按应用设置运行
- [ ] 4.2 验证设置页能正确解释“保存值”和“实际生效值”的差异
- [ ] 4.3 更新相关文档，记录配置保存位置、发布保留策略与 CLI 全局配置隔离规则
