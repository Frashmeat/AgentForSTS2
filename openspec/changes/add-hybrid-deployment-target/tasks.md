## 1. OpenSpec 与计划

- [x] 1.1 建立 `hybrid` 变更 proposal / design / tasks / spec 文档
- [x] 1.2 写入实现计划文档，明确 `full` 与 `hybrid` 的边界

## 2. 打包与部署脚本

- [x] 2.1 为 `package-release.ps1` 增加 `hybrid` 目标
- [x] 2.2 为 `deploy-docker.ps1` 增加或明确 `hybrid` 目标语义
- [x] 2.3 补充 `hybrid` 的定向测试或脚本级验证
- [ ] 2.4 完成脚本任务后提交一次独立 commit

## 3. 文档与验收

- [x] 3.1 更新 README，明确 `hybrid` 是正式推荐形态
- [x] 3.2 更新 tools 文档，区分 `hybrid`、`full` 与 `split-local`
- [x] 3.3 更新决策文档与验收文档，说明用户侧 / 服务器侧部署方式
- [ ] 3.4 完成文档任务后提交一次独立 commit
