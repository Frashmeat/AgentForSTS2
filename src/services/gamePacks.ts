// Slice 1 only ships the verified STS2 pack. A later registry API will replace
// this single installed-pack selection without introducing an implicit Core fallback.
export const INSTALLED_GAME_PACK_ID = "sts2";
