# Rust
target/
**/*.rs.bk

# sqlx offline cache — re-generated by `cargo sqlx prepare`
# .sqlx/  # uncomment once we adopt sqlx prepare in CI

# Node / Vite
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Local env / signing
.env
.env.local
*.key
*.pem

# Active runtime config (contains secrets / machine paths); ship .example only
runtime/agentthespire.config.json
